# GDD — Crafteo: materiales por tier, refinamiento y recetas

**ESTADO: APLICADA (2026-08-29), verificada — AMPLIADA (2026-08-30) con fallback al suelo y desgaste de herramientas.** Arquitectura acordada en conversación con el streamer para el sistema de crafteo que `docs/GDD_Profesiones.md` dejó como "pendiente, oficio a oficio". Aquí se fija la FORMA del sistema (dos capas, catálogos, cómo persiste) y se implementa el MECANISMO completo con una receta representativa por familia — el árbol exhaustivo de recetas de cada uno de los 38 oficios sigue siendo trabajo posterior, oficio a oficio, con este contrato ya construido y probado. La ampliación del 30-08 nació de contrastar una propuesta externa genérica de "sistema de crafteo por capas" — casi todo lo que pedía ya estaba construido con nombre propio (ver §5); solo faltaban dos piezas reales, ambas aplicadas: entrega al suelo si no cabe, y desgaste de herramientas de gate. Verificado: `inventario.test.ts` +1 test (durabilidad de cuchillos), suite completa de servidor 675/675, `tsc --noEmit` limpio en server y client.

**Aplicado**: `insumos` real en `DatosProduccion` (`server/src/construccion/produccion.ts`) — el refinamiento consume stock de verdad, capado por el insumo más escaso, mismo cálculo perezoso de siempre; `server/src/construccion/crafteo.ts` (nivelDeXp, validarCrafteo, crafteoListo) — capa activa; 14 materiales refinados tier1/tier2 + `familiaMaterial`/`tier` en los 24 recursos crudos ya existentes (`items/catalogo/items.json`); 6 plantillas de refinamiento nuevas (fundicion, forja_aleaciones, cantera, planta_curtido, hilanderia, taller_lapidario) + retrofit de `aserradero` con insumo real (`interiores/catalogo/tipos_edificio.json`); `items/catalogo/recetas.json` con 7 recetas representativas (una por familia, varias transversales); protocolo Colyseus `refinamiento:depositar`/`crafteo:iniciar`/`crafteo:recolectar` (`RoomExteriorBase.ts`); tabla `jugador_oficios` (XP por oficio, nivel derivado en código puro, nunca persistido). Verificado: 21 tests puros nuevos (produccion.test.ts, crafteo.test.ts, datos.test.ts), 275/275 tests de servidor, `tsc` limpio, y E2E manual con servidor real + reinicio: fundición refina hierro→lingote_hierro con insumo real (sobrevive al reinicio), crafteo activo clavos_hierro completo (insumo descontado al iniciar, tiempo real, XP otorgada, ítem entregado), y casos negativos (mesa incorrecta, nivel insuficiente) — 12/12 comprobaciones OK.

## 0. Concepto (pedido literal del streamer, 2026-08-29)

Escalera de materiales POR FAMILIA (metal, madera, piedra, cuero, tela, precioso — cada una con su propia cadena, no un tier global): material base → refinado(s) → refinado(s) más avanzados, mezclando entre tiers y entre familias. Esos materiales alimentan recetas de CUALQUIER cosa (armas, armaduras, comida, muebles, herramientas, accesorios, y materiales de otros oficios). Una receta exige: plano desbloqueado + nivel de habilidad/oficio + mesa/zona correcta.

## 1. Dos capas, dos ritmos distintos

**Refinamiento (tier N → tier N+1): automatizado, "más Factorio"** — extiende `resolverProduccion` (docs/GDD_Produccion.md) con INSUMOS reales que se consumen (hoy la colmena/aserradero producen "de la nada"; el refinamiento consume stock de verdad). Cálculo perezoso, cero tick, igual que hoy: se resuelve al tocar la construcción. El trabajador (ya existe) activa la línea; la energía motriz (ya existe, `energia.consume`/`multiplicador`) acelera el ritmo — es EL caso de uso para el que Motriz se diseñó (fragua con martillo pilón mecanizado, refinando mineral→lingote más rápido que a mano). El transporte NPC (ya existe) mueve el output de una estación al insumo de la siguiente sin tocar nada de logística — mina→fragua→forja es una cadena de 3 construcciones con 2 contratos, con la maquinaria que ya existe.

**Crafteo del objeto final: activo, disparado por el jugador** — mesa correcta + plano desbloqueado + nivel de oficio suficiente, consume del INVENTARIO del jugador (no de un almacén automatizado), tarda un tiempo base multiplicado por `factorVelocidadPorEnergia` si la mesa está conectada a la red motriz (el gancho ya existe desde Motriz, sin usar todavía). Recetas TRANSVERSALES: una espada pide acero (herrero) + empuñadura de cuero curtido (curtidor) — al jugador solo le importa tener ambos en el inventario, no de qué oficio salió cada uno.

## 2. Familias y tiers — taxonomía de arranque

Cada familia es una escalera INDEPENDIENTE (el streamer confirmó: por familia, no global). Ejemplo completo con metal (el caso más claro, mina→fragua→forja) y esquema ligero para el resto — se rellena oficio a oficio cuando toque, mismo patrón:

| Familia | Tier 0 (crudo, ya en items.json) | Tier 1 (refinado, NUEVO) | Tier 2 (avanzado, NUEVO) |
|---|---|---|---|
| Metal | hierro, cobre, estano, oro, plata, platino, plomo | lingote_hierro, lingote_cobre, lingote_estano, lingote_oro, lingote_plata, lingote_platino, lingote_plomo | acero (lingote_hierro+carbon), bronce (lingote_cobre+lingote_estano) |
| Madera | madera_blanda, madera_abedul, madera_sauce, madera_palmera | **madera_dura ya existe y ya la produce el aserradero de Producción** — encaja sin tocar nada | madera_tratada (madera_dura+resina, NUEVO) |
| Piedra | piedra_comun, cuarzo | piedra_tallada, cristal_pulido (picapedrero/vidriero, ya dados de alta) | piedra_reforzada (piedra_tallada+metal) |
| Cuero/piel | cuero_grueso, cuero_reptil, piel_basta, piel_fina, piel_invierno, piel_exotica | cuero_curtido (curtidor, ya dado de alta) | cuero_endurecido (cuero_curtido+resina o metal) |
| Tela | fibra_vegetal | tela_hilada (sastre) | tela_reforzada |
| Precioso | gema | gema_tallada (joyero) | (entra directo en receta final, engarzada — no necesita tier 2 propio) |

**Comida NO sigue este patrón** — es ingrediente→plato, no una escalera de "dureza"; se craftea directo con el cocinero, fuera de esta taxonomía.

Nota real encontrada al mirar el catálogo: `madera_dura` YA es, sin que nadie lo llamara así, un tier 1 de la familia madera — el aserradero de Producción ya lo produce. Es la prueba de que el patrón encaja con lo que ya existe, no una invención nueva.

## 3. Catálogo — extensión de `items/catalogo/items.json` (aditivo)

```json
"lingote_hierro": {
  "peso": 1, "apilable": true, "stackMax": 20,
  "familiaMaterial": "metal",
  "tier": 1
}
```

`familiaMaterial` (string|null) y `tier` (entero, 0 = crudo) son campos NUEVOS opcionales — los ítems que ya existen sin ellos siguen funcionando exactamente igual (aditivo puro, mismo criterio que `energia`/`produccion` en construcción). El tier 0 de cada familia son los recursos crudos que YA tiene `categoriaRecurso` — solo hace falta añadirles `familiaMaterial`/`tier:0`, no crear nada.

## 4. Refinamiento — extensión de `DatosProduccion` (`server/src/construccion/produccion.ts`)

```ts
export interface InsumoProduccion {
  itemId: string;
  /** Consumido del almacén de la construcción por cada unidad de output producida. */
  cantidadPorUnidad: number;
}

export interface DatosProduccion {
  itemId: string;
  cantidadPorIntervalo: number;
  intervaloHoras: number;
  capacidadMax: number;
  requiereTrabajador?: boolean;
  /** NUEVO: si está presente, el refinamiento consume esto — sin insumos, se comporta EXACTAMENTE como hoy (colmena/aserradero, "de la nada"). */
  insumos?: InsumoProduccion[];
}
```

`resolverProduccion` necesita un dato más: el stock disponible de cada insumo en el almacén de la construcción (mismo `extra` que ya guarda el acumulador de output) — la producción se capa por el insumo más escaso, igual que `resolverTransporte` ya capa por stock/hueco. Sin insumos declarados, cero cambio de comportamiento (colmena/aserradero de Producción siguen igual). El almacén de insumos de una estación de refinamiento reutiliza el mismo mecanismo que un tenderete (`tenderete_items` — GDD_Mercado.md) para "cuánto stock tengo aquí", en vez de inventar una tercera forma de guardar cantidades.

## 5. Crafteo final — nuevo `items/catalogo/recetas.json` + protocolo `crafteo:*`

```json
"espada_hierro_basica": {
  "oficio": "herrero_armas",
  "mesas": ["yunque", "martillo_pilon"],
  "nivelMinimo": 2,
  "planoRequerido": "plano_espada_hierro_basica",
  "insumos": [
    {"itemId": "acero", "cantidad": 2},
    {"itemId": "cuero_curtido", "cantidad": 1}
  ],
  "resultado": {"itemId": "espada_hierro_basica", "cantidad": 1},
  "tiempoBaseSeg": 30
}
```

Flujo: jugador junto a una mesa con `temasProfesion` compatible → `crafteo:iniciar {recetaId}` → servidor valida (dueño del plano, nivel de oficio, insumos en el inventario, mesa correcta) → descuenta insumos → aplica `factorVelocidadPorEnergia` (ya existe, `server/src/construccion/energia.ts`) al `tiempoBaseSeg` → tras el tiempo, entrega el resultado al inventario (mismo mecanismo que "coger"). Mismo criterio de validación server-autoritativa que el resto del proyecto — el cliente solo sugiere.

**Entrega: al inventario, o al suelo si no cabe (ampliación 2026-08-30)**. Antes, si la mochila no tenía hueco/peso libre al recolectar, el crafteo daba error y el jugador se quedaba SIN el resultado (los insumos ya se habían descontado al iniciar) — el material se perdía sin más. `manejarCrafteoRecolectar` ahora usa `entregarOSoltar` (`RoomExteriorBase.ts`): si no cabe (hueco O peso), el resultado se materializa a los pies del jugador como `ObjetoMundoSchema`, mismo mecanismo que "soltar" manual — nunca se pierde. `crafteo:completado` manda `enSuelo:boolean` para que el cliente lo indique. Mismo fallback aplicado a `cocina:preparar`/ensalada/bocadillo (`docs/GDD_Cocina.md`).

**Herramientas "de gate" con desgaste real (ampliación 2026-08-30)**. `cuchillo_desollar` (desollar cadáver, raspar piel salada, `animal:sacrificar`) y `cuchillo_cocina` (cortar ensalada, cortar pan) solo se validaban por TENENCIA — nunca perdían durabilidad. Ahora ambas llevan `durabilidadMax:40, desgastePorUso:1` en `items.json` y cada uso llama a `registrarUso`/`estaRoto` (`server/src/inventario/desgaste.ts`, el MISMO sistema que ya desgastaba armas/armaduras — nada nuevo, solo enganchado) vía el helper `usarHerramientaDeGate`. Un cuchillo roto (0 durabilidad) bloquea la acción con error explícito — sin sistema de reparación todavía, hay que reemplazarlo. Aprovechado también para corregir una clave `cuchillo_desollar` DUPLICADA en `items.json` (JSON.parse se quedaba silenciosamente con la última, sin bug funcional pero sucio).

`edificioRequerido` (campo opcional añadido en `docs/GDD_Barcos.md`, pedido
2026-08-30): además de la mesa correcta, exige que exista una construcción
viva de ese id (`interiores/catalogo/tipos_edificio.json`) en el
asentamiento actual — comprobado en `manejarCrafteoIniciar`
(`RoomExteriorBase.ts`), NO en `validarCrafteo` (que se mantiene pura, sin
`ContextoConstruccion`). Sirve para atar una receta a un edificio especial
del jarl (p.ej. barcos ↔ `astillero`) sin que baste con tener la mesa suelta.

## 6. Nivel de oficio (XP) — tabla nueva en BD

El streamer confirmó las DOS cosas del punto 4 combinadas, no una u otra: XP por oficio (sube con el uso, como ya decía el Backlog) Y tier de material como gate — se unen así: el nivel de oficio (derivado de XP) es lo que DESBLOQUEA qué tier puedes tocar, tanto para operar una estación de refinamiento de tier alto (asignarte como trabajador) como para intentar una receta de tier alto.

```sql
CREATE TABLE IF NOT EXISTS jugador_oficios (
  jugador_id INTEGER NOT NULL,
  oficio TEXT NOT NULL,
  xp INTEGER NOT NULL DEFAULT 0,
  PRIMARY KEY (jugador_id, oficio)
);
```

Nivel = función pura de XP (tabla de umbrales, ej. nivel 1=0xp, 2=100xp, 3=300xp...) — no se persiste el nivel en sí, se deriva, mismo espíritu que "nunca dupliques una fuente de verdad". XP sube al completar un crafteo activo (§5) — el refinamiento pasivo (§4) NO da XP (es automatizado, no es "el jugador practicando el oficio").

**Investigado 2026-09-07 (pedido streamer: "revisar si cada nivel de mesa tiene blueprints adecuadas en cada oficio")**: auditando `recetas.json` se confirmó que NINGÚN oficio tiene receta con `nivelMinimo` por encima de 5. Para herrero, una primera lectura ("las 36 piezas `_bonificado(a)` nunca se pueden fabricar, solo caen de loot") resultó ser una falsa alarma propia — SÍ tienen un camino de crafteo real vía `resultadoPerfecto` (§7ter, un golpe 5★ en el minijuego de forja de la receta base las entrega directamente) que el primer script de auditoría no comprobó. El hueco real que SÍ sigue en pie: el nivel de oficio EN SÍ (más allá del minijuego de forja, que solo existe para herrero) no desbloquea ninguna receta nueva por encima de 5 en ningún oficio — sin cerrar todavía, pendiente real. Detalle completo (incluida la corrección) en `docs/GDD_Combate.md` §11sexies.

## 7bis. Planos ligados a mesas + niveles de oficio (pedido 2026-08-30)

Cierra varias de las preguntas abiertas de §7 (versión anterior de este documento). Confirmado por el streamer:

- *"los planos nuevos los vinculamos a mesas, si construyo nueva mesa mejor tengo mejores y más planos o blueprint, eso con cada oficio, empiezan con blueprints básicos"* — `RecetaCrafteo.planoRequerido?: string` (el campo ya existía en el schema, v1 lo dejaba sin comprobar) pasa a validarse de VERDAD: si una receta lo declara, exige que ESA construcción (típicamente una mesa de tier avanzado, un `EntradaConstruible`) exista YA levantada en el asentamiento — **exactamente el mismo mecanismo y el mismo código** que `edificioRequerido` (existencia en `ctx.vivas`, comprobado en `RoomExteriorBase.manejarCrafteoIniciar`, NO en `validarCrafteo` que sigue pura). No hay un "plano" persistido por jugador: el desbloqueo es "la mesa avanzada existe en el asentamiento", coherente con "si construyo una mesa mejor, tengo más planos" — construirla es lo que los desbloquea, para todo el asentamiento. Ausente = plano básico, cualquiera con la mesa normal puede intentar la receta desde el arranque ("empiezan con blueprints básicos").
- *"los niveles de oficio permiten poder usar mejores mesas, construir o poner las mejoras de mesa"* — nuevo campo `EntradaConstruible.nivelOficioMinimo?: { oficio, nivel }` (mesas en `interiores/catalogo/elementos.json`, o piezas exteriores en `exteriores.json`): al intentar `construir` esa pieza, `RoomExteriorBase.ts` comprueba `nivelDeXp(xpOficio) >= nivel` (mismo `obtenerXpOficio`/`nivelDeXp` que ya usa el resto de crafteo) ANTES de dejar colocarla — gatea tanto construir una mesa de tier alto como poner una mejora de mesa suelta, con el mismo campo (una "mejora" es, mecánicamente, otra pieza más de `EntradaConstruible`). Ausente = cualquiera construye (comportamiento de siempre, la inmensa mayoría de construibles).
  - "Tener alguna blueprint exclusiva por nivel" ya estaba cubierto por `RecetaCrafteo.nivelMinimo`, que `validarCrafteo` YA comprobaba desde antes de esta pasada — no hizo falta código nuevo, solo asignar el número en la receta cuando toque.
- *"por cada crafteo de esa blueprint asigna tú la cantidad de xp que da para subir nivel de oficio"* — `RecetaCrafteo.xpOtorgada?: number`, usado en `manejarCrafteoRecolectar` (`receta.xpOtorgada ?? XP_POR_CRAFTEO`) — ausente = cae al valor global de siempre, para no obligar a rellenar TODAS las recetas existentes de golpe.

**Sigue como contenido pendiente, no mecanismo** (pedido literal: *"las asignas tú y si no deja apuntado asignarlas a futuro"*): qué mesas concretas llevan `nivelOficioMinimo`, qué recetas llevan `planoRequerido`/`xpOtorgada` y con qué valores — es trabajo de catálogo oficio a oficio, igual que el resto de §7. El mecanismo de los tres campos está completo y probado (tsc + suite 785/785); les falta contenido real, no código.

**`nivelOficioMinimo` ya relleno para los 10 oficios de jugador finales** (docs/GDD_Profesiones.md §0, 2026-08-30): las mesas de nivel 1-4 de cada oficio ya llevan el campo. Esa misma pasada añadió, además, un mecanismo NUEVO y distinto — `EntradaConstruible.mejoraMesa` (módulos de velocidad/cantidad colocados ADYACENTES a una mesa, no un requisito de nivel para construirla) — ver GDD_Profesiones.md §0 para el detalle completo. `planoRequerido`/`xpOtorgada` siguen sin contenido real todavía.

## 7ter. Minijuego de forja — armas y armaduras de herrero (pedido 2026-09-01)

Partiendo de un prototipo externo (server+client con fases calentar/forjar/templar) que el streamer subió pidiendo opinión: *"generar para algunas [oficios] estos minijuegos en ciertos crafteos (en el de las armas y armaduras) por que podria conseguir bonus si lo hace perfecto"*, confirmando el alcance exacto: *"solo para las armas y armaduras, el resto de crafteos del errero no se hacen con el minijuego"*.

**Alcance — exactamente 30 recetas de herrero** (`items/catalogo/recetas.json`), las 9 de tipo `arma` (daga, espada_corta, hacha_combate, maza_guerra, lanza, arco_corto, espada_larga, arco_largo, ballesta) y las 21 de armadura de hierro/acero (gafas/coderas/manos/rodilleras/casco/brazos/zapatos/hombreras/piernas/mascara en ambos tiers + pechera_cota_malla + pechera_placas_acero) — **NO** las armaduras de cuero (oficio curtidor) ni las herramientas/instrumentos del propio herrero, que siguen el crafteo normal por temporizador (§5) sin tocar. Cada una de estas 30 recetas lleva ahora `minijuego: "herreria"` y `resultadoPerfecto: {itemId, cantidad}`.

**Motor puro** — `server/src/construccion/herreria.ts` (mismo patrón que `arenaCombate.ts`): 3 fases sobre una `SesionForja` mutada por acción, sin tick de servidor (dt perezoso desde `ultimaAccionEn`, igual que el resto de crafteo/curtido):
- **CALENTAR**: `avivarFuego` sube la temperatura y consume combustible (5 cargas); pasa a FORJAR a los 68°.
- **FORJAR**: 12 `golpearYunque` — la calidad de cada golpe sale de la posición de la aguja de ritmo **en el instante exacto** en que llega el mensaje. Server-autoritativo de verdad: la aguja la simula el propio servidor (`avanzar`, dentro de `herreria.ts`), nunca se confía en un `timing` que mande el cliente (a diferencia del prototipo original, que sí dejaba que el cliente declarase su propio timing).
- **TEMPLAR**: 1 acción, ventana de temperatura (no de timing — la temperatura ya baja sola, llegar tarde también penaliza).

**Que no salga siempre perfecto** (pedido: *"que fuera algo mas complicado para no salga siempre perfecta"*) — dos ejes de dificultad, ninguno presente en el prototipo original:
1. La velocidad de la aguja se **re-sortea tras cada golpe** (`rnd` inyectable, mismo criterio que `tirarHuida`) y crece con el progreso — no hay un tempo fijo que memorizar.
2. La ventana de "perfecto" se **estrecha según avanza la forja** (±0.13 en el primer golpe → ±0.08 en el último).

**Resultado perfecto = objeto de catálogo nuevo, no un bonus oculto en la instancia.** Decisión de diseño clave, verificada contra el código real antes de implementar: `SlotsEquipo` (`server/src/inventario/inventario.ts`) guarda **solo el itemId** por slot (`{[slot]: itemId}`) — al equipar, `equiparItem` descarta la instancia entera (durabilidad incluida) y `calcularStatsEquipo` lee `ataqueFisico`/`defensaFisica` **directo del catálogo por itemId**. Un "bonus" guardado como campo opcional en `ItemInstancia` (como si fuera `durabilidad`) se perdería en el momento de equipar — no sobrevive el pipeline real. Por eso un golpe perfecto (5★, `resultadoForja`) entrega **`receta.resultadoPerfecto`**: el mismo tipo de objeto pero una entrada de catálogo real aparte (`<id>_bonificado/a(s)`, 30 nuevas en `items/catalogo/items.json`, nombre calculado con `nombreBonito.js` como manda la regla de CLAUDE.md), mismo aspecto (`prendaId` sin cambios — "aunque se vea igual"), con `ataqueFisico`/`defensaFisica` +25% **fijo** (no aleatorio: el catálogo es la fuente de verdad, nada de un porcentaje calculado en caliente) — exactamente el mismo criterio que ya usa el catálogo para `casco_cuero`/`casco_hierro`/`casco_acero` (tres entradas de tier, no un multiplicador).

**Bloqueo de movimiento** (pedido: *"cuando este en el minijuego el pj no podra moverse, esta en el minijuego hasta que complete"*) — mismo mecanismo que el combate táctico (§9.3 de GDD_Combate.md): mientras `forjasEnCurso.has(sessionId)`, el handler genérico de `"input"` fuerza `x=y=0, correr=false`, servidor-autoritativo (no depende de que el cliente coopere).

**Protocolo** (`RoomExteriorBase.ts`) — arranca con el `crafteo:iniciar` de siempre (misma validación de mesa/nivel/insumos, insumos descontados AL INICIAR igual que el resto de crafteo, nunca devueltos si se cancela); si `receta.minijuego==="herreria"` responde `crafteo:herreria:iniciado` (con `cfg`/`sesion`) en vez de `terminaEn`. Mensajes nuevos: `crafteo:herreria:accion {accion:"avivar"|"golpear"|"templar"}` → `crafteo:herreria:progreso` (o, tras templar, resuelve y entrega de verdad: XP, suciedad, `entregarOSoltar`, y manda `crafteo:herreria:completado`); `crafteo:herreria:cancelar` → `crafteo:herreria:cancelado`. Un segundo `crafteo:iniciar` mientras hay una forja activa se rechaza (no se puede pisar la sesión); `onLeave` limpia `forjasEnCurso` igual que `craftesEnCurso`.

**Sin panel de cliente todavía** — mismo estado que el resto de crafteo (§5: "crafteo no tiene panel de cliente hoy"), protocolo real disponible vía `window.__test.enviar(...)`. Verificado end-to-end contra el servidor real (`server/test/herreria.e2e.mjs`: fases, doble-inicio bloqueado, cancelado limpio, insumos gastados al iniciar y nunca devueltos, entrega real base/bonificada según estrellas) además de la suite unitaria del motor puro (`server/test/herreria.test.ts`, 20 tests: cada fase, los dos ejes de dificultad, calidad/estrellas, simulación completa perfecta e imperfecta).

## 7. Fuera de alcance de esta propuesta (pendiente, oficio a oficio)

- Las recetas/planos concretos de cada oficio — esto solo fija el CONTRATO, no rellena los ~38 oficios.
- ~~Cómo se consigue un plano nuevo~~ — **resuelto (2026-08-30), ver §7bis: ligado a construir la mesa correspondiente, no a un desbloqueo por jugador.**
- Umbrales exactos de XP por nivel, y cuánto XP da cada receta — placeholders a afinar como el resto de números de balance del proyecto (`xpOtorgada` por receta ya es asignable desde §7bis, falta rellenarlo).
- Si un personaje puede tener varios oficios a la vez o hay que especializarse — sigue abierto en el Backlog.

## 8. Ampliación masiva de crafteo + velocidad/durabilidad real por tier + reparto 1-10 (2026-09-08, pedido streamer: "que todos los objetos del mundo ahora puedan craftearse (menos las armas y armaduras de bosses)... herramientas de diferentes materiales (las mas basicas mas lentas, las de mejor material mas rapidas y mas durabilidad) para asi repartirlas en los niveles como tiers")

Cierra dos huecos a la vez: (1) el "mapa de oficios" (artefacto visual pedido en la misma sesión) mostraba las 290 recetas topando en nivel 4-5, niveles 6-10 vacíos en los 10 oficios; (2) el tier de herramienta (1-4, `familiaMaterial: "herramienta_<oficio>"`) YA existía desde 2026-08-30 pero era **solo una puerta de acceso** — el propio código lo decía: "aunque hoy da igual, ninguna se destruye, solo se desgasta" — recolectar tardaba lo mismo con tier 1 que con tier 4, y la durabilidad era una constante plana (60) en vez de escalar.

### 8.1 Alcance real de "todos los objetos" — auditado antes de tocar nada

Antes de generar nada se hizo un barrido real de `items/catalogo/items.json` (614 entradas) cruzado contra `recetas.json` (290 recetas) por `tipo`, para separar el hueco REAL del ruido:

| tipo | sin receta | qué era de verdad |
|---|---|---|
| `armadura` | 35/35 | las 5 piezas × 7 temas de `catalogoLootLegendario.json` — **excluido a propósito, sigue siendo el único loot de jefe** |
| `arma` | 14/45 | 7 legendarias + 7 de `catalogoLootTematico.json` (enemigo temático) — **excluido, mismo criterio** |
| `equipable` | 7/123 | las 7 armaduras temáticas de `catalogoLootTematico.json` — **excluido** |
| `semilla` | 32/32 | se consiguen cosechando o comprando a NPC (confirmado por el streamer) — **fuera del reparto a propósito** |
| `libro` | 18/19 | casi todos son libros de TUTORIAL/lore — el único crafteable de verdad es el que el propio jugador puede escribir (concepto distinto, no una receta más) — **fuera de esta pasada** |
| `objeto` | 64/77 | 40 son `cadaver_*` (contenedor auto-generado al cazar, no se craftea) + moneda/reliquia/bolsas de semilla (atadas a la exclusión de arriba) — el resto (23: vajilla, iluminación, papelería, arcón...) **SÍ tiene receta nueva** |
| `recurso` | 90/162 | la mayoría es materia prima BRUTA (mineral, madera en tronco, plantas, carne, pieles) — se sigue recolectando/talando/cazando, NUNCA crafteando (decisión explícita del streamer: "solo procesados") — los 13 PROCESADOS reales (lingotes ×7, acero, bronce, piedra_tallada, cristal_pulido, cuero_curtido, tela_hilada, gema_tallada) + 4 muebles-que-también-son-ítem (silla/mesa_comedor/cama_individual/arcón) **SÍ ganan receta** |
| `consumible` | 28/35 | 5 son `pocion_alquimica_*` (minijuego de alquimia, `alquimia.ts::itemIdPocion` — ya tienen SU propio camino de crafteo, no uno de recetas.json) — el resto (23: asados, cocinados, pan, mantequilla, queso, raciones) **SÍ gana receta**, cocinero |

Total real: **89 recetas nuevas** sobre ítems que YA existían en el catálogo (cero arte nuevo, cero id nuevo salvo lo del §8.2/8.3) — el resto del "hueco" era ruido (loot exclusivo, subproductos automáticos, materia prima que se recolecta por diseño).

**Los 616 elementos de `interiores/catalogo/elementos.json` sin representación en `items.json`** (de 716 totales, solo 100 tienen un id gemelo en `items.json` — el resto es decorado puramente bakeado: clutter, arquitectura de sala, set-dressing de un taller concreto) se dejan **fuera a propósito** — convertirlos en ítems craftables exigiría darles entrada nueva en `items.json` (peso/categoría/pila) para CADA uno sin la garantía de que tenga sentido cargarlos y colocarlos como mueble de jugador (una bóveda de piedra o una viga de techo no son "un objeto que se lleva en la mochila"). Si el streamer quiere piezas concretas de esa lista crafteables, es una petición dirigida, no un barrido ciego.

### 8.2 Tier 5 — techo nuevo de herramienta por oficio (nivel 5, uno por familia)

Extiende la escalera `tier1→nivel1 ... tier4→nivel4` ya establecida con un quinto peldaño real: `martillo_yunque_celestial` (herrero), `hacha_corazon_roble_milenario` (carpintero), `pico_veta_eterna` (picapedrero), `cuchilla_maestra_talabartero` (curtidor), `buril_luz_prismatica` (joyero), `guadana_cosecha_dorada` (molinero), `podadera_sagrada_arboleda` (curandero), `compas_precision_estelar` (ingeniero), `cuchillo_rastreador_supremo` (cazador), `cuchillo_gran_maestro_cocina` (cocinero) — 10 ítems nuevos, cero arte nuevo (reusan un `prendaId` ya existente, el mismo mecanismo procedural que ya usan armas/herramientas desde 2026-09-07: `equipoVisual.ts` genera el vóxel EN VIVO o carga el `.glb` ya aprobado de `taller-vox/generar_herramientas.js`).

### 8.3 Pieza insignia por oficio (nivel 7-10) — el techo real por encima de lo craftable de siempre

11 ítems nuevos (uno o dos por oficio, herrero se lleva dos por ser el más profundo del catálogo), extendiendo el MEJOR producto ya existente de cada oficio un escalón más — mismo criterio que ya usa el juego para `_bonificado(a)` (mejor material, mejores stats), **nunca compitiendo con el loot legendario de jefe**: `martillo_guerra_mithril`(8)/`peto_placas_mithril`(10) herrero, `gran_salon_ebanisteria_carro_real`(7) carpintero, `coraza_marmol_reforzado`(8) picapedrero, `abrigo_piel_exotica_maestro`(7) curtidor, `diadema_gemas_reales`(9) joyero, `banquete_real_gran_asado`(7) cocinero, `elixir_restauracion_completa`(8) curandero, `ballesta_repeticion_maestra`(9) cazador, `automata_asistente_mecanico`(10) ingeniero, `silo_grano_gremial`(7) molinero.

### 8.4 Velocidad y durabilidad REALES por tier (no solo puerta de acceso)

`items.json`: las 66 herramientas con `familiaMaterial: "herramienta_<oficio>"` (de 71 — las 5 restantes, `cana_pesca`/`cubo_ordeno`/`tijeras_esquilar`/`antorcha_portatil`/`instrumental_cirugia`, nunca tuvieron tier, quedan fuera igual que antes) ganan `cooldownMs` (2200/1700/1300/950/700 ms por tier 1-5) y `durabilidadMax` escalado de verdad (50/90/140/200/280, antes una constante plana de 60 o ausente).

**`server/src/mundo/herramientasRecoleccion.ts`**: nueva función pura `msFaltantesParaRecolectar(cooldownMs, ultimoMs, ahoraMs)`. **`RoomExteriorBase.manejarCoger`**: nuevo `ultimoCogerPorSesion` (Map por sesión, limpiado en `onLeave`) — antes de confirmar una recolección GATEADA por herramienta (rama picapedrero equipada Y rama `mejorHerramientaPara`), comprueba el cooldown de la herramienta CONCRETA usada y rechaza con `coger:error {motivo:"demasiado_pronto", faltanMs}` si no ha pasado suficiente; recoger algo YA soltado por otro jugador sigue instantáneo, como siempre (nunca pasa por `requisitoDeCategoria`). Verificado con `server/test/herramientasRecoleccion.e2e.mjs` (gating real sin regresión) + `server/test/herramientasRecoleccion.test.ts` (17 tests: la función pura con timestamps fabricados + un test de catálogo que confirma que CADA familia mejora cooldown Y durabilidad tier a tier, no solo que existan los campos).

### 8.5 Monturas — 2 tiers nuevos sobre `silla_montar`, con bonus real (no solo cosmético)

`docs/GDD_Monturas.md` §3 ya tenía la silla básica (`esMontura`, consumida por `mascota:ponerMontura`, gatea `mascota:montar`) — pero era un flag booleano, sin distinguir QUÉ silla. Nuevo `bonusVelocidadMontura` en el catálogo (`silla_montar`=0, `silla_montar_reforzada`=0.1, `silla_montar_maestra`=0.2, nivel 3 y 6 respectivamente) que ahora SÍ se persiste por mascota (`Mascota.monturaBonusVelocidad`, columna nueva `montura_bonus_velocidad` migrada en los dos backends, mismo patrón que `arnesPesoMaximo`) y se aplica de verdad a `velocidadMontura` al montar (`manejarMascotaMontar`). Verificado: `server/test/mascotasBd.test.ts` (+1 test, roundtrip del bonus en BD), `server/test/monturas.e2e.mjs` sin regresión (la montura sembrada sin bonus explícito sigue moviéndose exactamente igual que antes — 0 es no-op).

### Verificado (§8, conjunto)

Servidor 1271/1271 (+7 tests nuevos: 5 de velocidad/durabilidad por tier, 2 de bonus de montura en BD), `tsc --noEmit` limpio en cliente y servidor (cliente sin cambios — `equipoVisual.ts` ya renderizaba genéricamente cualquier `prendaId`/material nuevo, cero pieza de arte nueva que aprobar), `items.json` 614→637 entradas (23 nuevas: 10 tier5 + 11 insignia + 2 tiers de montura), `recetas.json` 290→379 (89 nuevas), `server/test/herramientasRecoleccion.e2e.mjs`/`monturas.e2e.mjs`/`npcs_trabajadores_montura.e2e.mjs` sin regresión (el último tiene un fallo de timing PRE-EXISTENTE, confirmado reproduciéndolo igual con `git stash` sobre el commit limpio anterior a esta pasada — no es de esta pasada). **Reparto final por nivel** (recetas por oficio, 1→10): con esta pasada los 10 oficios tienen contenido real en 1-5 y AL MENOS una pieza en 6-10 (herrero e ingeniero llegan a nivel 10, el resto a 7-9) — antes de esta pasada, 9 de 10 oficios topaban en nivel 4. **Sin verificación visual en cliente real** (equipar una pieza insignia nueva y verla en pantalla, craftear un procesado y confirmarlo por Playwright) — documentado como gap, no dado por bueno sin más, mismo criterio honesto que el resto de esta sesión.

## 9. Cierre del ciclo de minerales — plomo/turba sin uso, gemas colapsadas, mesa de joyero inalcanzable, slot cuello nuevo (2026-09-08, misma noche, pedido streamer: "que mas falta, materiales minerales todo el ciclo? revisa a ver" → "habria que cerrar los 4")

Auditoría dedicada del ciclo completo recolección→fundición→consumo (verificada a mano con grep/lectura directa, no solo con el informe de un agente — una de sus conclusiones se corrigió antes de tocar nada, ver más abajo). 4 huecos reales confirmados, los 4 cerrados en la misma pasada, más una ampliación de catálogo de vidrio pedida en el mismo mensaje.

### 9.1 Lo que SÍ funcionaba de punta a punta (confirmado, no se tocó)

Recolección de mineral bruto (15 categorías, todas con pico/tier real en `herramientasRecoleccion.ts`) → fundición (`herrero`: lingotes ×7, acero, bronce) → consumo real en decenas de recetas. Picapedrero completo (NPC/edificio/censo/ropa). `azufre` y `sal` **NO estaban realmente sin uso** pese a dar 0 resultados en `recetas.json` — ambos ya se consumían de verdad en el minijuego de alquimia (`items.json`: `azufre.alquimiaCorruptivo`/`sal.alquimiaIngrediente`, leídos por `server/src/construccion/alquimia.ts`) — la primera pasada de auditoría solo miró `recetas.json` y los dio por muertos; verificado leyendo `alquimia.ts` antes de "arreglar" algo que no estaba roto.

### 9.2 Los 4 huecos reales

1. **`lingote_plomo` sin ningún consumidor** — se fundía pero 0 recetas lo pedían después (confirmado: aparecía 1 sola vez en todo `recetas.json`, su propia receta de fundición).
2. **`turba` sin ningún uso** — 0 apariciones en `recetas.json`, ni cruda ni procesada. (el verdadero "mineral muerto" del cierre — azufre/sal, punto anterior, no lo estaban de verdad).
3. **6 tipos de gema (geoda/amatista/esmeralda/rubí/zafiro/diamante) colapsaban en el MISMO itemId `"gema"`** — `mapaColision.ts:201` usa literalmente `categoriaRecurso` como itemId del drop, y las 6 rocas compartían `"categoriaRecurso":"gema"` en `baker/catalogo/rocas.json` pese a tener rareza muy distinta (diamante `densidadBase:0.0002` vs. amatista `0.001`) — un diamante valía exactamente igual que una amatista.
4. **`gema_tallada_procesado`/`cristal_pulido_procesado` (oficio `joyero`) exigían mesas (`mesa_tallado_cristal`/`horno_vidrio`) etiquetadas EXCLUSIVAMENTE `temasProfesion:["vidriero"]`** — nunca podían generarse en un edificio `joyeria` (`temaTaller:"joyeria"`, el edificio real del oficio). Corregido el diagnóstico inicial durante la investigación: no es una dependencia cruzada intencional plausible ("el joyero compra cristal al vidriero") porque **`vidriero` ni siquiera es un oficio jugable** — se fusionó dentro de `joyero` exactamente igual que sastre/peletero se fusionaron en curtidor (`server/src/personaje/oficios.ts` no lo lista) — era simplemente una etiqueta que faltaba.

### 9.3 Arreglos

- **Plomo**: `vidriera_mosaico_coloreado_craft` (joyero) gana `lingote_plomo ×2` como insumo — came de vidriera real (plomo para sujetar los cristales de una vidriera de verdad), mismo receta ya existente, sin ítem nuevo.
- **Turba**: `fertilizante_procesado` (molinero) gana `turba ×1` — turba como enmienda de suelo/compost es un uso real de jardinería, mismo criterio que el resto de "procesados reales" del §8.
- **Sal**: además de su uso ya real en alquimia, `cuero_curtido_procesado` (curtidor) gana `sal ×1` — curtido con sal es un proceso históricamente real, y le da a `sal` un uso central y descubrible, no solo el de un minijuego secundario de RNG.
- **Mesa de joyero inalcanzable**: `mesa_tallado_cristal` y `horno_vidrio` (`interiores/catalogo/elementos.json`) ganan `"joyeria"` en su array `temasProfesion` (quedan `["vidriero","joyeria"]`) — mismo patrón exacto que el fix del caldero de alquimia de `torre_mago`/`academia_magia` (auditoría de oficios, 2026-09-03): un edificio `joyeria` ya puede generar estas mesas, sin quitarle nada a `vidrieria`.
- **Gemas preciosas separadas**: `amatista`/`esmeralda`/`rubi`/`zafiro`/`diamante` ganan `categoriaRecurso` PROPIA en `baker/catalogo/rocas.json` (antes las 5 compartían `"gema"` con `geoda`, que se queda como la única "gema genérica" — es la más común de las 6) + entrada propia en `CATEGORIA_HERRAMIENTA_RECOLECCION` (mismo tier 4 que la genérica) + itemId bruto propio en `items.json` + su propia gema tallada (`amatista_tallada`/`esmeralda_tallada`/`rubi_tallado`/`zafiro_tallado`/`diamante_tallado` — género gramatical correcto según el mineral, `rubí`/`zafiro`/`diamante` son masculinos) vía nueva receta `<tallada>_procesado` (joyero, mesa `mesa_tallado_cristal`). Verificado con un bake real de prueba (`baker/config/ejemplo-rapido.json`, el único permitido): los sectores exportados ya traen `"i":"amatista"`/`"esmeralda"`/`"rubi"`/`"zafiro"`/`"diamante"` como objetos de roca distintos, confirmando que el drop real en juego ya diferencia cada gema (antes de este cambio los 5 habrían exportado como roca distinta pero dado el MISMO itemId al recogerlos).

### 9.4 Gemas con bonificación real (pedido streamer: "con gems hacer cosas que te den bonificaciones") + slot `cuello` nuevo

`anillo_rubi` YA EXISTÍA (defensaMagica 4 + ataqueMagico 4) pero se crafteaba con 2× `gema_tallada` GENÉRICA pese a llamarse "de rubí" — repuntado a `rubi_tallado ×1` (bug real de coherencia temática, cerrado de paso). 4 anillos nuevos (`anillo_amatista`/`anillo_esmeralda`/`anillo_zafiro`/`anillo_diamante`), cada uno con una bonificación TEMÁTICA distinta dentro de los 4 stats que `calcularStatsEquipo` (`server/src/inventario/inventario.ts`) de verdad suma (`ataqueFisico`/`ataqueMagico`/`defensaFisica`/`defensaMagica` — no hay stat "vida"/"velocidad" que un anillo pueda dar hoy, así que las bonificaciones se quedan dentro de las 4 ya wireadas end-to-end): amatista→`ataqueMagico` (foco mágico ofensivo), esmeralda→`defensaFisica` (tanque/vitalidad), zafiro→`defensaMagica` (ward puro), diamante→un poco de las 4 (pieza endgame, nivel 8, mismo rango que las piezas insignia del §8.3).

**Slot `cuello` — hueco de equipo nuevo** (docs/GDD_Equipo.md §1, pasa de 20 a 21 huecos): no existía ningún sitio para llevar un collar/gargantilla — mismo patrón EXACTO que la adición de `capa` (2026-09-03): `POSICION_POR_SLOT` en `ropa/src/generarEquipo.js` + su copia sincronizada `client/src/render3d/generarEquipoVoxel.ts` (pivote `torso`, caja pequeña justo bajo la cabeza) y `SLOTS` en `client/src/personaje/panelJugador.ts` — cero cambio en el servidor (`SlotsEquipo` sigue siendo un índice de string libre). 5 collares nuevos (`collar_amatista`/`collar_esmeralda`/`collar_rubi`/`collar_zafiro`/`collar_diamante`), versión más fuerte del anillo de la misma gema (más metal + 2 gemas talladas en vez de 1). **Nota de vocabulario, para no confundir a la próxima sesión**: el pedido decía "cuello y manos (anillos)" — el hueco de "manos" YA EXISTÍA desde antes (guantes de armadura, `docs/GDD_Equipo.md §1`) y el de anillo TAMBIÉN (`anilloIzquierdo`/`anilloDerecho`, `docs/GDD_Equipo.md` "Anillos — un caso especial de slot compartido") — el ÚNICO hueco genuinamente nuevo de este pedido era `cuello`; los anillos temáticos de gema reusan el slot ya existente.

### 9.5 Ampliación de vidrio interactivo/decorativo (mismo mensaje, pedido streamer: "al vidriero habra que meterle vasos cuberteria de bebidas jarras frascos para pociones... ampliar mucho mas catalogo de cosas interactivas o decorativas")

`vidriero` no es oficio jugable (ver 9.2.4) — todo esto es oficio `joyero`, mesa `horno_vidrio` (soplado de vidrio real), insumo `cristal_pulido`. 7 objetos nuevos, simples (`tipo:"objeto"`, apilables, sin necesitar el sistema de mobiliario bakeado de `interiores/`): `vaso_cristal`, `copa_cristal`, `plato_cristal` (vajilla básica, nivel 1), `jarra_cristal`, `florero_cristal`, `frasco_pocion_grande` (nivel 2 — variante de tamaño del `frasco_pocion` ya existente), `decantador_cristal` (nivel 3, con remate de plata). Primer lote de una lista que el streamer pidió explícitamente seguir ampliando — no se agotó el catálogo de vidrio en esta pasada, solo se abrió.

### Verificado (§9, conjunto)

Servidor 1283/1283 (+8 tests nuevos en `server/test/cierreMinerales.test.ts`: gate de herramienta por gema, cadena de talla completa por gema, `anillo_rubi_craft` repuntado, mesas con `joyeria`, plomo/turba/sal con consumidor real, equipar un collar en el slot `cuello` nuevo suma stats de verdad, 4 anillos temáticos cada uno suma su stat distinta, catálogo de vidrio completo), `tsc --noEmit` limpio en cliente y servidor, `interiores/test/catalogo.test.js` 42/42 sin regresión (elementos.json tocado), `items.json` 637→663 (26 nuevas: 5 gemas + 5 talladas + 4 anillos + 5 collares + 7 objetos de vidrio), `recetas.json` +21. Bake de prueba real (`ejemplo-rapido.json`) confirma las 5 gemas dando drops distintos de verdad. **Sin verificación visual en cliente real** (equipar un collar/anillo nuevo y verlo en el rig, craftear un vaso de cristal y confirmarlo por Playwright) — documentado como gap, no dado por bueno sin más, mismo criterio honesto que el resto de esta sesión.

## 10. Mesas de oficio siempre presentes + coste de materiales para construirlas + panel de crafteo REAL en el cliente + XP/nivel consultable + toast de subida de nivel (2026-09-10/11)

Esta sección junta trabajo de DOS pasadas paralelas sobre el mismo hueco ("crafteo no tiene panel de cliente hoy") que llegaron a `main` casi a la vez — una centrada en "que las mesas de oficio siempre estén presentes y cuesten materiales construirlas" (pedido streamer: *"cada mesa que se incluya como mueble en los edificios de ese oficio, obligatorio siempre 1 o 2 mesas de cada oficio en cada edificio de oficio... y luego que se puedan construir, las de nivel 1 de crafteo fácil con material fácil, las siguientes complicándose con materiales de otras mesas o más elaborados"*), la otra en un playtest real que confirmó que ni siquiera se podía craftear desde el navegador (pregunta streamer: *"farmeaste, hiciste crafteos en mesa, subiste nivel de oficios y viste nuevos crafteos, probaste a crear muebles nuevos o los minijuegos?"*). Las dos escribieron su PROPIO `client/src/construccion/panelCrafteo.ts` de forma independiente — al fusionar ambas ramas se adoptó la versión del playtest (más completa: XP/nivel consultable, toast de subida de nivel, precio/XP por receta ya integrados con la ampliación de catálogo de §11) como la única que sobrevive, y se le sumaron las dos piezas de la otra rama que eran ortogonales (el bug de mesas ausentes, el coste de materiales para construir). El `client/test/panelCrafteo.e2e.cjs` de la rama descartada se eliminó por redundante: `client/test/playtestOficios.e2e.mjs` ya cubre el mismo flujo de punta a punta jugando por la UI real.

### 10.1 Bug real: mesas de oficio ausentes en 2 edificios

Confirmado con generación programática real (`generarEdificio`, no solo grep de catálogo) que:
- **`herreria`/`fragua`**: ninguna de sus mesas (`yunque_tocon`, `forja_campo`, etc.) llevaba ninguna marca de obligatoriedad — la colocación de mobiliario (fases Dominante→Secundario→Decoración, `interiores/src/colocarElementos.js`) podía, con mala suerte de semilla, generar el edificio sin ninguna mesa de herrero real.
- **`cocina_comunal`**: su `salasPorPlanta.planta_baja` generaba una sala de tipo `"cocina"`, pero las mesas reales de `cocinero` en `elementos.json` declaran `tiposSalaValidos:["taller"]` — **0/40 semillas** de prueba colocaban ninguna mesa de cocinero ahí, el edificio del propio oficio no podía nunca albergar su propio crafteo.

**Arreglos**: (1) 9 entradas de mesa ganan `"isMandatory": true` en `interiores/catalogo/elementos.json` (`yunque_tocon`, `forja_campo`, `fogon_campamento`, `estacion_despiece_caza`, `estacion_boticario`, `molino_mano`, `mesa_tajado_limpieza`, `banco_clasificacion_cincelado` — `banco_carpintero` ya lo tenía correcto desde antes), consumido por `colocarElementos.js` en la fase Dominante para garantizar que ningún bake de ese edificio salga sin su mesa. (2) `cocina_comunal.salasPorPlanta.planta_baja` cambia de `"cocina"` a `"taller"` (confirmado que `poblacion/catalogo/accionesPorSala.json` incluye ambos tipos en su lista de "trabajar", así que el enrutado de NPC trabajador no se rompe). Verificado: 40/40 semillas con mesa real tras el fix (antes 0/40 en cocina_comunal), `interiores/test/catalogo.test.js` 42/42 sin regresión.

### 10.2 Coste de materiales para construir una mesa de oficio (tecla B)

`EntradaConstruible`/`EntradaElemento`/`EntradaExterior` (`server/src/construccion/catalogo.ts`) ganan un campo opcional `receta: {itemId, cantidad}[]` — el mismo campo que `docs/GDD_Construccion.md` ya reservaba sin implementar. Aplicado a los 61 elementos con `nivelOficioMinimo` en `elementos.json` (generado con una tabla `MATERIAL_POR_TEMA` + una fórmula `recetaParaNivel(mat, nivel)` que escala: nivel 1 = material bruto barato y poca cantidad, niveles altos = material PROCESADO del propio oficio — nunca de otro oficio, decisión confirmada con el streamer — en cantidad creciente) y a las 6 vasijas de cocina en `exteriores.json` (madera+piedra básicos, son nivel 1). El handler `"construir"` (`RoomExteriorBase.ts`) valida que el inventario tenga TODOS los insumos antes de construir, los descuenta de verdad, y los devuelve íntegros si la construcción se revierte por cualquier otro motivo de validación posterior. El cliente (`constructor.ts`) muestra un tooltip con "Requiere nivel N de `<oficio>`. Materiales: X, Y" en cualquier mesa con coste. Verificado con `server/test/costeConstruirMesaOficio.e2e.mjs` (colyseus.js directo, 5/5 — rechazo sin materiales, éxito+descuento real con materiales); `server/test/megaEstresTodasLasMecanicas.e2e.mjs` actualizado para seguir sembrando materiales suficientes al dueño de parcela (no re-ejecutado completo, coste alto de esa suite).

### 10.3 Panel de crafteo REAL en el cliente + XP/nivel consultable + toast de subida de nivel (2026-09-11, pregunta del streamer: "farmeaste, hiciste crafteos en mesa, subiste nivel de oficios y viste nuevos crafteos, probaste a crear muebles nuevos o los minijuegos?")

**Hallazgo honesto antes de tocar nada**: la respuesta era NO, y además NO SE PODÍA — todo el sistema de crafteo (§5-§9, 400 recetas) era "server-only por ahora" (comentario literal de `game.ts`): `crafteo:iniciar` solo lo mandaban sondas de test (`window.__ajedrez` para la mesa de ajedrez, los e2e de herrería/cocina/alquimia). Un jugador real en el navegador NO tenía ninguna forma de elegir una receta en una mesa. Los minijuegos (forja/cocina/alquimia) sí tenían panel, pero solo se abrían por un `crafteo:iniciar` que nadie podía disparar desde la UI.

**Cerrado**:
- `client/src/construccion/panelCrafteo.ts` (NUEVO, marco compartido `crearMarcoPanel`, tema madera/pergamino). Se abre desde el menú de interacción de CUALQUIER construcción cuyo id aparezca en `mesas` de alguna receta (`MESAS_CON_RECETAS`, calculado de `items/catalogo/recetas.json` importado al bundle — mismo criterio que `panelReclutador.ts`: el catálogo es público, no se pide al servidor). Por receta: resultado (nombre real de `items.json`), oficio + nivel mínimo frente a tu nivel real, insumos que llevas / que pide (verde/rojo, sumados del `InventarioSchema` replicado), tiempo, "⚒ minijuego" si lo tiene, plano/edificio requerido si lo pide. Botón "Craftear" deshabilitado si falta nivel/insumos — es solo ayuda: el servidor sigue validando cada crafteo con `validarCrafteo` como siempre.
- Protocolo, sin cambios: `crafteo:iniciar` → `crafteo:iniciado {terminaEn}` (barra de progreso local) → al cumplirse, el panel pide `crafteo:recolectar` solo (margen de 250ms para no adelantarse al reloj del servidor) → `crafteo:completado` → toast "Has fabricado N× X". Una receta con `minijuego` abre el panel del minijuego como siempre (el servidor manda `crafteo:herreria:iniciado` en vez de `crafteo:iniciado`).
- **`oficio:consultar {oficios?}` → `oficio:estado {oficio1, oficio2, xp:{}, nivel:{}}`** (NUEVO, `RoomExteriorBase.ts`): la XP de oficio vive solo en BD (`jugador_oficios`), nunca se replicó — el panel la pide al abrirse para marcar qué recetas están a tu alcance. Bajo demanda a propósito, nunca Schema: solo cambia al craftear, y `crafteo:completado` ya trae la XP/nivel nuevos.
- **Subida de nivel visible**: el panel compara el `nivel` de `crafteo:completado` con el último conocido y lanza "¡Herrero nivel 2! Nuevas recetas: Olla de Metal, Instrumental de Cirugía, …" (recetas de ESE oficio con `nivelMinimo === nivel nuevo`, en cualquier mesa) — antes subir de nivel no se notaba en ningún sitio.
- Detalle de implementación que importa: el panel NO reconstruye su DOM cada segundo — reconstruye solo si cambia una firma (inventario, nivel, crafteo en curso) y actualiza la barra de progreso en su sitio; la primera versión rehacía todo cada segundo y un clic real podía caer sobre un botón recién destruido (el propio playtest lo cazó: timeout de 30s clicando "Craftear").

**Bug real de cliente encontrado por el playtest, ajeno al crafteo pero que lo bloqueaba (y bloqueaba TODO clic sobre muebles con arte real)**: ver `docs/GDD_UI_Paneles.md` §2ter — el raycast del clic sobre construcciones era NO recursivo y un `.glb` real es un Group con la malla como hijo, así que "Craftear en…/Sentarse en…/Abrir…" por clic solo funcionaba sobre la caja placeholder.

**Verificación** (`client/test/playtestOficios.e2e.mjs`, NUEVO — servidor+Vite+Playwright reales sobre `assets/mapas/testflat`, jugando por la UI): en verde de punta a punta (21/21 comprobaciones, 5ª ejecución tras 4 rondas de arreglos reales, ver abajo): elegir herrero+carpintero con el Maestro real de la Test Zone; el jarl coloca un `yunque_tocon` y un `banco_carpintero` con `construir` (el banco exige carpintero nivel 4 — gate real del catálogo, confirmado por el propio rechazo "necesitas nivel 4 de carpintero para construir esto"); CLIC REAL sobre el yunque → "Craftear en Yunque de Tocón" → panel con las 14 recetas del yunque y nivel real (`oficio:estado`: herrero 1), clavos desbloqueada con insumos OK y olla (nivel 2) bloqueada; 5 crafteos de clavos por el botón real en 97.6 s (8 s de receta + recolectar automático + bono) → XP 100 → toast "¡Herrero nivel 2! Nuevas recetas: Olla de Metal, Instrumental de Cirugía, Expositor de Espadas Cruzadas, Espada Corta, Hacha de Combate, Maza de Guerra…" en pantalla, olla ya desbloqueada en el panel, 50 clavos en el inventario; `daga_craft` desde el mismo panel abre el minijuego de forja y se juega con los botones reales (Avivar ×N hasta FORJAR, 12 golpes confirmados uno a uno por `progreso.sesion.golpes`, Templar) → daga real (1★, no bonificada) en el inventario y XP 120; capturas `client/test/capturas/playtest_oficios_{panel_crafteo,nivel2,forja}.png`. Además los e2e de protocolo de crafteo (`herreria`/`cocina`/`alquimia`/`npcs_trabajadores_crafteo`) se movieron a `testflat` (parcela real `tf_0001`, teleport de jarl junto a la mesa sembrada) — llevaban rotos desde el rehorneo de Vetrheim (2026-09-08): sembraban su mesa con `propiedad:"p_0001"` en el mapa principal, cuyo `parcelas.json` quedó vacío, y el servidor descartaba la construcción ("mesa inexistente"/"necesitas estar junto a una vasija"). Tras moverlos: `herreria.e2e.mjs`, `cocina.e2e.mjs`, `alquimia.e2e.mjs` y `npcs_trabajadores_crafteo.e2e.mjs` los 4 en verde ("TODO OK"); el último destapó además el bug real del interest-management de `docs/GDD_Rendimiento.md` §5.4.

**Pendiente real**: el panel es un placeholder funcional con el tema compartido (sin iconos de ítem, sin filtros por oficio); cocina sigue con su panel de proximidad propio (no pasa por este panel — sus "recetas" son el propio minijuego); sin verificar en el hosting real del streamer.

**Actualización tras fusionar con §10.1/§10.2 (2026-09-11, ver CLAUDE.md "49º pedido real")**: re-ejecutar `playtestOficios.e2e.mjs` después de fusionar sacó 2 bugs reales, ninguno visible con `tsc`/tests unitarios. (1) Mi propio coste de materiales rompía la construcción del yunque/banco en este test (sin `piedra_comun`/`madera_blanda`/`madera_dura` sembrados) — cerrado sembrando las cantidades EXACTAS del catálogo (7/11/2), nunca "de sobra": `admin:debug:darItem` no comprueba el peso máximo transportable (20kg a Fuerza 1), así que cualquier remanente de esos materiales (2-2.5kg/unidad) se queda pesando para siempre y `entregarOSoltar` (el que reparte lo crafteado, que SÍ comprueba peso) empieza a tirar todo al suelo en vez de a la mochila. (2) El bucle craftaba clavos 5 veces asumiendo el `XP_POR_CRAFTEO` plano de 20 (100XP≥90) de cuando se escribió este test — pero §11.3 (posterior, otra rama) le dio a `clavos_hierro` una `xpOtorgada` real de solo 17, así que 5 crafteos (85XP) se quedaban cortos del umbral real de nivel 2 (90) — subido a 6 crafteos (102XP). **Verificación final, TODO OK, 0 fallos**: mismo flujo de arriba con 6 crafteos de clavos (60 en el inventario, no 50), resto idéntico. Servidor 1352/1352, cliente 78/78, `tsc --noEmit` limpio en ambos.

### 10.4 Panel de alquimia + mecánica de frasco reutilizable (2026-09-10/11, misma tanda de peticiones que §10.1/§10.2: "el tema de la alquimia necesitamos funcione también como el resto, marcaban que salía si metías X y tal, y las pociones se pueden usar o consumir y se pierde y se queda el frasco, y para craftear pociones necesitas el frasco más ingredientes")

Cierra el hueco de "sin panel en absoluto" (ni siquiera placeholder, a diferencia de forja/cocina) que tenía el minijuego de alquimia desde su creación. `client/src/construccion/panelAlquimia.ts` (nuevo, `crearMarcoPanel`, mismo motor de temperatura que la forja): preview de color EN VIVO mientras se marcan ingredientes (mismo `colorPocion` de `alquimia.ts` replicado en cliente solo para mostrar, nunca para decidir el resultado real — 3+ catalizadores únicos da "Radiante"), sesión con barra de temperatura, avivar/enfriar/colar. El botón "Colar" nunca se deshabilita en cliente — se apoya por completo en el rechazo real del servidor ("demasiado pronto") en vez de intentar adivinar el timing exacto en el navegador.

**Mecánica de frasco nueva en servidor**: `manejarAlquimiaIniciar` exige y consume un `frasco_pocion` real del inventario (antes solo pedía ingredientes, sin ningún "recipiente" que gastar); `manejarPocionBeber` devuelve el frasco vacío al beber (vía `entregarOSoltar`, nunca se pierde el material — solo la poción). Preparar una poción nueva exige frasco + ingredientes; beberla consume la poción y deja el frasco disponible para la siguiente tanda.

**Mismo patrón de bug de re-render/DOM-churn de Playwright ya documentado en `docs/GDD_UI_Paneles.md` §3/§6, cerrado de forma distinta a §10.3**: el intervalo de refresco del panel seguía re-renderizando durante la sesión (botón "Avivar" con clics fantasma detectados por Playwright, `element was detached from the DOM, retrying`) — en vez de ajustar el timing del intervalo, se ELIMINÓ el polling del cliente por completo (0 `setInterval`), siguiendo el patrón ya probado de `panelForja.ts`: el panel solo re-renderiza en respuesta a mensajes reales del servidor (`alquimia:iniciado`/`progreso`/`completado`/`cancelado`/`error`).

De paso, se encontraron y arreglaron 2 e2e YA ROTOS antes de esta pasada por el motivo de siempre (Vetrheim/`parcelas.json` vacío tras el rehorneo de "Isla 1") — `server/test/alquimia.e2e.mjs` migrado a testflat + el atajo `pt_<asentamiento>_`.

**Verificado**: `client/test/panelAlquimia.e2e.cjs` (NUEVO, 9/9 — preview de color con 3 catalizadores marcados dando "Radiante", preparar consume frasco+ingredientes de verdad, barra de temperatura real, colar tras la duración mínima real entrega el resultado correcto, y sin frasco un segundo intento se rechaza mostrando el aviso real), `server/test/alquimia.e2e.mjs` (9 pasos, reparado, incluida la verificación explícita del roundtrip de frasco: bebe → recupera frasco vacío → prepara de nuevo con ese mismo frasco). Servidor y cliente en verde tras la fusión con §10.1-§10.3 — ver el registro consolidado en `CLAUDE.md`. **Sin verificación visual en vivo con el streamer** — mismo criterio honesto del resto de esta sesión. Detalle completo (contrato exacto del panel, tipos exportados) en `docs/GDD_Pociones.md` §10.

## 11. Coherencia y ampliación del catálogo entero — valor por fórmula, niveles 6-10 reales, recursos muertos con uso (2026-09-11, pedido streamer: "revisar todos los objetos, recetas de oficios, crafteos... coherencia, órdenes mejores, el costo de cada uno con sentido... más materiales, más combinaciones, más crafteos, más materiales crafteables... adelante vía libre")

Auditoría completa de `items/catalogo/items.json` (665 → **779** ítems), `items/catalogo/recetas.json` (402 → **512** recetas), `interiores/catalogo/elementos.json` (+11 muebles) y `server/src/mercado/catalogoMercaderes.json`, cruzados contra los catálogos del baker (`vegetacion.json`/`rocas.json`/`animales.json`), las mesas con `nivelOficioMinimo` y el código que consume cada campo. Todo lo que sigue está protegido por un test nuevo, `server/test/coherenciaCatalogo.test.ts` (12 tests), para que ninguna ampliación futura vuelva a dejar un hueco en silencio.

### 11.1 Lo que estaba roto de verdad (no "mejorable": incoherente o inalcanzable)

- **Ningún ítem tenía precio.** `items.json` no llevaba campo de valor; solo 71 ítems tenían un `precioBase` fijado a mano en el pool de mercaderes (§9 de `docs/GDD_Economia.md`), y esos 71 se contradecían con las recetas reales: `lingote_hierro` a 5₣ cuando su receta consume 2 `hierro` (3₣) + 1 `carbon` (2₣) = 8₣ — fundir DESTRUÍA valor. Cerrado con `valorBase` en TODOS los ítems, calculado por fórmula (§11.3), y con el pool de mercaderes cayendo a ese valor (§12 de `docs/GDD_Economia.md`).
- **Insumos sin fuente**: `cera` (jabón y velas eran incrafteables desde que existen), `pienso` (los animales de granja tenían un ítem de comida que nadie podía producir), las 4 `bolsa_semillas_*` (solo se podían ABRIR, nunca llenar).
- **10 recursos crudos del bake que nadie consumía**: `coral`, `madera_abedul`, `madera_sauce`, `madera_carbonizada`, `cereal_silvestre`, `hierba_curativa`, `hierba_venenosa` (solo alquimia), `hongo_comestible`, `raiz_comestible`, `alga` — se recolectaban y solo servían para vender.
- **4 piezas insignia (§8.3) NUNCA equipables**: `peto_placas_mithril`/`coraza_marmol_reforzado`/`abrigo_piel_exotica_maestro` llevaban `slotEquipo:"torso"` y `diadema_gemas_reales` `"cabeza"` — huecos que no existen en `SlotsEquipo` (los reales son `pechera`/`casco`, ver `docs/GDD_Equipo.md` §1). Corregidas, con `prendaId` y durabilidad como el resto de su clase.
- **2 consumibles insignia NUNCA consumibles**: `elixir_restauracion_completa` (`efectoCuracion`, campo que nada leía) y `banquete_real_gran_asado` (`efectoBuff` sin `restauraMultiple`, y `personaje:consumir` exige `restaura`/`restauraMultiple`). Ahora el elixir restaura vida+estamina a tope y el banquete sacia Y aplica su buff: `personaje:consumir` acepta `efectoBuff` y lo empuja por el MISMO `buffsPocionPorSesion` que las pociones de alquimia (`stat:"todos"` = los 4 stats de combate).
- **`ballesta_repeticion_maestra`** usaba `requiereMunicion` (campo inexistente; el servidor lee `municionId`) y no tenía `prendaId` — ahora dispara `virote_ballesta` y se ve en la mano. `martillo_guerra_mithril` tampoco tenía `prendaId`.
- **Niveles 6-10 casi vacíos**: 8 de 10 oficios no tenían NADA entre nivel 6 y 9 (herrero n6:0 n7:0 n8:1 n9:0 n10:1), y las recetas de nivel 6 eran MÁS RÁPIDAS de media (26 s) que las de nivel 5 (61 s).
- **`mesa_ajedrez`** no tenía `nombre` (única entrada del catálogo sin él).

### 11.2 Lo que se amplió

**23 materiales intermedios nuevos** (la escalera de tiers de §2, ahora completa por familia): `cuerda` (tela t1, fibra), `mimbre` (madera t1, de `madera_sauce`), `chapa_abedul` (madera t1, de `madera_abedul`), `carbon_vegetal` (de `madera_carbonizada` o carbonera de madera blanda — funde hierro igual que el carbón mineral, vía receta alternativa), `brea` (madera carbonizada + grasa), `madera_tratada` (madera t2 = dura + brea), `cal_coral` (piedra t1, de `coral`), `argamasa`, `piedra_reforzada` (piedra t2), `marmol_pulido` (piedra t2), `cuero_endurecido` (cuero t2 = curtido + brea; y `cuero_curtido` gana un camino alternativo por encalado con cal de coral), `tela_reforzada` (tela t2), `tinte` (bayas + sal; la ropa noble lo lleva ahora), `cola_animal` (tendones + tripas), `acero_templado` (metal t3, nivel 6), `mithril` (metal t3, nivel 8 — las piezas "de mithril" que ya existían lo consumen de verdad, antes pedían platino a secas), `lente_cristal`, `vidrio_coloreado`, `papel` (fibra + alga — alternativa al pergamino de piel para libros y mapas), `tinta`, `aceite` (frutos secos), `barniz`, `esencia_floral`. Más `cera` (miel colada) y `pienso` (cereal silvestre + hierba) con receta real.

**Cocinero** (15 platos/bebidas, niveles 1-10): gachas de cereal, raíces asadas, sopa de algas, guiso de hongos, hidromiel, vino de bayas, pastel de fresas, estofado de caza, pescado en aceite, pan especiado, licor de hierbas, queso curado, festín marino, aguardiente, festín del Jarl (n10). **Curandero** (7, niveles 2-10): tónico curativo (hierba curativa), antídoto (hierba venenosa), elixir de vigor, tónico de agilidad (buff real de velocidad), gran elixir de vitalidad, panacea, elixir del Jarl (n10, buff a los 4 stats). **Herrero** (16 piezas + sus 16 bonificadas por minijuego, niveles 6-10): espada/hacha doble/alabarda/maza de acero templado, espada de mithril, set completo de acero templado (casco/manos/zapatos/piernas/pechera/escudo torre) y set de mithril (yelmo/guanteletes/grebas/botas/escudo). **Carpintero**: escudo de madera reforzado, bastón de roble tratado, y 3 muebles exclusivos (escritorio de abedul, armario barnizado, cama con dosel). **Picapedrero**: escudo de piedra pulida, estatua/chimenea/fuente de mármol. **Ingeniero**: catalejo, brújula naval, reloj de bolsillo, prensa de grabado. **Curtidor**: set de cuero endurecido (botas/guantes/pechera/capucha), capa de viaje, mochila maestra (6x6), tapiz bordado del Jarl. **Joyero**: amuleto de coral, gafas de cristal, lámpara de vidrio coloreado, anillo de platino, jarrón esmaltado, brazalete de platino con gemas, collar estrella de mithril (n10). **Molinero**: molinillo de especias, barril de roble, tonel de hidromiel gremial. **Cazador**: carcaj (bandolera 2x4), lanza de caza, botas de rastreador, capa de camuflaje, arco compuesto, jabalina de acero, trofeo de cornamenta real. Todo consumible nuevo usa `restauraMultiple`/`efectoBuff`, todo equipable nuevo reusa un `prendaId` ya existente (cero arte nuevo), todo mueble nuevo sigue el patrón `requiereItemColocar` de la decoración exclusiva.

**Progresión**: los 10 oficios tienen ahora recetas en TODOS los niveles 1-5 y al menos una en 6-7 y en 8-10 (el test lo exige). Suelos de tiempo por nivel (6→50 s, 7→60, 8→75, 9→90, 10→120), solo al alza: la media de tiempo y de XP por nivel es monótona creciente de 1 a 10 (test).

**Orden**: `recetas.json` está ahora ordenado oficio → nivel → mesa → id (herrero, carpintero, picapedrero, ingeniero, curtidor, joyero, molinero, cocinero, curandero, cazador) — la progresión de cada oficio se lee de arriba abajo. El panel de crafteo muestra por receta la XP que da y el valor de referencia del resultado.

### 11.3 `items/catalogo/valorBase.js` — valor y XP por fórmula, nunca a mano

Mismo patrón que `nombreBonito.js`: `node items/catalogo/valorBase.js` imprime el informe, `--aplicar` escribe `valorBase` en cada ítem y `xpOtorgada` en cada receta. **Regla permanente**: toda entrada nueva de `items.json`/`recetas.json` pasa por él en el mismo commit — `coherenciaCatalogo.test.ts` compara lo guardado con lo calculado y falla si no coincide.

1. **Crudo** (tabla `VALOR_CRUDO`): refleja el tier de herramienta que exige recolectarlo (tier 1 ≈ 1₣, 2 ≈ 2-3, 3 ≈ 4, 4 ≈ 7), la rareza (gemas preciosas por encima del oro: amatista 12, esmeralda 16, zafiro 18, rubí 20, diamante 30; platino 18, oro 12, plata 8) y lo que ya cobraban los mercaderes cuando había precio a mano (hierro 3, sal 2, piel fina 4...).
2. **Crafteado**: `(Σ valor(insumo)×cantidad + manoDeObra) / cantidadResultado`, con `manoDeObra = nivel×2 + tiempo/8`. Si hay varias recetas manda la más barata. Así nada crafteado vale menos que sus insumos (test: ninguna receta destruye más del 20% del valor — una alternativa puede ser algo peor trato, nunca ruinosa).
3. **Bonificada** (5★ del minijuego): ×1.4 sobre su base.
4. **Equipo sin receta** (loot temático de enemigo, legendario): `4 + ataque×2.5 + ataqueMágico×4 + defensas×10`, ×1.2 (loot normal) o ×4 (legendario) — un set legendario de jefe cotiza en la liga del mejor peto craftable (~500-1000₣), nunca como chatarra.
5. **Cadáver**: `(carne + piel + 1) × factor por tamaño` (2/4/7/12 — las cantidades de `lootCaza.ts`).
6. **Semilla de cultivo**: 60% de lo que rinde una cosecha; semilla de árbol: 2₣.
7. **Sin fuente calculable** (tabla `VALOR_ESPECIAL`): pociones del minijuego de alquimia 30₣, libros por categoría, reliquia 60, moneda suelta 1.

**XP por receta**: `12 + nivel×4 + tiempo/6` — nivel 1 (12 s) 18 XP (≈ el `XP_POR_CRAFTEO=20` histórico), nivel 10 (120 s) 72 XP. Con la curva de 10 niveles (4050 XP) el tope llega con ~110 crafteos mezclados en vez de ~200 iguales, pero cada crafteo alto tarda 4-10× más: el tiempo real se queda en el orden de "~2 semanas dedicado" de `docs/GDD_Profesiones.md`.

Rango resultante: `flecha`/`piedra_honda` 1₣ … `espada_mithril_bonificada` 1046₣; `pechera_acero_templado` 538₣, `legendario_pechera_*` 516₣, `festin_del_jarl` 146₣.

### 11.5 Mobiliario del carpintero (2026-09-11, misma noche)

88 recetas más sobre las 512 de §11 (84 de carpintero, 4 textiles del curtidor en el telar): camas por tier y plazas, sofás/butacas por material, contenedores, expositores con filtro, lámparas y decoración — todas con el patrón "ítem portador `tipo:"recurso"` + mueble `requiereItemColocar`" de la decoración exclusiva, con `valorBase`/`xpOtorgada` por la fórmula de 11.3 y los tiempos dentro de la banda de su nivel. Qué hace cada mueble al colocarlo (plazas, calidad de descanso, filtro de contenido, contenido visible, luz) vive en docs/GDD_Construccion.md §9 — aquí solo cuenta que son recetas normales del oficio. Catálogo tras esto: 867 ítems, 598 recetas.

### 11.4 Decisiones tomadas para no repetir el debate

- Los pools de `catalogoMercaderes.json` pasan a `null` = "usa `valorBase`" (`precioBaseArticulo`). Un número sigue mandando si algún día se quiere un precio especial a mano por oficio.
- El nivel de la mesa (`nivelOficioMinimo`, N1→1/N2→4/N3→6/N4→8) y el `nivelMinimo` de la receta son gates INDEPENDIENTES a propósito: una mesa de tier alto bakeada en un taller de ciudad la puede usar un nivel bajo (solo construirla en casa exige el nivel), la receta gatea por sí misma. No se "alineó" nada ahí.
- Consumibles con mecánica propia (`venda`/`unguento`/`tablilla`/`protesis_*`/`jarabe_catarro` — médico; `pocion_alquimica_*` — `pocion:beber`) no llevan `restaura*` a propósito; el test los excluye por lista explícita.
- `nombreBonito.js` gana adjetivos (`floral`/`marino`/`naval`/`compuesto`/`curativo`/`gremial`/`reales`/`maestra`/`baja`) y 13 excepciones de frase — de paso corrigió 7 nombres viejos ("Diadema de Gemas de Reales" → "Diadema de Gemas Reales", "Silo de Grano de Gremial", "Silla de Montar de Maestra"...). Los libros llevan TÍTULO (`librosContenido.json`), no nombre de catálogo — el test los excluye.

**Verificado**: servidor 1347/1347 (+12 del test de coherencia), cliente 78/78, `tsc --noEmit` limpio en ambos, `interiores/test/catalogo.test.js` 42/42, `taller-vox/test_items.js` 16/16 (los 4 conteos pinados llevaban rotos desde 2026-09-08 — 47/81/89/37 frente a 38/71/77/35 — y ahora fijan los reales: 61/81/95/59), `valorBase.js` 779 ítems valorados con 0 avisos. E2E reales de servidor afectados (`herramientasRecoleccion`, `herreria`, `cocina`, `alquimia`, `npcs_trabajadores_crafteo`, `oficios`, `combateHabilidadDesgastePocion`) ejecutados tras el cambio — ver el registro de esa fecha en `CLAUDE.md`. **Sin verificación visual en cliente real** de los ítems nuevos (equipar un set de mithril, ver el tónico de agilidad aplicar su buff en pantalla) — documentado como gap, mismo criterio honesto que el resto de ampliaciones de catálogo.
