# Generador procedural de personajes/NPCs — decisión y estado (v1, 4 arquetipos de prueba)

Documento de referencia — léelo antes de tocar `personajes/` o ampliar su catálogo. Es el hermano de `GDD_Ropa_Procedural.md`: mismo patrón que el mobiliario de interiores (catálogo como fuente de verdad, generación determinista por semilla, offline).

## Decisión (confirmada con el streamer)

Antes de completar el catálogo de ropa, existe el **generador de personajes**: como con los muebles, habrá una **lista de NPCs** (catálogo) y el generador los va creando de ahí. La lista definitiva se pactará más adelante; ahora el catálogo arranca con **4 arquetipos de prueba** para validar el algoritmo — cuando la lista completa esté, se amplía `npcs.json` y se regenera todo del tirón sin tocar el generador.

Cada entrada de NPC declara su **ropa** (ids de `ropa/catalogo/prendas.json`): cuando el catálogo de prendas esté completo, el cliente generará y colgará esa ropa **auto** al materializar el NPC — misma morfología en la ficha, así que acopla sola (ver GDD_Ropa_Procedural, sección morfología).

Variedad exigida desde el primer momento: **pelos (estilo y color), alturas, complexiones, barbas, y colores de piel y ojos** distintos por individuo.

## Estructura

- `personajes/catalogo/rasgos.json` — diccionario global de rasgos: **30 estilos de pelo POR SEXO** (`peloEstilosHombre`/`peloEstilosMujer`, ampliación 2026-09-01 — ver más abajo), **15 de barba** (`barbaEstilos`, sin split por sexo), 7 colores de pelo "naturales" + `probabilidadColorFantasia` (ruleta de color, ver más abajo), 6 tonos de piel, 5 colores de ojos, pesos de sexo. Todo como listas `[id, peso, hex]` al estilo de `salasPorPlanta` de interiores.
- `personajes/catalogo/npcs.json` — LA LISTA (hoy: aldeano, herrero, guardia, anciano_sabio). Cada NPC: `profesion` (cruza con `ropa/catalogo/profesiones.json`), `morfologia` con **rangos** `[min,max]` de altura/corpulencia (cada individuo cae en un punto por semilla), overrides de pesos de rasgos (solo lo que cambie: el herrero sube `completa`, el anciano sube canoso/blanco), y `ropa`.
- `personajes/src/generarPersonaje.js` — el generador. Devuelve por individuo:
  - **`ficha`** (el contrato con el cliente): sexo, morfología concreta, colores (piel/pelo/ojos con id+hex), estilos (pelo/barba), profesión y ropa. El rig del cliente ya consume todo lo de la ficha: morfología (`OpcionesRig.morfologia`), `colorPiel`, `colorPelo`, `colorOjos`.
  - **`voxelesCabeza`**: pelo + barba en vóxeles colgando del pivote `cabeza`, mismo contrato `{x,y,z,color,zona,pivote}` que las prendas de `ropa/` — el cliente los fusiona en una geometría.
  - Los estilos de pelo/barba son **cajas normalizadas sobre la cabeza** (lado=1) voxelizadas a celdas de 1/6 — un estilo nuevo se da de alta en `rasgos.json` Y en `CAJAS_PELO`/`CAJAS_BARBA` del generador. El casquete superior nunca sobrevuela la cara (tope z=0.5): frente y ojos siempre visibles; el flequillo es una tira aparte.
  - Barba solo en hombres por defecto (`npc.permiteBarba` puede pisarlo por entrada del catálogo).

## Ampliación de pelo/barba (pedido streamer 2026-09-01: "en torno a 30 pelos por sexo, en torno a 15 barbas, color cualquiera con ruleta")

- **30 estilos de pelo, uno por sexo pero CRUZADOS**: `peloEstilosHombre`/`peloEstilosMujer` en `rasgos.json` son dos listas de peso sobre el MISMO diccionario de 30 ids reales (`CAJAS_PELO` en `generarPersonaje.js`) — nunca dos catálogos de geometría distintos. "Cada sexo tiene 30 pelos distintos" se cumple literalmente (las 30 entradas tienen peso>0 en las dos listas), y "mujeres también tendrán estilos cortos y hombres largos" también: solo cambia la PROBABILIDAD (ej. `rapado` pesa 15 en hombre y 3 en mujer; `melena_larga` pesa 12 en mujer y 4 en hombre), nunca una exclusión dura. `calvo` cuenta como uno de los 30, tal como se pidió. Los 6 estilos originales (calvo/rapado/corto/melena/coleta/monje) se dejaron intactos; los 24 nuevos (`corto_flequillo`, `corto_alborotado`, `corto_ondulado`, `media_melena`, `media_melena_ondulada`, `melena_larga`, `melena_rizada`, `melena_ondulada`, `coleta_alta`, `coleta_baja`, `doble_coleta`, `mono`, `mono_bajo`, `trenza_simple`, `trenza_doble`, `trenza_corona`, `rizado_afro`, `ondulado_suelto`, `flequillo_lateral`, `mohawk`, `cresta_lateral`, `rapado_lateral`, `recogido_medio`, `despeinado`) se construyen con dos helpers nuevos (`caja(...)`/`parLateral(...)`, cajas normalizadas + su espejo en X) en vez de repetir 24 listas de coordenadas sueltas — mismo criterio "parametrizar en vez de duplicar" que `ropa/`/`taller-vox/`.
- **15 barbas** (`barbaEstilos`, un único pool — no tiene sentido cruzarlo por sexo ya que `permiteBarba` decide quién puede llevarla, no este catálogo): a las 4 originales (ninguna/bigote/perilla/completa) se suman `bigote_fino`, `bigote_grueso`, `perilla_larga`, `candado`, `patillas`, `patillas_bigote`, `barba_corta`, `barba_larga`, `barba_trenzada` (con una "coleta" fina en la punta), `barba_partida` (dos mechones separados) y `barba_hacha` (forma apuntada).
- **"Ruleta" de color de pelo**: `probabilidadColorFantasia` (0.1 por defecto, overrideable por NPC como cualquier otro rasgo) — con esa probabilidad, `generarPersonaje()` NO tira de la paleta natural de 7 tonos de `coloresPelo`, sino de `colorPeloFantasia()` (hue aleatorio 0-360 sobre TODA la rueda cromática vía `hslAHex`, saturación 55-90% y luz 38-62% para que salga un tinte vivo y nunca gris/negro/blanco quemado) — pensado para dar variedad vistosa a NPCs generados aleatoriamente sin tocar la paleta "realista" de siempre.
- **Verificado**: 176 cajas nuevas/existentes con rangos válidos en los 3 ejes (ninguna invertida ni de tamaño cero); 1170 generaciones (39 NPCs reales × 30 semillas) sin una sola excepción, con los 30 estilos de pelo y las 15 barbas apareciendo todos al menos una vez en la muestra, y ~8.5% de tasa de color fantasía (target 10%, dentro de varianza estadística esperable). `poblacion/test/*.test.js`: 30/31 en verde (el único fallo es de un test de huertos preexistente, no relacionado — mensaje "revisar si cambió el bake").
- **Alcance real — a quién afecta esto HOY**: no existe todavía ningún "creador de personaje" en vivo (ni de jugador ni de NPC) — `personajes/generarPersonaje.js` es, como el resto del módulo, un generador OFFLINE que `poblacion/` hornea una vez en `poblacion.json` y el cliente solo pinta esos vóxeles ya resueltos (ver CLAUDE.md, entrada de `poblacion/`). Esta ampliación entra en vigor la próxima vez que se rehornee un asentamiento con `poblacion/src/exportarAsentamiento.js`. El "creador de personaje" del jugador en vivo sigue siendo un hito futuro (ver `docs/GDD_Ropa_Procedural.md`/`docs/GDD_Personaje.md`) — cuando exista, reutilizará este mismo catálogo/geometría (mismo rig, mismos pivotes), no uno nuevo.
  - **Todas las caras construidas, siempre** (regla del streamer, igual que en ropa — ver GDD_Ropa_Procedural punto 5): cada vóxel de pelo/barba y cada caja del cuerpo llevan sus 6 caras aunque queden tapadas; al fusionar en el cliente solo se pueden quitar caras interiores entre vóxeles adyacentes, nunca exteriores. Nada puede verse hueco desde ningún ángulo (buceo bajo agua translúcida incluido).
- `personajes/src/prueba_render_pj.js` — galería SVG de 8 individuos (cuerpo morfado con su piel + pelo/barba/ojos, orden de pintado del pintor) + `prueba_render_png.js` (Playwright global). Salida en `personajes/output/` (gitignored).

## Animales (v1.1) — el mismo creador para todo lo que tenga esqueleto

Decisión del streamer: este creador vóxel no es solo de PJs — **todo lo que tenga esqueleto (animales, insectos, aves...) sale de aquí**, tirando del listado de fauna que ya existe (`baker/catalogo/animales.json`, ~100 especies).

- `personajes/catalogo/animales_rig.json` — rig por especie: MISMOS ids que el catálogo del baker (el color base sale de su `colorDebug` — cero duplicados, el generador valida el cruce), `esqueleto` (plantilla), proporciones en casillas, `escala` por rango (variación individual por semilla) y rasgos (orejas/cuernos/cola/cresta/rayas/alas/antenas). HOY: 6 especies de prueba (conejo, lobo, vaca_salvaje, ciervo, gallina_salvaje, abeja) para validar las plantillas — cuando el streamer dé la orden, se etiqueta el listado entero y se genera todo del tirón.
- `personajes/src/generarAnimal.js` — 3 plantillas de esqueleto implementadas: **cuadrupedo** (cuerpo+4 patas+cabeza con hocico/orejas/cuernos/cola por rasgo), **ave** (cuerpo+2 patas+alas plegadas+pico/cresta/cola abanico) e **insecto** (cabeza+tórax+abdomen con rayas opcionales+6 patas+antenas+alas). Salida por individuo: `ficha` (especie, esqueleto, escala, color con tono individual, sexo, rasgos) + `piezas` como cajas colgando de PIVOTES con nombre (`cuerpo`/`cabeza`/`pataDelIzq`/.../`cola`/`alaIzq`) — mismo contrato que el rig humanoide: el cliente creará un grupo por pivote y animar será rotar pivotes (andar = patas en contrafase, volar = alas). Rasgos por sexo funcionan (cuernos ramificados del ciervo solo en machos).
- **v1.2 — 7 plantillas**: a las 3 primeras se sumaron **pez** (cuerpo fusiforme + caudal/dorsal/pectorales; `dorsal:"alta"` para tiburones; anclado por el vientre, el cliente lo coloca a su altura de nado), **serpiente** (6 segmentos en S con pivote propio cada uno para ondular, anillos y cascabel opcionales), **crustáceo** (caparazón ancho + pinzas + 6 patas + ojos en pedúnculo) y **anfibio** (cuerpo agachado, ancas plegadas, ojos saltones). Especies de prueba: pez_mediano, tiburón, culebra_de_agua, serpiente_de_cascabel, cangrejo, rana — 12 en total en `animales_rig.json` (6 + estas 6). Sin plantilla quedan casos raros del listado (medusa, pulpo, moluscos con concha, estrella de mar...) que se pactarán al etiquetar el listado completo.
- `personajes/src/prueba_render_animales.js` — galería (8 individuos, con zoom de encuadre por especie) sobre el render compartido `renderIso.js` (cajas con las 6 caras, regla del streamer).

## Razas por color (v1.3, pedido del streamer 2026-08-29)

Antes de esto cada especie tenía un único `colorDebug` con un jitter sutil de ±6% de brillo (mismo color, tono ligeramente distinto por individuo) — NO existían razas de verdad. Pedido: que especies como la vaca tengan variantes reales (negra, marrón, blanco y negro...) y que un rebaño pueda salir mezclado con esas variantes.

- `personajes/catalogo/animales_rig.json` — campo opcional `coloresPosibles: [[hex, peso], ...]` por especie, MISMO formato `[valor, peso]` que ya usa `rasgos.json` para pelo/piel/ojos de los NPC humanos, resuelto con el mismo helper `elegirPonderado` (`interiores/src/azar.js`). Cubierto hoy (las 5 especies domésticas/de granja del catálogo, ver fauna urbana más abajo): `vaca_salvaje`, `gallina_salvaje`, `perro`, `gato`, `gallo`. Especies sin el campo siguen igual que antes (colorDebug del baker + jitter) — no hace falta tocar el resto del listado.
- `personajes/src/generarAnimal.js` — si la especie declara `coloresPosibles`, se sortea la raza ENTERA por semilla (no solo un tono); el jitter individual (±6%) se aplica DESPUÉS, encima de la raza elegida — variedad dentro de la raza, no la sustituye.
- **Rebaños con razas mezcladas ya funciona**: `ciudades/src/fauna.js` (fauna doméstica urbana v1.3 — gallinas/vaca suelta/perros/gatos/algún gallo por poblado, ver `GDD_Agentes_Moviles.md`) genera una `semilla` distinta por individuo (`${ciudad.semilla}:fauna:${i}`), así que cada animal del mismo asentamiento tira su propia raza de forma independiente — un pueblo puede tener gallinas blancas, marrones y negras a la vez, o perros de razas distintas sueltos.
- **Pendiente honesto**: `oveja` NO tiene entrada en `animales_rig.json` todavía (solo existe `oveja_salvaje` en `baker/catalogo/animales.json`, sin rig/esqueleto) — cuando se le dé plantilla, añadir `coloresPosibles` ahí también es trivial. La fauna SALVAJE del mapa exterior (la que coloca `baker/decoracion.js` fuera de ciudades — lobos, ciervos, vacas_salvajes sueltas, jabalíes...) todavía se pinta como prop de color plano (`InstancedMesh`/`colorDebug`), no pasa por `generarAnimal`/rig — las razas por color de esta sección solo aplican, hoy, a la fauna doméstica urbana y a la galería de prueba de `personajes/`.

## Razas por tamaño y forma, no solo color (v1.4, pedido del streamer 2026-08-29)

Pedido explícito: "perros y gatos con razas diferentes (tamaños diferentes, colores y forma), estilo doberman/caniche/labrador" — un paso más allá de las razas por color de la sección anterior, que solo cambiaban el tono. Perro y gato son "lo más principal de compañero", así que llevan el tratamiento completo.

- `personajes/catalogo/animales_rig.json` — campo opcional `razas: [{id, peso, escala?, proporciones?, rasgos?, coloresPosibles?}, ...]` por especie. Cada raza SUSTITUYE los campos de la especie base SOLO en lo que declare (patrón "solo lo que cambie", igual que los overrides de NPC por profesión en `npcs.json`) — lo que no toque sale de la base. Se sortea una raza entera con `elegirPonderado`, igual que las razas por color.
  - **perro** (5 razas): `labrador` (grande, corpulento, dorado/negro/chocolate), `doberman` (alto, esbelto, orejas puntiagudas, negro/marrón oscuro), `caniche` (pequeño, patas cortas, cola en pomo, blanco/negro/albaricoque), `pastor` (mediano-grande, cuerpo alargado, negro y fuego/gris lobo/marrón), `callejero` (mestizo genérico, la base de siempre — sin sobreescribir nada).
  - **gato** (5 razas): `comun_europeo` (base, la de siempre), `siames` (esbelto, patas largas, cabeza fina, crema con puntas oscuras), `persa` (pequeño, rechoncho, patas cortas, cola en pomo, blanco/gris humo/crema), `maine_coon` (el más grande, cuerpo largo, atigrado/negro/gris), `angora` (esbelto, mayormente blanco).
- `personajes/src/generarAnimal.js` — `escalaRango`/`proporcionesBase`/`rasgosBase`/`coloresPosibles` se resuelven fusionando base+raza ANTES de aplicar el rango de escala y el jitter de color de siempre; `ficha.raza` expone el id de la raza elegida (`null` en especies sin `razas`). Especies sin el campo funcionan exactamente igual que antes — no hace falta tocar el resto del catálogo.
- **Bug real encontrado y corregido de paso**: la plantilla `cuadrupedo` no tenía entrada para `orejas: "caidas"` (la que usa `perro` desde siempre) en su tabla de alturas — la caja de la oreja salía con altura `NaN`. Añadido `caidas: 0.65` (entre `puntiagudas` y `largas`).
- **Verificado**: 200 individuos de muestra por especie reparten razas cerca de los pesos del catálogo; un pueblo real de prueba sacó perros labrador/doberman/caniche/callejero con escalas visiblemente distintas (0.45 a 0.89) en el mismo bake; cero piezas con `NaN` tras el fix.

## Animales de trabajo/monta: caballo, burro, buey (mismo pedido 2026-08-29)

`caballo_salvaje`/`burro_salvaje` (mapa exterior, población salvaje domesticable) ya existían, pero faltaba el **par doméstico urbano** — mismo hueco que ya se había resuelto para `oveja`/`carnero`. A diferencia de esos dos (y de `cerdo`/`ganso_domestico`), estos SÍ llevan rig completo desde ya:

- `caballo` — plantilla cuadrúpedo, patas largas, hocico largo, 5 capas de color (castaño/negro/blanco/palomino/pardo). Par doméstico de `caballo_salvaje`, que se mantiene igual.
- `burro` — mismo esqueleto, orejas largas (rasgo distintivo), capas más apagadas. Par doméstico de `burro_salvaje`, que se mantiene igual. **Sustituye a `mulo`** (ver nota abajo).
- `buey` — el más corpulento, con cuernos cortos (par de labranza de `vaca_salvaje`) — a diferencia de caballo/burro sí lleva `categoriaRecursoCarne`/`categoriaRecursoPiel` (un buey retirado se aprovechaba de verdad).

Añadidos a `baker/catalogo/animales.json` (patrón urbano `biomas: []`, como perro/gato/gallo) y enganchados ya a `ciudades/src/fauna.js` (pesos bajos: son animales caros de mantener, no todas las casas tienen uno). Verificado con un bake real de pueblo: aparecen sin `NaN` y con vóxeles completos.

**Por qué `mulo` se retiró**: se añadió primero (mismo pedido de "animales de trabajo"), pero justo después el streamer pidió un sistema de trío macho/hembra/cría de cara a una futura mecánica de reproducción — y el mulo es un híbrido de caballo+burro biológicamente estéril, no encaja ahí. Se quitó `mulo`/`mula` de `baker/catalogo/animales.json`, `personajes/catalogo/animales_rig.json` y `ciudades/src/fauna.js`, y `burro` (que sí se reproduce de verdad, y ya tenía `burro_salvaje` esperando su par) ocupa su sitio con el mismo rig adaptado.

## Convención de trío macho/hembra/cría, y su extensión a fauna salvaje (mismo pedido 2026-08-29)

Pedido explícito, con ejemplo: "si más adelante digo añadimos el cerdo, pues cerdo, cerda, cerdito se debe crear" — y después extendido: "también los animales salvajes deben tener esta regla... macho hembra y cachorro". Es una convención de **catálogo**, no de generación: cada trío es tres entradas nuevas en `baker/catalogo/animales.json` (nombre real cuando existe en español — toro/vaca, caballo/yegua, cerdo/cerda —, o flexión de género normal si no — perro/perra, gato/gata) más UNA sola cría común a ambos sexos (el sexo se decide al llegar a adulto, nunca "cría macho"/"cría hembra").

- **Doméstico** (+16, completando los pares que ya existían): `perra`/`cachorro`, `gata`/`gatito`, `pollito` (de gallina_salvaje/gallo), `yegua`/`potro`, `toro`/`vaca`/`ternero` (bovina doméstica completa junto a `buey`), `cerda`/`cerdito`, `oca`/`ansarino`, `cordero`, `burra`/`burrito`.
- **Salvaje** (+12, solo la fauna de caza/campo pedida explícitamente, NO todo el catálogo): `jabalina`/`jabato`, `cierva`/`cervatillo`, `corza`/`corcino`, `macho_cabrio`/`cabrito` (`cabra` YA era el nombre real de la hembra, así que aquí se añadió el macho), `oca_salvaje`/`ansarino_salvaje`, `ratona_de_campo`/`ratoncillo_de_campo`. Llevan `densidadBase`/`capacidadMaximaPorChunk` en 0 a propósito — repartir la densidad real de spawn entre macho/hembra/cría sin triplicar la población de cada especie es una decisión de diseño que no se ha tomado todavía; hoy jabalí/ciervo/corzo/cabra/ganso_salvaje/raton_de_campo siguen spawneando exactamente igual que antes.

Regla anotada como PERMANENTE dentro del propio `baker/catalogo/animales.json` (`_nota_convencion_macho_hembra_cria_2026_08_29` y `_nota_convencion_salvaje_macho_hembra_cria_2026_08_29`) para que cualquier especie doméstica o de caza/campo que se añada de aquí en adelante lleve su trío sin que haga falta volver a pedirlo. Ninguna de las 28 entradas nuevas tiene esqueleto todavía — es la base para un futuro sistema de cría entre animales de la misma especie y sexo distinto, no la mecánica en sí.

## Marchas embebidas (regla del streamer, acordada 2026-08-27)

**Todo lo que tiene esqueleto lleva SIEMPRE tres marchas de serie: parado, andar y correr** — aunque hoy nadie corra. API única `actualizar(dt, marcha)` con marcha 0/1/2 (boolean sigue valiendo como andar/parado) tanto en `rigHumanoide.ts` como en `animalVoxel.ts`. Correr = mismo ciclo acelerado y ampliado (zancada más larga y rápida, torso inclinado en humanos; galope con rebote en cuadrúpedos/aves/anfibios; coleteo/ondulación más fuertes en peces/serpientes) — funciona en los 7 esqueletos sin código por especie, con rampas suaves entre marchas.

**El disparo es automático por velocidad**: el cliente deduce la marcha del hueco hasta el destino de servidor en cada patch (andar deja ~0.25 casillas; más de 0.34 = corriendo). Cuando una mecánica futura haga correr a algo (sprint, huida de un ciervo, montura al galope), la animación se activa SOLA — cero cableado nuevo. Las acciones concretas (atacar, recolectar...) se triggearán más adelante sobre esta misma API.

## Verificado (v1)

- **Determinismo**: misma semilla+npcId → ficha idéntica (comprobado programáticamente).
- **Distribución**: 500 aldeanos → los rasgos salen clavados a los pesos del catálogo (corto 38% con peso 35, melena 24% con peso 25, sexo 50/50, marrón de ojos 45%...).
- **Visual**: galería con los 8 de prueba — se distinguen a simple vista alturas, anchuras, calvos/melenas/coletas/monje, barbas, canosos, tonos de piel y colores de ojos. El herrero sale ancho y barbudo, el guardia rapado/corto, el anciano menudo y cano — los overrides por NPC funcionan.

## Qué falta (pendiente, no bloquea)

- **La lista definitiva de NPCs** (la pacta el streamer). Al llegar: ampliar `npcs.json` + regenerar. Nada más.
- ~~Vestir auto en el cliente~~ — **HECHO (v1.3, el "materializado")**: `client/src/render3d/personajeVoxel.ts` crea el rig con la ficha (morfología+colores), fusiona pelo/barba y CADA prenda en una malla por pivote (`voxelMalla.ts`, color por vértice, un draw call por pivote, todas las caras) y la cuelga del pivote que declaran sus vóxeles — hereda la animación gratis. `animalVoxel.ts` hace lo mismo con animales: un grupo de Three por pivote con su origen de giro deducido (patas desde la cadera, alas desde el flanco, cola desde su unión) e idle por esqueleto (cola que se mueve, alas de insecto batiendo a 22hz, coleteo de pez, ondulación de serpiente por segmento, pinzas, respiración) + ciclo de andar genérico por contrafase listo para cuando se muevan. Los generadores emiten ahora `tam` (tamaño de celda) por vóxel con (x,y,z) = CENTRO — contrato exacto, el cliente no adivina resoluciones. El circuito entero está validado en el juego real: `personajes/src/exportar_demo.js` escribe `assets/personajes/demo_personajes.json` (3 NPCs vestidos con las 3 prendas según su profesión + 5 animales) y `game.ts` los materializa en una plaza junto al spawn de la ciudad — captura en el e2e de streaming. Cuando el servidor pueble NPCs de verdad consumirá este MISMO formato y la plaza demo desaparece.
- ~~Estilos de pelo femeninos propios (trenzas, recogidos)~~ — **resuelto (2026-09-01)**: ver "Ampliación de pelo/barba" más arriba — `trenza_simple`/`trenza_doble`/`trenza_corona`/`recogido_medio`/`mono`/`mono_bajo` ya existen, disponibles (con más peso) para mujer pero también accesibles a hombre.
- ~~parametrizar barba/estilos por sexo si hace falta más finura que "barba solo hombres"~~ — **resuelto (2026-08-30)**: `npc.permiteBarba` (bool, opcional en `npcs.json`) pisa la regla por defecto (`sexo==="hombre"`) sin tocar el generador — un NPC concreto puede llevar barba siendo mujer. Sin el campo, comportamiento idéntico a siempre.
- **Dónde vive la semilla de cada NPC del mundo** (¿del bake de POIs/interiores? ¿del servidor al poblar?) — misma pregunta abierta que el spawn de fauna.

## Ampliación 2026-08-30 (afinar parámetros antes de conectar IA)

- **Ropa automática por profesión**: si un NPC no trae `ropa` en `npcs.json`, `generarPersonaje` la resuelve cruzando `npc.profesion` con `ropa/catalogo/profesiones.json` vía el nuevo `elegirConjuntoPorProfesion` (`ropa/src/elegirPrenda.js`, ver `docs/GDD_Ropa_Procedural.md`) — determinista por `profesion` (misma profesión, mismo conjunto siempre, no por semilla del individuo, para que un mismo oficio vista igual en toda la aldea). Los 39 NPCs con `ropa` ya curada a mano no se tocan.
- **Razas de caballo** (`animales_rig.json`): `percheron` (grande, oscuro, de tiro), `arabe` (esbelto, veloz), `poni` (pequeño), `pinto` (bicolor), `criollo` (base sin override) — mismo mecanismo `razas` ya probado en perro/gato, cero código nuevo en `generarAnimal.js`.
- **Razas de cerdo**: `iberico`/`duroc` (oscuros/rojizos), `yorkshire` (rosado, = color base de siempre), `vietnamita` (pequeño y panzudo).
- Deliberadamente NO activados: los 3 colores de piel humana reservados a peso 0 (`hueso`/`ceniciento`/`piedra`) — subirlos haría que NPCs comunes salieran con tono de piedra/hueso sin que exista todavía ninguna mecánica de no-muertos/estatuas que lo justifique visualmente; se quedan reservados para cuando llegue esa mecánica.

## Ampliación de razas de granja/compañía (pedido streamer 2026-09-01: "más razas donde creas que puede haber... centrémonos en animales de granja y que puedan ser compañeros")

Mismo mecanismo `razas` de la sección "Razas por tamaño y forma" de arriba — CERO código nuevo en `generarAnimal.js`, solo contenido en `animales_rig.json`. Se tocaron las especies domésticas/de granja/compañía ya rigadas y se rigaron 4 nuevas que faltaban:

- **Perro** (+3, 8 en total): `chihuahua` (diminuto), `husky` (mediano, capas grises/blancas/negras), `bulldog` (bajo y macizo).
- **Gato** (+3, 8 en total): `bengala` (moteado), `azul_ruso` (gris-azulado), `esfinge` (silueta más fina — sin pelaje representable en vóxeles, se aproxima con un tono de piel claro en vez de pelaje).
- **Cerdo** (+2, 6 en total): `mangalica` (capa leonada/lanuda), `kunekune` (pequeño y rechoncho).
- **Caballo**: ya tenía 5 razas (`percheron`/`arabe`/`poni`/`pinto`/`criollo`) — sin tocar, cubría bien el hueco.
- **Burro** (nuevo `razas`, no tenía ninguno): `andaluz` (grande, gris), `miniatura` (pequeño), `comun` (base).
- **Buey** (nuevo `razas`): `pardo_alpino` (claro), `retinto` (rojizo oscuro), `comun` (base).
- **Gallo** (nuevo `razas`): `leghorn` (blanca, ligera), `orpington` (grande, dorada), `sedosa` (pequeña, plumaje suave), `comun` (base).
- **4 especies de granja NUEVAS en `animales_rig.json`** (no tenían rig todavía pese a existir en `baker/catalogo/animales.json` con `domesticable:true`): `oveja` (razas `merina`/`churra`/`negra`/`comun`), `cabra` (razas `alpina`/`enana_africana`/`angora`/`comun`), `oca` (esqueleto ave, razas `blanca_domestica`/`toulouse`/`comun`), `vaca` (lechera, par doméstico de `vaca_salvaje` distinto de `buey` — razas `holstein`/`jersey`/`charolesa`/`comun`, la holstein blanquinegra aproximada con dos colores sólidos ponderados, mismo truco que `pinto` en caballo). ~~`gallina`/`carnero`/`macho_cabrio`/`toro`/`ternero` siguen sin rig propio~~ — **corregido en la pasada siguiente, ver más abajo**: sí que reciben rig propio, cerraba un hueco real.
- **Verificado**: 1080 generaciones (27 especies rigadas × 40 semillas) sin una sola excepción; cada raza declarada en cada una de las 11 especies con `razas` apareció al menos una vez en la muestra (perro 8/8, gato 8/8, caballo 5/5, burro 3/3, cerdo 6/6, buey 3/3, gallo 4/4, oveja 4/4, cabra 4/4, oca 3/3, vaca 4/4). `poblacion/test/*.test.js`: 30/31 en verde (mismo fallo preexistente de huertos, no relacionado con animales).

## Trío macho/hembra/cría para TODA especie con razas (pedido streamer 2026-09-02: "¿todas las nuevas razas tienen sus versiones macho/hembra y cachorro? Obligatorio siempre que metamos nuevos animales")

Auditoría honesta que destapó el pedido: `baker/catalogo/animales.json` ya modelaba el trío macho/hembra/cría como IDS SEPARADOS desde antes (convención documentada en "Convención de trío macho/hembra/cría" más arriba) — pero `animales_rig.json` (el rig visual, incluidas las razas) solo se había dado de alta para UNO de los tres miembros de cada trío. La propia entrada `perra` en `baker/catalogo/animales.json` ya avisaba de esto desde que se creó: *"Sin rig propio todavía — cuando 'perro' tenga razas con esqueleto de verdad, 'perra' hereda el mismo tratamiento."* Con las razas de tamaño/forma ya en pie (v1.4) y ampliadas (sección de arriba), tocaba cerrar ese aviso.

- **19 rigs nuevos**: para cada especie con `razas`, se añadió su macho/hembra que faltaba (MISMAS `razas` que la entrada ya rigada — una raza no es de un sexo, un chihuahua puede ser perro o perra) y su cría (SIN `razas` propias — a esta fidelidad visual un cachorro/cría no distingue raza, mismas `proporciones` con una `escala` mucho menor). Tríos completados: `perro`/`perra`/`cachorro`, `gato`/`gata`/`gatito`, `caballo`/`yegua`/`potro`, `burro`/`burra`/`burrito`, `cerdo`/`cerda`/`cerdito`, `oveja`/`carnero`/`cordero` (oveja YA era la hembra real, carnero=macho añadido con cuernos), `cabra`/`macho_cabrio`/`cabrito` (cabra YA era la hembra real, macho_cabrio añadido con cuernos más largos), `oca`/`ganso_domestico`/`ansarino` (oca YA era la hembra real, ganso_domestico=macho añadido), `vaca`/`toro`/`ternero` (vaca YA era la hembra real, toro=macho añadido con cuernos largos — distinto de `buey`, el macho castrado de labranza que no se reproduce y ya tenía su propio rig sin trío). `gallo`/`gallina_salvaje`/`pollito`: `gallina_salvaje` YA tenía rig (de las 6 especies de prueba originales) pero sin razas; `pollito` se rigó como cría común.
- **Machos con más presencia que las hembras donde tiene sentido biológico** (mismo criterio ya usado por `percheron`/`arabe` etc. dentro de una raza): `carnero`/`macho_cabrio`/`toro` llevan `escala` ligeramente mayor y cuernos más largos/presentes que su contraparte hembra base — el resto de especies no diferencia tamaño por sexo (mismo nivel de fidelidad que el resto del catálogo, no merece más detalle).
- **`caballo` gana 2 razas más** (pedido streamer, "¿de caballo hay más razas?" — ya tenía 5, ahora 7): `frison` (negro, elegante) y `mustang` (resistente, colores variados) — heredadas automáticamente por `yegua` al compartir el mismo array `razas`.
- **REGLA PERMANENTE anotada en el propio catálogo** (`animales_rig.json`, clave `_notaReglaPermanente_macho_hembra_cria_2026_09_02`, mismo criterio que la nota de convención ya existente en `baker/catalogo/animales.json`): a partir de ahora, toda especie doméstica/de granja/compañía que reciba rig+razas DEBE dar rig también a su trío completo — sin que haga falta volver a pedirlo.

### Corrección inmediata: `heredaDe` en vez de duplicar el bloque entero (mismo pedido, siguiente mensaje: "¿la perra y el perro no pueden ser el mismo 3D con un atributo de sexo distinto? ¿Esto ya estaría hecho?")

Pregunta acertada — la primera pasada de arriba dejó los 19 rigs como **copias literales** del bloque `esqueleto`/`proporciones`/`rasgos`/`razas` de su especie base (perra = copia exacta de perro, etc.). Funcionaba (visualmente perro y perra YA eran idénticos, que es lo que se pedía), pero era contenido DUPLICADO de verdad: si alguien tocara las razas de `perro` más adelante sin acordarse de `perra`/`cachorro`, las tres entradas se habrían desincronizado en silencio.

Cerrado en la misma sesión, esta vez con un pequeño cambio de CÓDIGO (la excepción justificada a "las listas crecen, el código no": había duplicación real con riesgo de desincronización, no una abstracción especulativa):

- **`generarAnimal.js` gana `heredaDe`**: una entrada con `"heredaDe": "perro"` NO copia nada — en tiempo de generación toma el `esqueleto`/`proporciones`/`razas`/`coloresPosibles` ENTEROS de `perro`. Solo `escala` (obligatoria en quien hereda — una cría no mide lo mismo que el adulto) y un `rasgos` PARCIAL opcional (ej. `carnero: {rasgos:{cuernos:"cortos"}}` sobre la `oveja` base, que no tiene cuernos) pisan a la base. `esCria:true` además descarta `razas`/`coloresPosibles` (sin distinción de raza en una cría) y limpia cuernos/cresta con `limpiarRasgosCria()`. Las 19 entradas de la sección de arriba pasaron de ~50 líneas cada una a **una sola línea** (`{ "heredaDe": "...", "escala": [...], ... }`) — el catálogo entero bajó de 2624 a 1269 líneas sin perder ni un solo dato real (todo sigue derivándose, nunca copiado).
- **`heredaRazasDe` para el caso más ligero**: `gallina_salvaje` YA tenía su propio cuerpo real (proporciones/`colorDebug` distintos de `gallo`, no es un clon) — solo necesitaba COMPARTIR el reparto de razas, no heredar cuerpo entero. `"heredaRazasDe": "gallo"` hace justo eso: toma `razas` de `gallo` sin tocar nada más.
- **Respuesta directa a la pregunta**: sí, ahora perro/perra (y los otros 8 pares macho/hembra) son literalmente **el mismo modelo 3D** — mismo `esqueleto`, mismas `proporciones`, mismas `razas` — la única diferencia es qué id de `baker/catalogo/animales.json` se pidió (qué entidad/sexo es en el juego). Antes de esta corrección YA se veían igual en pantalla, pero por dos bloques de datos idénticos mantenidos a mano; ahora es un solo bloque de verdad (`perro`) más una referencia (`perra: {heredaDe:"perro", ...}`) que no puede desincronizarse.
- **Verificado**: 2760 generaciones (46 especies × 60 semillas) sin una sola excepción tras el refactor; comparación directa `perro` vs `perra` confirma mismo `esqueleto` y mismo pool de razas; `carnero`/`cordero` confirmados con cuernos/sin cuernos según toca; `gallina_salvaje` confirmada con proporciones propias (`tamCabeza` distinto de `gallo`) pero razas heredadas; `poblacion/test/*.test.js` (30/31, mismo fallo preexistente) y `server/test/{catalogoMonturas,inventario,interpretarPromptRopa}.test.ts` (60/60) sin regresión.

### Auditoría completa (pedido streamer 2026-09-03: "revisa que todos los animales están bien con la regla de sexo, mismo 3D")

Repaso sistemático de LOS 15 TRÍOS reales que existen en `baker/catalogo/animales.json` (cruzando cada adulto con su `criaId`), no solo de los 9 que ya tenían `razas`. Encontró y cerró 2 huecos reales y 1 bug real:

- **Hueco 1 — `conejo`/`coneja`/`gazapo` sin trío**: `conejo` es `domesticable:true` con par urbano real (`coneja`/`gazapo`, `biomas:[]`, EXACTAMENTE el mismo patrón que `perro`/`perra`) — se había quedado fuera de la pasada anterior porque esa pasada se centró en las especies que YA tenían `razas` (conejo no tiene). Cerrado con `heredaDe`, igual que el resto.
- **Hueco 2 — `ciervo`/`jabali` con macho rigado pero sin hembra/cría**: ambos son fauna salvaje domesticable/montable (con rig desde las 6 especies de prueba originales), pero `cierva`/`cervatillo`/`jabalina`/`jabato` nunca se rigaron — mismo motivo que el hueco 1. Cerrados con `heredaDe` (`cierva`/`jabalina` heredan también `montable`, ver bug de abajo); `corzo`/`corza`/`corcino` y `raton_de_campo`/`ratona_de_campo`/`ratoncillo_de_campo` se dejan sin rig A PROPÓSITO (siguen siendo `domesticable:false`, fauna de caza pura, decisión ya documentada en "Convención de trío macho/hembra/cría" arriba — no es un hueco, es fauna fuera del alcance de "granja/compañía").
- **Bug real (no un hueco de contenido, un fallo de código)**: `server/src/mundo/catalogoMonturas.ts` lee `montable`/`velocidadMontura` DIRECTAMENTE del bloque JSON de cada especie — nunca resolvía `heredaDe`. Resultado: `yegua`/`burra`/`cerda` (literalmente el mismo animal que `caballo`/`burro`/`cerdo`, verificado por esta misma auditoría con `cargarCatalogoMonturas()` real) habían quedado FUERA del catálogo de monturas desde la pasada anterior — una yegua no se podía montar aunque el caballo sí. Corregido: `cargarCatalogoMonturas` ahora resuelve `heredaDe` igual que `generarAnimal.js` (fusiona el bloque heredado, la propia entrada puede pisarlo si declara su propio `montable`), EXCLUYENDO siempre `esCria:true` (un potro/lechón/cervatillo no se monta aunque el adulto sí). Verificado con la función real: antes 7 especies montables, después 12 (+`yegua`/`burra`/`cerda`/`cierva`/`jabalina`, ninguna cría colada).
- **Verificado el catálogo entero, no solo lo nuevo**: los 15 trios reales de `baker/catalogo/animales.json` se recorrieron con un script — 13 COMPLETOS (rig en los 3 miembros) + 2 deliberadamente sin rig (fauna de caza no domesticable). 1560 generaciones (52 especies × 30 semillas) sin una sola excepción; `cierva`/`cervatillo` confirmados sin cuernos (el rasgo `ramificados` es solo de macho); `server/test/{catalogoMonturas,inventario,interpretarPromptRopa}.test.ts` (60/60) y `poblacion/test/*.test.js` (30/31, mismo fallo preexistente) sin regresión.

## Las 137 especies sin NINGÚN rig, cerrado — 177/189 (2026-09-08, checkpoint "¿podemos bakear Isla 1?", pedido streamer: "todo horneado ya, si algo no tiene su 3d se hace")

Distinto del hueco de arriba (`corzo`/`raton_de_campo` "sin rig A PROPÓSITO", 2026-09-03): AQUEL pendiente era sobre si una especie de caza pura necesita el TRÍO completo macho/hembra/cría con distinción de sexo (no, porque no cría de forma visible por ese sistema) — sigue siendo cierto y no se revierte aquí. Este es un problema DISTINTO y más gordo, encontrado en la auditoría "¿podemos bakear Isla 1?": de las 189 especies de `baker/catalogo/animales.json`, 137 no tenían NINGUNA entrada en `animales_rig.json` — ni una, ni con trío ni sin él — así que su fauna VIVA simulada (`server/src/mundo/fauna*.ts`, la que huye/caza/se reproduce, no la decoración estática de abajo) caía siempre a `client/src/render3d/animalPlaceholder.ts`, una caja genérica sin ninguna silueta real. Incluía TODA la fauna salvaje de tierra (zorro, oso_pardo, lince, corzo/corza/corcino, raton_de_campo y parientes...), TODAS las aves, TODOS los insectos y arácnidos, TODOS los reptiles/anfibios, y prácticamente toda la fauna marina.

**Cerrado con los MISMOS 7 esqueletos ya existentes** (`cuadrupedo`/`ave`/`insecto`/`pez`/`serpiente`/`crustaceo`/`anfibio`, cero plantilla nueva ni código nuevo en `generarAnimal.js`) — 125 especies añadidas: cada una con `esqueleto` + `proporciones` calculadas por un factor de tamaño relativo a una especie ya calibrada del mismo esqueleto (lobo=1.0 para cuadrúpedo, gallina_salvaje=1.0 para ave, abeja=1.0 para insecto pequeño, pez_mediano=1.0 para pez, culebra_de_agua=1.0 para serpiente, cangrejo=1.0 para crustáceo, rana=1.0 para anfibio) + `escala` de variación individual (0.85-1.15, mismo rango que el resto del catálogo). Reclasificaciones por SILUETA en vez de taxonomía estricta cuando encajaban mejor: anguilas (`anguila_abisal`/`anguila_marina`) y caballito de mar bajo esqueleto **serpiente** (cuerpo elongado/curvo, no fusiforme), y cetáceos (`orca`/`delfin_mular`/`ballena_azul`/`ballena_franca`) bajo esqueleto **pez** (torpedo/aerodinámico, aunque son mamíferos) — el rig es sobre forma visible, no sobre clasificación biológica.

**12 especies A PROPÓSITO siguen sin rig, documentado para que nadie las fuerce mal**: `medusa`, `estrella_de_mar`, `mejillon`, `almeja`, `pulpo`, `calamar`, `calamar_gigante`, `caracol_de_mar`, `pepino_de_mar`, `anemona_de_mar`, `ostra`, `erizo_de_mar` — moluscos/cefalópodos/equinodermos/cnidarios de cuerpo radial o con concha; NINGUNO de los 7 esqueletos existentes encaja con esa anatomía (ni cuadrúpedo ni serpiente ni nada con patas/columna sirve para un cuerpo sin ejes claros). Forzarlos habría sido peor que la caja genérica actual. Haría falta UNA plantilla nueva (filosofía #7 de CLAUDE.md: "si un contenido nuevo no encaja en ningún arquetipo, se añade una plantilla") — decisión de diseño aparte, no tomada en esta pasada.

Verificado: las 177 especies rigadas (52 previas + 125 nuevas) generan sin excepción con `generarAnimal()` real; galería SVG→PNG de una muestra de 21 especies nuevas (mamíferos de tamaños muy distintos, aves, reptiles) confirma visualmente siluetas y proporciones relativas coherentes (un ratón de campo minúsculo junto a un oso pardo grande, un águila real más grande que una codorniz, un cocodrilo bajo y alargado). **Sigue vigente, sin tocar**: la fauna DECORATIVA ESTÁTICA del mapa exterior (la que coloca `baker/decoracion.js`, InstancedMesh de color plano, NO simulada) sigue sin pasar por `generarAnimal`/rig — es un cableado de CLIENTE aparte (haría falta que esa pieza consulte el rig igual que ya hace la fauna viva), fuera de alcance de esta pasada, ya documentado como pendiente desde antes.

## Las últimas 12 especies (fauna marina radial/con concha), cerrado — 189/189 (2026-09-08, misma noche, pedido streamer: "crea esa fauna marina que falte")

Cierra el hueco que la entrada anterior dejó documentado a propósito. Ninguno de los 7 esqueletos existentes servía para un cuerpo radial o con concha, así que aquí sí hacía falta plantilla nueva (filosofía #7 de CLAUDE.md) — **9, no 1**, porque los 12 cuerpos que faltaban no son UNA sola anatomía: bivalvo (concha con bisagra, sésil), radial de 5 brazos (estrella), esférico con púas (erizo), columna+corona de tentáculos (anémona, sésil), tubo blando sin cabeza diferenciada (pepino de mar), cabeza+8 brazos colgando (pulpo), manto torpedo+aletas+ramillete de tentáculos (calamar), pie reptante+concha en espiral (caracol), y campana+tentáculos colgando (medusa) son anatomías genuinamente distintas — forzarlas en menos plantillas habría dado siluetas incorrectas.

**Limitación real del motor confirmada al diseñarlas**: una pieza es SIEMPRE una caja sin rotar (`{cx,y0,cz,w,h,d}`, nunca un ángulo) — así que cualquier "brazo"/"púa"/"tentáculo" que debe apuntar en una dirección concreta se resuelve eligiendo qué dimensión de la caja se alarga en el eje correcto (vertical para lo que cuelga o pincha hacia arriba/abajo, profundidad para lo que apunta al frente de nado), nunca con un ángulo arbitrario — la estrella de mar, por ejemplo, sale como una cruz de 4 brazos cardinales + 4 nudos diagonales cortos (aproximación de su simetría real de 5, la única forma de que un brazo "apunte" de verdad con cajas sin rotar).

Nuevas plantillas en `personajes/src/generarAnimal.js`: `bivalvo` (ostra/mejillon/almeja, reusada tal cual las 3 — solo cambian proporciones/color), `estrella`, `erizo`, `anemona`, `tubular` (pepino_de_mar), `pulpo`, `calamar` (reusada por `calamar_gigante` vía `heredaDe` + `escala` mucho mayor — MISMO patrón que perro/perra, un boss real del catálogo con `categoriaVida:alfa`/`vidaMaxima:300`), `caracol`, `medusa`. 12 entradas nuevas en `animales_rig.json`, dos de ellas (`estrella_de_mar`, `anemona_de_mar`) con `coloresPosibles` reales (el catálogo declara `variantes:5`/`variantes:8` — aprovechado aquí, no todas las especies del catálogo lo hacen).

**Animación de cliente añadida, no solo el rig offline**: `client/src/render3d/animalVoxel.ts` ganó casos de idle para `anemona`/`medusa`/`pulpo`/`calamar`/`tubular` (tentáculos/brazos que ondulan, campana de medusa que pulsa al nadar, aletas de calamar) — `bivalvo`/`estrella`/`erizo`/`caracol` se dejaron A PROPÓSITO sin caso (son sésiles o casi inmóviles de verdad; forzarles animación sería menos fiel que dejarlos quietos, mismo criterio que documentaba ya el propio archivo para pez/serpiente/crustáceo/anfibio).

Verificado: 189/189 especies rigadas (177 previas + 12 nuevas) generan sin excepción, 3 semillas cada una; galería SVG→PNG de las 12 nuevas confirma siluetas reconocibles (concha de bivalvo, cruz de estrella de mar, domo con púas del erizo, corona dorada de la anémona, tubo del pepino de mar, campana morada con tentáculos de la medusa, pulpo/calamar con brazos, calamar_gigante mucho más grande y de color propio); `cd client && npx tsc --noEmit` limpio, `client/test/*.test.ts` 35/35 sin regresión. **Con esto, la fauna VIVA simulada de toda la isla (`baker/catalogo/animales.json`, 189 especies) tiene rig 3D real al 100% — cero especie cae ya a la caja placeholder genérica.** No hizo falta rebakear nada: el bake solo coloca el `especieId`/posición (ya bakeado desde antes, ver `docs/GDD_Bakeador_POIs.md`), el rig se resuelve en cliente/servidor en el momento de simular/renderizar cada individuo.

## Bug real de 10 especies con geometría corrupta ("h": null en las orejas) — cerrado (2026-09-09, pedido streamer jugando: "algunos animales están desproporcionados según el tamaño del PJ")

Investigado midiendo (no a ojo) la altura real de cada especie generada por `generarAnimal()` — confirmado que la sospecha inicial de "bug dependiente del orden de llamada" (una generación corrupta solo si se llama después de otra especie en el mismo proceso) era **falsa**: el mismo defecto reproducía también generando la especie SOLA, en un proceso fresco. Causa real, mucho más simple: la tabla de alturas de oreja de `esqueletoCuadrupedo` (`generarAnimal.js`) —

```js
const altoOreja = { largas: 0.9, puntiagudas: 0.45, laterales: 0.2, caidas: 0.65 }[orejas] * p.tamCabeza;
```

— nunca tuvo entradas para `"cortas"` (jabalí/jabalina/jabato, `rasgos.orejas:"cortas"` en `animales_rig.json`) ni para `"ninguna"` (lagarto_ocelado/lagarto_de_ceniza/cocodrilo_del_pantano/galapago/foca_comun/foca/morsa — el catálogo YA declaraba `"orejas":"ninguna"` con la intención correcta de "sin orejas visibles", coherente con el convenio ya usado para `"cuernos":"ninguno"` en 9 especies distintas, pero el código nunca lo interpretó). Con cualquiera de esos dos valores, la búsqueda en el objeto devolvía `undefined`, `undefined * tamCabeza` daba `NaN`, y `JSON.stringify`/el pool exportado lo serializaba como `"h": null` en las 2 piezas de oreja — nunca en el resto del cuerpo (de ahí que el defecto pareciera "solo 2 piezas de 12", no una corrupción total). En el cliente, esa caja con `h` no numérico llega a `new THREE.BoxGeometry(p.w, NaN, p.d)` (`animalVoxel.ts`/`faunaDecorativaPool.ts`) — vértices `NaN` que además pueden corromper el bounding sphere de la malla FUSIONADA completa del animal (`voxelMalla.ts`), un candidato real para que el bicho entero se vea con geometría rota o desaparezca de la vista por un culling erróneo — consistente con la queja de "desproporcionado".

**Arreglado** añadiendo `cortas: 0.28` a la tabla y saltando el bucle de orejas entero cuando `orejas === "ninguna"` (sin dibujar nada, en vez de un valor arbitrario) — cero cambio para las especies que ya usaban `largas`/`puntiagudas`/`laterales`/`caidas`. Auditado el resto de rasgos (`hocico`/`cola`/`cuernos`) del catálogo entero contra sus tablas de lookup por si había más valores huérfanos: `perro`/`burro`/`perra`/`cachorro`/`burra`/`burrito` (`cola:"media"`), `macho_cabrio`/`toro` (`cuernos:"largos"`), `pollito` (`cola:"abanico"`), `zorro`/`ardilla` (`cola:"tupida"`), `castor` (`cola:"plana"`) también usan valores sin rama en su tabla — pero a diferencia de `orejas`, esas ramas usan un `if/else if` sin `else`, así que un valor no reconocido simplemente **no añade la pieza** (sin cola/sin cuernos extra) en vez de generar `NaN` — visualmente incompleto (un zorro sin su cola tupida detallada, con solo la genérica de tronco… en realidad ninguna, revisando el código: significa que esas especies concretas se quedan sin pieza de cola/cuernos en absoluto), pero NUNCA corrupto. Se deja documentado aquí para una pasada de contenido futura (añadir esas variantes reales a las tablas), no es la misma clase de bug y no bloquea nada hoy.

Se investigó también la queja de "árboles deberían ser más altos que el personaje": midiendo los 5 arquetipos de árbol reales (`taller-vox/generar_naturaleza.js`) contra la altura del jugador (1.57 unidades, `proporcionesRig.json`) — `ARBOL_CADUCO` 2.3-3.0u, `CONIFERA` 1.7-2.0u, `PALMERA` 2.0-2.4u, `SAUCE` 2.0-2.2u, todos ya por encima del jugador por diseño; solo `ARBOL_SECO` (tocón/árbol muerto/carbonizado) puede bajar a 1.4u, coherente con ser un tronco reseco, no un árbol vivo. Confirmado además con el bake real (`assets/mapas/principal/sector_004_006.json`, 1102 instancias de 17 especies de árbol reales) que la escala aplicada por individuo (`es`, jitter 0.85-1.15) nunca reduce un árbol por debajo del jugador. **Sin bug encontrado en árboles** — no se tocó nada; el árbol correcto en pantalla ya sale más alto que el jugador, contrario a la sospecha inicial.

Y la sospecha de que `perro` sale "del tamaño de una abeja": confirmado con 30 semillas que es varianza de RAZA intencional, no un bug — `perro` reparte 8 razas por peso (`razas` en `animales_rig.json`) desde `chihuahua` (`escala:[0.22,0.32]`) hasta `labrador`/`husky`/`pastor` (`escala` hasta 0.9) — la altura real oscila 0.18-0.95 unidades según qué raza+escala toque esa semilla, un chihuahua real ES así de pequeño comparado con un labrador. Ninguna acción tomada, documentado para que ninguna sesión futura lo "arregle" sin comprobarlo primero.

Verificado: sweep completo de las 189 especies reales del catálogo (`generarAnimal()` sobre cada una) — 0 piezas corruptas tras el fix (antes 10 especies con 2 piezas `NaN`/`null` cada una); `assets/animales/pool.json` (fauna decorativa) regenerado y confirmado 0 corrupciones en sus 770 variantes. **Sin verificación visual en cliente real** (ver directamente un jabalí/foca con orejas ya bien formadas en pantalla) — documentado como gap, mismo criterio honesto del resto de esta sesión.

## Fauna EN VIVO (salvaje/mascotas/monturas) con cuerpo real, port parcial cuadrupedo+ave (2026-09-10, pedido streamer jugando: "veo rectángulos con vida arriba moverse")

Hueco DISTINTO de todos los de arriba: los apartados anteriores cerraron el rig OFFLINE (`personajes/catalogo/animales_rig.json`, 189/189 especies) y su consumo por la fauna DECORATIVA estática (`assets/animales/pool.json`) — pero ningún individuo VIVO en pleno juego (fauna salvaje de `GestorFaunaSalvaje`, mascotas, monturas) pasaba nunca por ese rig: `client/src/game.ts` los renderizaba siempre con `render3d/animalPlaceholder.ts`, una única caja con las proporciones correctas pero sin patas/cabeza/cola — de ahí la queja ("rectángulos con vida arriba moverse": la caja + la barra de vida de `escena.actualizarVida`, que SÍ es real y aplica a cualquier `Fauna` con `vida>0`, no a un bug de vida). La causa raíz: `generarAnimal.js` (el módulo que sí sabe construir el cuerpo completo) es CommonJS pensado para Node — nunca se había portado a TypeScript para que el cliente (bundle de navegador) pudiera generar un individuo bajo demanda, a diferencia de la ficha de personaje del jugador, que si tuvo ese port (`crearFichaVoxel.ts`, 2026-09-10 más temprano) precisamente para resolver el mismo tipo de problema.

**Alcance de esta pasada, acordado con el streamer explícitamente ("¿lo hago completo o empiezo por cuadrupedo+ave?" → "haz esto" sobre la segunda opción)**: portar SOLO `esqueletoCuadrupedo` y `esqueletoAve` (79+45 = 124 de las 189 especies, cubriendo lobos/ciervos/conejos/perros/gatos/vacas/ovejas/caballos y gallinas/patos/águilas/cuervos/pájaros — la inmensa mayoría de lo que se cruza a pie por tierra) — los otros 14 esqueletos (pez/serpiente/insecto/crustaceo/anfibio + las 9 plantillas marinas radiales) quedan para una pasada futura, siguen cayendo exactamente al mismo placeholder de caja que antes, sin regresión.

**`client/src/render3d/generarAnimalVoxel.ts`** (nuevo): port línea a línea de `esqueletoCuadrupedo`/`esqueletoAve` + `resolverHerencia`/`limpiarRasgosCria` (`heredaDe`/`heredaRazasDe` — imprescindibles: de las 79 especies cuadrupedo reales, 21 solo tienen rig vía herencia — perra/cachorro/gata/gatito/yegua/potro/toro/ternero/carnero/cordero/macho_cabrio/cabrito... — sin resolverla, la mayoría de la fauna de granja habría seguido cayendo al placeholder aunque su esqueleto base SÍ estuviera cubierto) + `elegirPonderado`/`crearPRNG` (mulberry32) inlineados, mismo criterio ya documentado en `crearFichaVoxel.ts` de no cruzar un `require()` de Node al bundle del navegador. Selección de razas/color/escala/sexo idéntica a `generarAnimal.js` (razas reales de perro — chihuahua a labrador — con su propio color/proporciones/escala, cuernos ramificados de ciervo solo en sexo macho...). Firma: `generarAnimalVoxel(especieId, individuoId)` — a diferencia del bake offline (una `semilla` de contexto), un individuo vivo no tiene semilla propia: se usa el `id` real de su entrada en el Schema de Colyseus (la clave del `MapSchema` de `fauna`/`mascotas`) como semilla estable, así que el mismo individuo se ve igual para cualquier cliente mientras exista, y sigue viéndose igual entre patches sucesivos (nunca se regenera por tick, solo una vez al crear su rig en `onAdd`). Cualquier especie fuera de estos dos esqueletos, o sin rig declarado, cae a `animalPlaceholder(especieId)` exactamente igual que antes — la función nunca lanza.

**Consumidores actualizados en `client/src/game.ts`** (los 4 sitios que antes llamaban a `animalPlaceholder` directamente): fauna salvaje (`state.fauna.onAdd`, semilla = id del Schema — solo si no hay vox pre-horneado de `fauna.json`, que sigue teniendo prioridad 1 sin cambio), mascotas (`state.mascotas.onAdd`, semilla = id), montura (semilla = `montura|${sessionId}` — sin id individual propio disponible en ese punto: mientras se monta, la entrada de `Mascota` desaparece del Schema fusionada en `Player.monturaEspecieId`, documentado ya en el propio schema; usar el `sessionId` del jinete da un aspecto estable mientras dure la sesión de montar, igual de determinista que el placeholder de una caja que sustituye, sin perder nada), y el fallback de cadáver de fauna salvaje (`cadaveres.onAdd`, rama `tipoOrigen !== "jugador"/"npc"`) — este último gana además la pose "caído" REAL de patas/alas ya implementada en `animalVoxel.ts::aplicarPoseCaidaAnimal` para cuadrupedo/ave (antes solo tenía el volcado genérico de una caja entera, sin patas separadas).

Verificado: `client/test/generarAnimalVoxel.test.ts` (nuevo, 11 tests) — determinismo por `individuoId` (mismo id, mismo resultado byte a byte), individuos distintos de la misma especie no coinciden, `lobo`/`arrendajo` salen con pivotes reales (`pataDelIzq`/`cabeza`/`cola`, `alaIzq`/`pico`...) en vez de las 2 piezas del placeholder, herencia `heredaDe` (`perra`) y `heredaRazasDe` (`gallina_salvaje`) producen cuerpo articulado igual que su base, razas de `perro` caen dentro del rango de escala combinado esperado (chihuahua a husky), regresión dirigida del bug de `orejas:"ninguna"` de la sección anterior (`lagarto_ocelado` sin piezas `NaN`), sexo determinista con cuernos de `ciervo` apareciendo/desapareciendo según semilla (nunca al azar entre llamadas repetidas), y que una especie de esqueleto no portado (`pez_mediano`) y una especie inexistente siguen cayendo exactamente al mismo placeholder de siempre. Además un smoke test manual (no comiteado, `crearAnimalVoxel` real de `animalVoxel.ts` sobre 9 especies incluyendo herencia/razas) confirmó que el resultado se integra sin excepciones con el renderer real: construye grupos/pivotes, anima las 3 marchas y la pose "caído" sin errores. `cd client && npx tsc --noEmit` limpio, suite completa de cliente 66/66 (55 previos + 11 nuevos) sin regresión, servidor 1317/1317 sin regresión (ningún archivo de servidor tocado — cambio puramente de renderizado de cliente, cero impacto en el Schema/simulación). **Sin verificación visual en navegador real** (ver un lobo/gallina con cuerpo articulado de verdad en pantalla, moviéndose) — documentado como gap honesto, mismo criterio del resto de esta sesión; pendiente de que el streamer lo confirme jugando. **Pendiente real, explícitamente diferido**: portar los otros 14 esqueletos (pez/serpiente/insecto/crustaceo/anfibio + las 9 plantillas marinas) para que TODA la fauna en vivo (no solo tierra) deje de caer al placeholder.
