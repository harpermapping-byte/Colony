# GDD — Bakeador de Interiores

Diseño del generador de interiores (casas, tabernas, castillos, aldeas — **no** mazmorras/cuevas, eso es un bakeador aparte, ver sección 11). Mismo espíritu que `GDD_Bakeador_Exteriores.md`: reutilizar patrones ya probados (catálogo en dos niveles, reglas de sitio, variantes, densidad regional) en vez de inventar un sistema nuevo desde cero.

## 1. Arquitectura general

- **Cada interior es una instancia separada**, con su propio espacio de coordenadas — nada que ver con la rejilla de chunks del mapa exterior. Se entra por la puerta de un POI del exterior (mismo mecanismo `tipo: "portal"` que ya existe en `baker/catalogo/pois.json`), que solo coloca el disparador; la instancia real vive aparte.
- **Escala humana, no de mapa exterior**: un edificio no necesita tiles equivalentes a "20 minutos andando". Misma unidad de tile que exteriores (para que moverse por una puerta no cambie la sensación de escala del personaje), pero una sala es una rejilla pequeña — del orden de 5 a 15 tiles de lado según tipo de sala — en vez de un chunk de 32x32. El detalle fino (columnas, conductos, mobiliario) sale de tener muchos elementos pequeños colocables, no de subdividir el tile.
- **Un edificio de varias plantas es una pila de plantas WFC independientes** (sótano/bodega hacia abajo, pisos hacia arriba desde la entrada), conectadas verticalmente por huecos de escalera/trampilla — mismo concepto que una puerta entre dos salas de la misma planta, solo que en el eje Z.
- **Generación dirigida por config**, igual que `generarMapa(config)` en exteriores: el usuario (o el propio POI del exterior) pide `tipoEdificio` + plantas + preferencias de material/riqueza, y el motor resuelve el resto con WFC + catálogo. Misma semilla = mismo edificio siempre; otra semilla = layout distinto pero coherente con lo pedido.
- **Consola visual con campos, no descripción libre** — mismo motivo que la GUI de exteriores tiene checkboxes/campos numéricos en vez de un prompt: la coherencia sale de que las reglas del motor actúan sobre un config estructurado, no de interpretar bien un texto la primera vez. Una capa de texto→config (ej. "taberna 2 pisos 1 bodega en aldea" → el JSON de abajo) puede añadirse después como azúcar sobre lo mismo, nunca como sustituto.
- **Todos los campos son opcionales excepto `tipoEdificio`** — lo que no se especifica lo decide `tipos_edificio.json` (rango de plantas, mezcla de salas por planta, riqueza, materiales) tirando de la semilla, exactamente como en exteriores un bioma sin `reglasSitio` extra simplemente usa el pool por defecto. Ejemplo con (casi) todos los campos usados a la vez:

```json
{
  "tipoEdificio": "taberna",
  "semilla": "taberna-aldea-04",
  "contexto": { "bioma": "pradera", "poi": "aldea_agricola" },
  "riqueza": "modesta",
  "estadoConservacion": "habitado",
  "materialesPreferidos": ["madera", "piedra"],
  "densidadDecoracion": 0.7,
  "densidadSuciedad": 0.2,
  "plantas": [
    {
      "nivel": -1,
      "tipo": "bodega",
      "materialPreferido": "piedra",
      "tamano": { "anchoTiles": [7, 9], "largoTiles": [7, 10] },
      "salasForzadas": ["almacen"]
    },
    {
      "nivel": 0,
      "tipo": "planta_baja",
      "conectorArriba": "escalera_recta"
    },
    {
      "nivel": 1,
      "tipo": "planta_alta",
      "tamano": { "anchoTiles": [6, 8], "largoTiles": [8, 10] }
    }
  ]
}
```

Campos de la sección `plantas[]`: `nivel` (0 = planta baja, negativo = bajo tierra, positivo = superior — decide ventanas automáticamente, sección 7), `tipo` (rol de planta usado para elegir el pool de `salasPorPlanta` en `tipos_edificio.json`), `materialPreferido` (override puntual de esa planta, si no se hereda `materialesPreferidos`), `tamano` (override del rango de tiles de esa planta — si se omite, sale del tamaño típico de las salas que le tocan), `salasForzadas` (tipos de sala que sí o sí aparecen, aunque el sorteo ponderado no los saque), `conectorArriba` (qué tipo de `conectores.json` sube a la siguiente planta — si se omite, el motor elige uno válido según riqueza/rol). `densidadDecoracion` y `densidadSuciedad` son los mismos multiplicadores 0-1 que ya existen en exteriores para vegetación/fauna, aplicados aquí a mobiliario y a la capa de suciedad respectivamente — independientes entre sí, para poder pedir "una sala noble pero muy sucia" (abandonada hace tiempo) sin bajarle la riqueza.

- **`amueblado`** (opcional, por defecto `"completo"`) — qué capas de decoración coloca el bakeador de verdad, pensado para que un edificio propiedad de un jugador pueda generarse como cascarón y el jugador lo amueble él mismo desde el futuro menú de construcción (sección 7ter):
  - `"vacio"`: solo capa Estructural (suelo/pared/techo/puertas/ventanas/escaleras) — nada de `decorFija`, `decorMovible`, `iluminacion` ni `suciedad`.
  - `"fijo"`: Estructural + `decorFija` (chimenea, columnas, estanterías empotradas, tapices) — `decorMovible` se deja vacío a propósito, son justo los huecos que el jugador rellena.
  - `"completo"` (lo que se ha descrito hasta ahora en todo este documento): las 5 capas, igual que un edificio NPC que nadie va a redecorar.
  - Un edificio NPC (taberna, castillo, ayuntamiento...) casi siempre pide `"completo"`; una parcela de vivienda de jugador pide `"vacio"` o `"fijo"`.

## 2. Generación de la forma: descartado Wave Function Collapse, catálogo de plantillas en su lugar

**Decisión cerrada 2026-09-04, no reabrir sin pedirlo el streamer explícitamente** (ver también sección 10, que ya daba este mismo argumento para la PLANTA): al preguntarle si quería WFC completo para la forma de sala o algo más simple, contestó *"prefiero que las salas tengan posibilidad de salir de 15 plantillas diferentes con variación de tamaño según edificio"* — es decir, NO un solver de Wave Function Collapse tile a tile, sino exactamente lo que dice: un **catálogo de plantillas de forma** (mismo espíritu que el resto del proyecto — "el catálogo coloca por id, el algoritmo decide cómo aplicarlo", CLAUDE.md punto 2) con variación de tamaño real. Implementado de punta a punta — ver sección 2ter para el diseño completo. Las notas de esta sección quedan como registro histórico de lo que se descartó y por qué:

- ~~WFC resuelve la planta de cada piso sobre una rejilla de tiles...~~ — nunca se llegó a implementar; la composición de PLANTA la resuelve el BSP de la sección 9septies (empaquetado de rectángulos ya dimensionados, no WFC) y la FORMA de cada sala individual la resuelve ahora el catálogo de plantillas de la sección 2ter.
- **Sin muros en diagonal** seguía siendo la decisión correcta y se conserva: las 15 plantillas de la sección 2ter son todas ortogonales (rectángulos sumados/restados), cero geometría a 45°.
- **Salas redondas como plantilla ya resuelta** — la idea de fondo (una forma especial con sus puntos de conexión ya definidos, en vez de resolverla tile a tile) es exactamente el mecanismo que sí se construyó: cada plantilla de `catalogo/formasSala.json` es una forma ya resuelta, aplicada por `interiores/src/formasSala.js` al tamaño concreto que le toque.
- Tras resolver la forma, cada sala recibe un **tipo de sala** (ver sección 4) — esto no cambió.

## 2bis. Detección de sala: un conjunto de tiles + metadatos, no un rectángulo

`interiores/src/salas.js` implementa la **Sala** como abstracción independiente de cómo se decidió qué tiles pertenecen a ella — es la pieza que hacía falta para que WFC (cuando exista), selección manual y una herramienta rectangular (ambas del futuro menú de construcción, no del bakeador — sección 11) puedan compartir el mismo objeto sin que el resto del motor (`colocarElementos.js`, cálculo de estadísticas) tenga que saber cuál de las tres se usó:

- Una `Sala` es `{ tiles: Set, puertas: Set, ventanas: Set, minX/maxX/minY/maxY, ancho, largo }` — el conjunto de tiles de suelo conectados, no una forma geométrica fija.
- **Detección por flood-fill de área cerrada por paredes** (`detectarSalas`): sobre una rejilla de tiles (`suelo`/`pared`/`puerta`/`ventana`), cada región de 4-conectividad de tiles `suelo` es una sala; las puertas/ventanas que tocan su borde quedan anotadas como sus aberturas. Es la opción fiable pedida — no depende de adivinar nada, solo de qué tiles son de qué tipo — y es exactamente lo que hace posible la sección 2ter sin tocar `salas.js`: una plantilla de forma no rectangular simplemente deja sin marcar `SUELO` las celdas fuera de su máscara dentro de la caja delimitadora, y `detectarSalas` dibuja el perímetro real (mordiscos/concavidades incluidos) tratándolas como pared, sin ningún caso especial.
- **Circulación intacta** (`circulacionIntacta`): antes de dar por buena la colocación de un mueble, se vuelve a hacer flood-fill de los tiles de suelo libres (los de la sala menos los ya ocupados por huellas) desde la puerta — si no todos quedan alcanzables, esa colocación se descarta y se prueba otra. Es el chequeo real de "no puede bloquear la circulación principal" (sección 6 del pedido de integración): sin pathfinding ni heurísticas de ruta, solo alcanzabilidad.

## 2ter. Forma de sala: catálogo de plantillas (NO Wave Function Collapse, 2026-09-04)

Cierra el pendiente de la sección 2: pedido explícito del streamer, "15 plantillas con variación de tamaño según edificio". Implementado en `interiores/catalogo/formasSala.json` (el catálogo) + `interiores/src/formasSala.js` (el algoritmo que lo aplica) — cero WFC, mismo patrón "catálogo por id + algoritmo determinista" que el resto del proyecto.

- **Cada plantilla es una máscara booleana DENTRO de una caja delimitadora `ancho x largo`** (el mismo `ancho`/`largo` que `colocarSala` ya decidía antes de este catálogo, tirado del rango `anchoTiles`/`largoTiles` de `tipos_sala.json`) — no una forma final: una lista de rectángulos en coordenadas FRACCIONALES `[0,1]x[0,1]` (`x0,y0,x1,y1` sobre el eje ancho/largo) que se suman/restan (`modo:"sumar"|"restar"`, procesadas en el orden del array). Al convertirlas a tiles para un ancho/largo concreto (celda `i` de `n` cae en la fracción si su CENTRO `(i+0.5)/n` está dentro del rango — evita ambigüedad de redondeo en los bordes), la MISMA plantilla sirve para una sala pequeña de una `casa_humilde` y para el gran salón de una mansión a escalas de tile distintas: parametrizada de verdad, no una rejilla de píxeles fija. Esto es lo que hace real "variación de tamaño según edificio".
- **15 plantillas declaradas**: `rectangulo` (la de siempre, sigue siendo un resultado válido y frecuente — nunca una entrada especial), `ele`/`ele_estrecha` (L con brazo grande/estrecho), `te`/`te_invertida` (barra+brazo, orientación normal/invertida), `u` (herradura abierta al norte), `cruz`/`cruz_asimetrica` (planta de cruz simétrica / con transepto desplazado), `esquina_mordida`/`esquina_mordida_doble` (chaflán en una o dos esquinas del muro norte), `alcoba_lateral`/`alcoba_doble` (nicho/hornacina a uno o los dos lados), `escalonada` (perfil en cuña, mitad sur ancha/mitad norte estrecha), `mancuerna` (dos bloques conectados por un cuello estrecho) y `nicho_central` (mordisco en el centro del muro norte, sin tocar ninguna esquina). El detalle geométrico exacto de cada una (las fracciones) vive únicamente en `formasSala.json` — no lo dupliques aquí, léelo ahí si hace falta tocar una forma.
- **Elegibilidad — no todas las salas pueden salir con forma no rectangular** (`elegirFormaSala`): solo las categorías con carácter arquitectónico real (`tipos_sala.json:categoria` ∈ `civico`/`religioso`/`ocio`/`comercio` — gran salón, capilla, salón de baile, sala de comercio...) y solo si el tamaño que le tocó a ESA instancia concreta es ≥6 tiles de lado (una celda 2x3 o un taller 4x4 no tiene margen real para que una L se LEA como tal). Fuera a propósito: residencial/utilidad/almacenamiento/artesania (mobiliario en fila contra un muro recto se lee mejor en rectángulo) y `circulacion` (el pasillo es la columna vertebral literal de `generarPlanta` — su ancho/largo real sostiene la fila de habitaciones de encima, tiene que seguir siendo un rectángulo exacto; `bsp.js` tampoco se tocó). Dentro de una sala elegible, 55% de probabilidad de forma no rectangular (`PROBABILIDAD_FORMA_NO_RECTANGULAR`) — el resto sigue saliendo rectángulo, determinista por semilla (`crearPRNG` de siempre). Salas con `tipos_sala.json:simetrico:true` (columnas/decoración en espejo vía `colocarSimetrico`) solo pueden tocarle una plantilla ya simétrica en el eje X (`simetricoEjeX:true` en el catálogo) — con una asimétrica, esas piezas simplemente no encontrarían su par en espejo la mayoría de las veces.
- **Salvaguarda real, nunca se cuela una sala rota** (`mascaraValida`): tras construir la máscara (y aplicar el espejo horizontal 50/50 en las plantillas con `espejoValido:true`, variedad barata sin declarar la plantilla ya reflejada), se exige única componente conexa por 4-vecinos, ≥40% del área de la caja delimitadora aprovechable, y suelo real en la fila sur (`y=largo-1`, donde cae la puerta). Si algo no cumple (combinación rara de fracciones + ancho/largo pequeño), cae automáticamente a `null` = rectángulo de siempre — mismo comportamiento que si esta plantilla no existiera.
- **Integración en `colocarElementos.js`, sin romper el rectángulo de siempre**: `mascaraGrid` (boolean[largo][ancho] o `null`) gobierna todo lo que antes asumía "toda la caja es suelo" — `libreSuelo` (huecos para muebles), la elección de `puerta.x` (la celda de suelo real de la fila sur más cercana al centro, en vez de siempre el centro exacto), `tocaPared` (generalizado: cualquier vecino que no es suelo real, así una L "toca pared" también en su rincón interior), la rejilla que alimenta `detectarSalas`, la ventana norte (`permiteVentanas`), los decals de suelo y lo colgado en pared. Con `mascaraGrid=null` (el 100% del motor anterior a este catálogo, y el resultado más frecuente incluso ahora) el comportamiento es BYTE A BYTE idéntico al de antes (verificado: el test "misma semilla + mismo catálogo = mismo edificio byte a byte" de `catalogo.test.js` sigue en verde sin tocarlo). `edificio.js:pintarSalaEnPlanta` (pinta la sala sobre la rejilla de la PLANTA) respeta la misma máscara; `buscarHuecoConector` (escaleras/trampillas) también, para que un conector vertical nunca "imprima" suelo pisable fuera de la forma real.
- **Contrato de salida — mismo formato que ya usan las mazmorras orgánicas**: `colocarSala` devuelve `mascara` (string `'ancho*largo'` de `'1'`/`'0'`, fila a fila) SOLO si la sala salió con plantilla no rectangular — con rectángulo, el campo es `undefined`, exactamente el contrato de antes. Este es el MISMO campo `resultado.mascara` que `mazmorras/src/celular.js` ya usaba para cuevas orgánicas y que `server/src/mundo/interiorColision.ts` ya sabía leer (`if (resultado.mascara && resultado.mascara[y*ancho+x] !== "1") continue;`) — cero cambio en el servidor para que la colisión respete la forma real de una sala civica/religiosa/ocio/comercio no rectangular. `formaSalaId` viaja aparte solo para depuración/UI (qué plantilla produjo esta sala) — nada del motor decide nada mirándolo, la máscara es la única fuente de verdad. `edicion.js:regenerarMobiliario` fuerza `anchoForzado`/`largoForzado`/`formaSalaForzada` al llamar a `colocarSala` de nuevo — "regenerar mobiliario" nunca debe cambiar la forma/tamaño de la sala por debajo del jugador.
- **Renderers de verificación visual actualizados** (antes asumían todos "un único rectángulo, muros solo en los 2 lados que da la cámara"): `interiores/src/prueba_render_iso.js` (galería SVG offline), `interiores/gui/index.html` (editor 2D interactivo — las celdas fuera de la máscara ni se pintan ni son clicables para mover/añadir) y `interiores/gui/vista3d.js` (vista 3D girable del edificio completo) dibujan ahora un tile de suelo por celda de suelo REAL y ponen pared en cualquier lado de una celda que colinde con una celda que no es suelo real (perímetro real de la forma, mordiscos/concavidades internas incluidas) — con rectángulo, resultado idéntico al de siempre. `interiores/src/formasSala.js:segmentosDePared` es el cálculo compartido (Node); `vista3d.js`/`gui/index.html` lo portan suelto (mismo criterio ya usado en el proyecto para `crearPRNG` entre cliente/servidor — ESM de navegador no puede `require` un módulo CommonJS de Node).
- **`interiores/src/edificio.js:generarHabitacionCompuestaL`** (la técnica de fusión de dos rectángulos por abertura ancha, previa a este catálogo, sección 9septies) fuerza `formaSalaForzada:"rectangulo"` en sus dos brazos a propósito: su propio algoritmo de fusión ya asume que cada brazo es un rectángulo completo en el borde compartido; dejar que un brazo saliera ADEMÁS con su propia plantilla no rectangular podía encoger o romper la abertura calculada entre los dos. No está conectada a `generarPlanta`/`generarEdificio` (nunca lo estuvo, es una función de utilidad solo ejercitada por su propio test) — la vía real para salas no rectangulares en un edificio de verdad es el catálogo de plantillas de esta sección.

**Probado**: `interiores/test/catalogo.test.js` 42/42 (sin tests nuevos dedicados a formasSala en esta pasada — la cobertura real es que las 74 tipologías de edificio, el BSP, las puertas múltiples y las ventanas siguen en verde generando edificios reales que ahora SÍ pueden traer salas no rectangulares mezcladas), `interiores/test/editor.e2e.js` 11/11 (Playwright, editor web real), `ciudades/test/ciudad.test.js` 13/13, `mazmorras/test/mazmorra.test.js` 13/13 y `server/test/interiorColision.test.ts` 7/7 sin tocar nada de `server/` — confirma que `resultado.mascara` de una sala de `interiores/` la consume el loader de colisión en vivo exactamente igual que ya hacía para las cuevas orgánicas de mazmorras. Verificado visualmente con capturas reales (no solo afirmado): galería SVG de `prueba_render_iso.js` con 6 salas de categorías/plantillas distintas (`gran_salon`/te_invertida, `sala_comercio`/escalonada, `capilla`/cruz, `salon_baile`/u, `biblioteca`/alcoba_doble, `salon_jarl`/mancuerna — sin huecos sueltos ni muebles fuera de la máscara, puertas en sitios sensatos), el editor 2D interactivo real (`gui/index.html`, `casa_noble`/biblioteca en forma `ele`) y la vista 3D girable del edificio completo (`gui/vista3d.js`, misma sala visible en su planta con el resto del edificio alrededor).

## 3. Catálogo en dos niveles (mismo patrón que exteriores)

- **Materiales** (lista pequeña, ampliable) — el acabado de suelo/pared/techo: madera, piedra, ladrillo, estuco, papel pintado, tela/tapiz, metal, cristal. Equivalente a `categoriaRecurso` en exteriores.
- **Elementos** (lista grande, ampliable) — cada mueble/decoración/estructura fija es su propia entrada, como una `especie` en vegetación/animales. Campos por entrada: `capa`, `colocacion` (reglas de sitio, como `reglasSitio` en POIs), `huella` (footprint rectangular, como `radio` en POIs), `variantes`, `tiposSalaValidos`, `materialesCompatibles`. Opcionalmente `tileInteraccion` (offset relativo dentro de la huella en orientación 0° — sección 7quater) y `aportes` (contribución a las estadísticas de sala — sección 9bis).
- **`madera` y `piedra` no tienen un número de variantes propio — reutilizan el catálogo real de exteriores**: una mesa de madera puede salir de cualquiera de las ~26 especies de árbol ya definidas en `baker/catalogo/vegetacion.json` (pino, roble, abedul, sauce...), agrupadas por su `categoriaRecurso` (`madera_dura`/`madera_blanda`/`madera_sauce`/`madera_abedul`/`madera_palmera`/`madera_carbonizada`); un mueble de piedra igual contra `baker/catalogo/rocas.json` (`categoriaRecurso: piedra_comun` — granito, pizarra, caliza...). El cambio es sobre todo de tinte/textura del sprite, no una malla distinta — es la forma barata de multiplicar variación real (`interiores/catalogo/materiales.json` documenta el campo `especiesFuente` exacto). Una sola fuente de verdad: si se amplía el catálogo de árboles/rocas de exteriores, el mobiliario de interiores se vuelve más variado sin tocar nada aquí.

## 3ter. Coherencia de mobiliario: anclas y satélites, no piezas sueltas al azar

Colocar cada elemento de forma independiente (tirada propia por casilla, sin mirar qué hay ya colocado) da salas con muebles sueltos por ahí sin relación entre sí — una mesa en una esquina y las sillas en otra punta de la sala. En vez de eso, `interiores/src/colocarElementos.js` distingue dos roles:

- **Ancla**: cualquier elemento con `esSuperficie: true` (mesa_comedor, mesa_consejo, altar, mostrador...) — se coloca primero (antes que el resto de la capa), normalmente cerca del centro de la sala.
- **Satélite**: un elemento con `colocacion: ["juntoAMesa"]` (silla, banco, taburete) — se coloca en el anillo de casillas justo alrededor de una de las anclas ya puestas, nunca en un punto suelto de la sala. Si hay más de una mesa, se reparte entre las que menos satélites tengan. A diferencia del resto de piezas (una sola instancia por sala pequeña, para no repetir), un satélite sí puede repetirse varias veces (hasta 4) — es justo lo que da la imagen real de "mesa con sus sillas alrededor".
- **Simetría real** para salas con `simetrico: true` (`tipos_sala.json`): un elemento cuya única colocación es `simetrico` (ej. `columna`) se coloca por parejas en espejo respecto al eje central de la sala, no como instancia única descentrada.
- Los elementos `pegadaAPared` (chimenea, estanterías empotradas...) ya se colocaban pegados a un muro real, no sueltos — eso ya funcionaba antes de este apartado, aquí se explicita como parte del mismo principio: **nada se coloca sin una razón espacial**, cada `colocacion` de la tabla de la sección 7ter implica una regla de posición real, no solo una etiqueta descriptiva.

Verificado con un render real (`interiores/src/prueba_render_iso.js`): una `cocina_comedor` sale con la mesa en el centro y las sillas pegadas a su alrededor, no dispersas.

## 4. Capas

1. **Estructural** — suelo, pared, techo. Terreno = mecánica (transitable/bloquea), no decoración, mismo principio que en exteriores.
2. **Decoración fija** — no se mueve en juego: estanterías empotradas, chimenea, columnas, ventanas, puertas.
3. **Decoración movible** — mobiliario: cama, mesa, silla, arcón. "Movible" es una etiqueta para una futura mecánica (el jugador podría reposicionarlo); el bakeador la coloca igual que la fija, no cambia cómo se genera.
4. **Suciedad/desgaste** — capa puramente cosmética encima de las anteriores: telarañas, grietas, manchas, hojas caídas, polvo. No bloquea nada. Densidad modulada por el **estado de conservación del edificio** (nuevo/habitado/abandonado/ruina parcial) — mismo mecanismo que la densidad regional del bosque en exteriores, pero aquí ligada al tipo de POI (una `mazmorra_antigua` parte con más suciedad base que un `mercado_itinerante`). El estado de conservación también puede implicar **daño estructural real** (un boquete en la pared, techo hundido en una zona), no solo suciedad ambiental.
5. **Iluminación** (datos, no cálculo) — cada ventana/vela/antorcha es una fuente de luz de datos (posición) para que el motor en vivo calcule sombras — el bakeador nunca calcula el efecto en sí, igual que con clima/sombras en exteriores. Ver sección 7bis para el detalle de qué dato exacto deja cada ventana y cómo se separa de la luz de las fuentes interiores.

## 5. Tipos de sala (como los biomas)

Cada tipo de sala (dormitorio, cocina, sala_comun, almacén, biblioteca, taller, bodega, pasillo, gran_salón, sala_comercio...) declara su propia lista de elementos válidos y densidades — mismo mecanismo que biomas→especies. **Los pasillos son un tipo de sala propio**, no solo habitaciones con función: dan sensación de casa real en vez de habitaciones pegadas sin transición, con ancho variable y decoración mínima (alfombra corrida, cuadros).

- **`categoria` y `nombre` describen la función pretendida de la sala, nunca el mobiliario requerido**: `{ id, categoria, nombre }` (ej. `cocina` → `utilidad`/`"Cocina"`) es exactamente el schema mínimo pedido — añadir un tipo de sala nuevo es una entrada más en `tipos_sala.json`, no un cambio de motor. `tipos_sala.json` no lista `categoria` como una taxonomía cerrada de antemano: hoy cubre `residencial`, `almacenamiento`, `artesania`, `religioso`, `civico`, `comercio`, `circulacion`, `ocio`, `utilidad`, y se amplía igual que el resto del catálogo si hace falta una nueva. Una `dormitorio` sin ningún elemento colocado (`amueblado: "vacio"`) **sigue siendo un dormitorio** — el tipo de sala nunca depende de qué haya dentro (ver sección 8bis).

- **Familias de sala por tamaño/configuración, no un único tipo genérico con un rango de tiles amplio**: `dormitorio` (genérico, se mantiene) convive con `dormitorio_individual` / `dormitorio_doble` / `dormitorio_comunal` / `dormitorio_con_bano` / `dormitorio_enorme`, y lo mismo con `comedor_pequeno` / `comedor_mediano` / `comedor_grande` / `cocina_comedor` (cocina y comedor en planta abierta, sin dividir en dos salas). No es solo cuestión de tamaño — cada variante implica mobiliario distinto (una `dormitorio_comunal` lleva literas, una `dormitorio_doble` una cama de matrimonio) y a veces riqueza mínima distinta (`dormitorio_con_bano`/`dormitorio_enorme` son noble). Esto le da a cada `tipoEdificio` control fino real: una taberna pide una mezcla de `dormitorio_individual`+`dormitorio_comunal` en sus habitaciones de huéspedes, un castillo mezcla `dormitorio_enorme` (alcoba del señor) + `dormitorio_doble` (nobles) + `dormitorio_comunal` (guarnición/servidumbre) en la misma planta — algo que un único tipo `dormitorio` con rango de tamaño no puede expresar. Mismo patrón aplicable a cualquier otra sala que lo necesite (`sala_comun`, `almacen`...) cuando haga falta, no es exclusivo de estas dos familias.

## 6. Tipos de edificio

Cada `tipoEdificio` (casa_humilde, casa_noble, taberna, tienda, castillo, choza_pescador...) declara: rango de plantas típico, qué tipos de sala le tocan por planta y con qué peso, riqueza típica, materiales por defecto. Se puede enganchar directamente a un POI del exterior (`tienda_cazador` → tipoEdificio pequeño y modesto; `castillo_en_ruinas` → tipoEdificio noble con daño estructural) sin inventar un sistema nuevo — o pedirse a mano vía config, igual que hoy se puede lanzar el bakeador de exteriores sin pasar por la GUI.
- **Cobertura amplia, estilo Skyrim/Stardew Valley/WoW**: vivienda (varios niveles de riqueza), oficios (herrería, molino, panadería, sastre...), negocios/hostelería (tienda, taberna, posada), gobierno/comunidad (ayuntamiento, casa de gremio, cuartel), servicios públicos y cultura (biblioteca pública, museo, baños públicos, templo) — ver `interiores/README.md` para el listado completo. El catálogo de tipos de edificio crece igual que el de elementos: se sigue añadiendo según haga falta, no hay un tope pensado.
- **`poiVinculado` es opcional, no obligatorio** — muchos `tipoEdificio` de pueblo/ciudad (herrería, ayuntamiento, casa de gremio, biblioteca pública...) no tienen ni necesitan un POI 1:1 en el exterior total. Un asentamiento (`aldea_agricola`, `ciudad_poblada_menor`) es en realidad su propia instancia navegable con varios edificios a la vez, cada uno con su propia puerta — un tercer tipo de mapa, intermedio entre el exterior total y los interiores. Diseño capturado por separado en `GDD_Bakeador_POIs.md` (esqueleto, sin motor todavía) para no mezclar dos escalas distintas en este documento.

## 7. Conectores verticales y aberturas

- **Escaleras**: recta, caracol, vertical/escala de mano — cada una con huella y si permite pasar mobiliario grande (una caracol no deja subir un armario).
- **Trampillas**: arriba y abajo, mismo hueco conceptual que una puerta pero en el plano techo/suelo.
- **Puertas**: individual y doble. La puerta principal (la que conecta con el POI del exterior) es visual/estructuralmente distinta de las interiores — marco más trabajado, tamaño mayor (`esPuertaPrincipal`).
- **Conducto de chimenea**: si se coloca un hogar en la planta N, esa misma casilla (x,y) se reserva como conducto — no transitable, sin mobiliario — en todas las plantas por encima, hasta la última planta del edificio. Mismo tipo de restricción vertical que una escalera (posición fija repetida entre plantas consecutivas), pero el conducto no da paso, solo ocupa espacio. El exterior no cambia nada por esto — sigue sin dibujar tejados ni chimeneas, es 100% interno al bakeador de interiores.
- **Ventanas**: solo dejan pasar luz — nunca renderizan vista al exterior (el interior es instancia separada, no tiene sentido "ver" el mapa exterior desde dentro). Solo pueden aparecer en plantas con fachada al exterior — sótanos/bodegas nunca llevan ventana (regla de sitio por nivel de planta, igual que `bandaElevacionMin` en exteriores).
- **Ventanas y puertas como combinación de atributos, no variantes fijas**: para cubrir tamaño × altura × marco × forma × cristal sin tener que pintar cientos de combinaciones a mano, cada ventana compone varios ejes independientes (`forma`: rectangular/redonda/arco: `tamano`: pequeña/media/grande; `marco`: con/sin; `cristal`: liso/vidriera) en vez de un único campo `variantes` como en exteriores. El arte se compone por capas superpuestas (marco + hueco + cristal).
- **Balcón/galería interior de doble altura** — un piso superior con hueco que mira a la sala de abajo, típico de gran salón de castillo. Siempre interior, nunca un balcón que mire al mapa exterior.

## 7bis. Iluminación: dato de "aporte de luz" vs. cálculo en vivo por hora

Misma separación dato/cálculo que el resto del proyecto (clima y sombras en exteriores, cono de visión en `Backlog_Mecanicas_Futuras.md`) — el bakeador nunca calcula cuánta luz hay en una sala en un momento dado, solo dos tipos de dato que el motor en vivo necesita para hacerlo:

- **Luz ambiente (exterior colándose por ventana)**: cada instancia de ventana ya colocada lleva un campo `aporteLuz` numérico, resuelto por el bakeador a partir de sus atributos combinatorios (`tamano.anchoTiles` × factor por `altaEnPared` × factor por `cristal` — normalmente 1, pero `cristal.esmerilado` y `tamano.tronera` declaran su propio `aporteLuz` explícito en `ventanas.json` porque su geometría no sigue la fórmula simple). Una sala sin ninguna ventana no lleva este dato — luz ambiente siempre 0 para ella, nunca se calcula "a través de la pared" ni de una sala vecina. **Sin orientación** — coherente con la sección 10 (nada de orientación solar): da igual a qué lado del edificio mire la ventana, solo cuenta su tamaño/tipo. El motor en vivo suma el `aporteLuz` de las ventanas de la sala y lo multiplica por una curva día/noche global (día: variable según hora; noche: 0 o un valor bajo fijo si hay luna) — esa curva y el resultado final NO son responsabilidad del bakeador, se explica en el backlog.
  - **HECHO (2026-08-30)**: las ventanas ya se instancian de verdad (`interiores/src/colocarElementos.js::colocarSala`, parámetro `permiteVentanas`) — hasta este momento `ventanas.json` era combinatoria de catálogo sin consumidor real, así que `aporteLuz` era 0 siempre en la práctica. Solo en el muro **NORTE** de la sala (el único lado que el layout de fila de `edificio.js` garantiza sin puerta ni sala vecina — ver `docs/GDD_Sistema_Puertas.md` "Ventanas reales, cono de visión y luz ambiente" para el detalle completo, incluida la fórmula final de `aporteLuz` y el cálculo en vivo de `client/src/render3d/luzInteriores.ts`). Cada sala trae ahora `resultado.ventanas: [{x, lado:"norte", ancho, forma, tamano, marco, cristal, aporteLuz, colorDebug}]`.
- **Luces interiores** (vela, antorcha, candelabro, lámpara — capa `iluminacion` de `elementos.json`): cada una es una fuente de luz de datos con posición fija, independiente de la hora exterior — normalmente se consideran encendidas de forma constante (nada de simular quién las enciende/apaga, eso sería una mecánica de juego aparte, no bakeador). **Render en vivo**: `client/src/render3d/interiorVisual.ts` añade un `THREE.PointLight` cálido por cada pieza `capa:"iluminacion"` colocada — antes se pintaban como un mueble más sin luz de verdad, así que un interior con paredes quedaba a oscuras de noche (la luz ambiente/sol exterior del ciclo día/noche no entra por las paredes). Sigue siendo probabilístico por `LIMITE_POR_CAPA_POR_RIQUEZA` (una sala humilde puede no tocarle ninguna, a propósito — más vacía y más oscura que una noble).

## 7ter. Reglas de colocación por plano — pared/suelo/techo, para que el futuro menú de construcción sea intuitivo

Hasta ahora `colocacion` en `elementos.json` era una etiqueta suelta ("pegadaAPared", "colgadoEnPared"...). Aquí se formaliza qué implica cada valor en términos de plano, colisión y requisito — la misma tabla que tendrá que consultar el motor de generación (para no colocar nada ilegal) y, más adelante, el menú de construcción en vivo (para saber qué puede hacer el jugador y dónde). Es la razón de ser de esta sección: preparar el dato ahora para que esa mecánica futura no tenga que inventarse sus propias reglas de cero, y quede coherente con lo que generó el bakeador.

| `colocacion` | Plano | ¿Bloquea movimiento? | ¿Bloquea visión? | Requisito para colocarse |
|---|---|---|---|---|
| `colgadoEnPared` | Pared | No | No | Un segmento de pared **en blanco** en esa posición — nunca sobre un hueco de puerta/ventana. Es el único valor "de pared": ningún elemento sin este tag puede ir sobre el plano pared. |
| `pegadaAPared` | Suelo, tocando una pared | Sí, según su `huella` (igual que cualquier decorMovible/decorFija con footprint) | No | Casilla de suelo libre adyacente a un segmento de pared (puede ser una pared con ventana encima, no hace falta que esté en blanco — solo el propio hueco de la puerta queda excluido). |
| `centroSala` / `libre` / `esquina` | Suelo | Sí, según `huella` | No | Igual que `pegadaAPared` pero sin requisito de estar junto a una pared — cada tag matiza la posición preferida dentro de la sala, no cambia la colisión. |
| `juntoAMesa` | Suelo, pegado a un ancla | Sí, según `huella` | No | Requiere una casilla libre del anillo justo alrededor de la huella de un elemento con `esSuperficie: true` ya colocado en la sala (mesa, mostrador, altar...) — nunca un punto suelto de la sala. Si hay varias anclas, se reparte entre las que menos satélites tengan todavía. Es lo que hace que "sillas alrededor de la mesa" salga de verdad como un conjunto, no piezas sueltas puestas al azar (ver sección 3ter). |
| `simetrico` (como único valor de `colocacion`, sin combinar con otro) | Suelo | Sí, según `huella` | No | En una sala con `simetrico: true` (`tipos_sala.json`), se coloca por parejas en espejo respecto al eje central de la sala (columnas a ambos lados, etc.) — nunca una sola instancia suelta descentrada. Combinado con `centroSala` (ej. `trono`) el elemento es único por definición — ahí `simetrico` solo significa "respeta el eje", no "duplícate". |
| `sobreSuperficie` | Suelo, pero encima de otro elemento | No (ya la bloquea el elemento anfitrión) | No | Requiere un elemento con `esSuperficie: true` ya colocado en esa misma casilla (mesa, mostrador, atril, altar — sección 3bis de `elementos.json`). Sin anfitrión no es una posición válida. |
| `techo` | Techo | No | No | Ninguno especial — cualquier casilla de la sala (viga vista, bóveda, artesonado, araña de luces). |
| `suelo` (capa suciedad) | Suelo, decal | No | No | Ninguno — es puramente cosmético, se puede pisar/superponer con cualquier otra cosa. |

Reglas estructurales que se derivan de la misma tabla, ya establecidas en otras secciones pero explicitadas aquí como regla formal:

- **Las paredes bloquean movimiento y visión** — no se puede atravesar ni ver a través de una pared. Coherente con la maqueta 2.5D validada al principio de esta conversación (oclusión exacta a la silueta del hueco de la puerta, sección 11/`Backlog_Mecanicas_Futuras.md`).
- **Puertas y ventanas son huecos en la pared, no la pared misma** — una puerta abierta no bloquea movimiento; ni puerta ni ventana bloquean visión en su propia silueta (la puerta porque literalmente no hay pared ahí; la ventana porque dejar pasar la vista sería lo mismo que "ver el exterior", explícitamente descartado en la sección 10 — así que una ventana bloquea visión igual que una pared, solo deja pasar el dato de luz de la sección 7bis). Ninguna de las dos admite un elemento `colgadoEnPared` en su misma posición.
- **Solo `decorMovible` desaparece bajo `amueblado: "vacio"`/`"fijo"`** (sección 1) — las reglas de esta tabla no cambian con el nivel de amueblado, solo cambia qué subconjunto de elementos coloca el bakeador de antemano. Los huecos que quedan vacíos son exactamente las posiciones legales (según esta misma tabla) que el jugador podrá rellenar desde el menú de construcción — mismo patrón dato-ahora/mecánica-después que iluminación (7bis) y el cono de visión.

## 7quater. Orientación (rotación) y tile de interacción

- **La huella no es una caja homogénea** — un mueble con footprint 1×2 (una cama, un banco de trabajo) tiene un lado que da a la pared y otro por el que se usa; tratarlo como un bloque uniforme pierde esa distinción. Cada elemento puede declarar `tileInteraccion` como un offset `[dx, dy]` relativo a la esquina de origen de su huella en orientación 0° — ej. una cama 1×2 (`cama_individual`) declara `[0, 1]`: se usa desde el tile de los pies, no desde el que toca la pared.
- **0°/90°/180°/270°** (`interiores/src/rotacion.js`, `ORIENTACIONES`): `rotarHuella` intercambia ancho/largo a 90°/270°; `rotarOffset` rota el `tileInteraccion` sobre la misma esquina de referencia que la huella, así que huella y punto de interacción siempre giran juntos, nunca uno sin el otro. `colocarElementos.js` prueba una orientación por intento de colocación en suelo (no una fija por elemento) y guarda la orientación elegida (`rotacion`) y el `tileInteraccion` ya resuelto a coordenadas absolutas de sala en el resultado.
- Los elementos sin `tileInteraccion` (la mayoría de decoración) simplemente no llevan ese campo en el resultado — no es obligatorio, solo se resuelve para piezas realmente interactivas (camas, mesas de trabajo, horno, yunque...).

## 8. Suelos

- **Material por nivel de planta**: tendencia piedra en sótanos/plantas bajas, madera en plantas altas — mismo patrón que el desnivel en exteriores varía el terreno, aquí la "banda" es el número de planta en vez de la elevación continua.
- **Mosaicos/patrones de suelo** como capa aparte del material base — un patrón que se estampa encima (suelo ajedrezado en un salón noble, por ejemplo), no mezclado con el material en sí.
- **Pared y suelo como paletas de material independientes por sala** — una cocina puede tener suelo de piedra y pared de estuco; un dormitorio, suelo de madera y pared con papel.

## 9. Otros elementos de detalle confirmados

- **Chimeneas/hogares**, con el conducto vertical de la sección 7.
- **Techo con estilo estructural**, no solo material: viga vista, bóveda de piedra, artesonado — coherente con lo noble/humilde del edificio.
- **Columnas** en salas grandes (salón del trono, gran comedor).
- **Tapices/cuadros de pared** como decoración fija de pared, no solo de suelo.
- **Riqueza/nivel socioeconómico del edificio** como parámetro que filtra qué materiales/mobiliario están disponibles.
- **Simetría en salas nobles vs. asimetría orgánica en salas comunes** — regla de estilo de colocación por tipo de sala.
- **Estado del propio mueble** (intacto/roto/volcado), no solo suciedad ambiental.
- **Camas de varios tamaños** (individual/doble/litera) según riqueza y tipo de sala.
- **Muebles marcados como contenedor sí/no** — dato reservado para la futura mecánica de inventario, mismo patrón "reservado, no implementado todavía" que `tierra_labrada` en exteriores.

## 9bis. Funcionalidad y estadísticas de sala (aportes aditivos)

- **Cada elemento puede declarar `aportes: { estadistica: valor }`** en `elementos.json` — ej. `cama_individual` aporta `{ funcionalidad: 40, confort: 15 }`, `armario` aporta `{ almacenamiento: 30 }`. `interiores/src/estadisticas.js` (`calcularEstadisticas`) suma los aportes de todo lo realmente colocado en la sala (suelo, pared, techo o encima de una superficie) y el resultado se adjunta a la salida de `colocarSala` como `estadisticas`.
- **Totalmente dirigido por datos**: añadir una estadística nueva, o cambiar cuánto aporta una pieza, es editar `elementos.json` — no tocar `estadisticas.js` ni `colocarElementos.js`. Hoy el catálogo usa `funcionalidad`, `confort`, `almacenamiento` y `decoracion` en unas 39 piezas representativas (camas, mesas/mostradores/altar/trono, contenedores, mobiliario de oficio, chimenea, decoración de pared, asientos) — el resto de elementos simplemente no lleva `aportes` y no participa en la suma.
- **Ejemplo real**: un dormitorio vacío da `estadisticas: {}`; con una `cama_individual` pasa a `{ funcionalidad: 40, confort: 15 }`; si además lleva `armario`, suma `almacenamiento: 30`. La suma es puramente aditiva, sin techos ni combinaciones especiales entre piezas.
- **Nunca decide si la sala es válida** — coherente con la sección 5: una sala sin ningún elemento con `aportes` (o sin ningún elemento en absoluto, `amueblado: "vacio"`) sigue siendo un tipo de sala legítimo, solo que con `estadisticas: {}`. El futuro uso en vivo de estas estadísticas (umbrales de confort para NPCs, requisitos de funcionalidad para tareas...) es mecánica de servidor, no de este bakeador.

## 9ter. Edificios multi-planta y editor manual (generar + editar, no solo generar)

Un interior deja de ser una única habitación aislada: `interiores/src/edificio.js` compone varias `colocarSala` en `Edificio → Planta → Sala`, y `interiores/src/edicion.js` + `interiores/gui/` dejan tocar cualquier cosa después sin que una regeneración la destruya. Ninguna de las dos piezas reescribe `colocarSala`/`salas.js` — los llaman y componen el resultado.

**Composición de planta** (`edificio.js`, `generarPlanta`): `tipos_edificio.json` ya traía `salasPorPlanta` como listas ponderadas por planta (`[[tipoSala, peso], ...]`) — perfecto para sorteo con variación real (misma semilla = mismo edificio, otra semilla = otra mezcla de salas/tamaños, sección 12 del pedido de edificios). El número de salas por planta sale de un rango según riqueza (2-5), no un valor fijo. Layout con una sola estrategia fiable, no una colección de heurísticas:
- **El muro no ocupa casilla propia** (`colocarSala`): `ancho x largo` es el suelo caminable real, de borde a borde — el muro es un límite sin grosor justo en ese canto, no una fila/columna reservada. La puerta, al ser el hueco EN ese límite, cae una fila más allá del propio rectángulo (`y = largo`), no sobre una casilla de suelo. Por eso dos salas contiguas en la composición de planta necesitan un hueco de separación explícito de 1 casilla entre ellas — ya no hay un muro propio de cada una que puedan compartir/solapar.
- **Con pasillo** (`tipos_sala.pasillo`, `esPasillo:true`): las demás salas de la planta se ponen en fila justo encima, alineadas por el muro sur, con una fila de separación real hasta el pasillo de debajo; la puerta sur que cada sala YA trae de fábrica (justo en esa fila) es la que conecta con el suelo del pasillo — se reutiliza la puerta real, no se inventa una segunda.
- **Sin pasillo**: las salas se ponen en fila con una columna de separación entre cada dos, y ahí se punza una puerta nueva — el único caso donde de verdad hace falta inventar una puerta que `colocarSala` no trajera ya.
- Cada sala pintada sobre la rejilla de planta se re-detecta con la misma `detectarSalas` de la sección 2bis: los huecos de puerta compartidos aparecen automáticamente en el conjunto `puertas` de las DOS salas vecinas — esa coincidencia **es** la conexión, no hace falta un grafo de adyacencia aparte.
- **Habitación no rectangular** (`generarHabitacionCompuestaL`, sección 3 del pedido): dos salas rectangulares reales unidas por una abertura ANCHA (varias celdas de puerta seguidas en el muro compartido, no solo una) — se lee como un espacio en forma de L sin tocar el motor de colocación de una sala (que sigue siendo rectangular, sección 2).
- **Plantas sin continuidad XY real** — mismo modelo que ya declaraba la sección 7 ("pila de plantas independientes conectadas por huecos de escalera"): cada edificio guarda solo en qué sala/planta cae cada conector vertical (`conectoresVerticales`, de `conectores.json`), no una coordenada 3D continua.

**Edición no destructiva** (`edicion.js`, sección 4/5 del pedido): cada pieza que coloca el generador lleva `instanceId` único y `origen: "generado"` (colocarElementos.js las marca al final de `colocarSala`). Operaciones sobre una pieza por su `instanceId`: `moverElemento`, `rotarElemento`, `eliminarElemento`, `anadirElemento`, `duplicarElemento`, `sustituirElemento` — todas ponen `origen: "modificado"` en lo que tocan. `cambiarTipoSala` hace lo mismo a nivel de sala. **Solo salirse de los límites de la sala bloquea de verdad** (`fuera_de_limites`, dato inválido); solapamiento y bloqueo de puerta son avisos (`solapa_con_otro_mueble`, `bloquea_la_puerta`) que se pueden confirmar con `forzar:true` — el diseñador tiene libertad para distribuciones poco convencionales, coherente con "no ser demasiado restrictivo" del pedido. La suciedad (capa `suciedad`) nunca avisa de solape, coherente con la sección 7ter ("se puede pisar/superponer con cualquier otra cosa").

**Regeneración parcial que respeta lo editado** (`regenerarMobiliario`/`regenerarHabitacion`/`regenerarPiso`/`regenerarEdificio`, sección 6 del pedido): regenerar vuelve a llamar a `colocarSala` desde cero, pero antes aparta todas las piezas ya marcadas `modificado`, y después descarta cualquier pieza recién generada que caería encima de una de ellas — así una regeneración nunca borra una edición manual sin pedirlo explícitamente (`forzar:true`). Si la SALA en sí fue editada a mano (tipo cambiado con `cambiarTipoSala`), `regenerarHabitacion` se niega a tocarla salvo `forzar:true` — regenerar el edificio entero nunca destruye a ciegas una habitación que el usuario ya afinó. Verificado con un edificio real: cambiar `biblioteca`→`almacen` a mano y luego regenerar la planta Y el edificio completo deja la sala como `Almacén (modificado)` intacta.

**Editor web** (`interiores/gui/`, mismo patrón que `baker/gui/`: servidor http plano sin framework + página estática, sección 4/8/10 del pedido): árbol Edificio→Planta→Sala en el panel izquierdo (niveles de edición de la sección 10), vista isométrica 2.5D interactiva en el centro (misma proyección de `prueba_render_iso.js`, ahora con cada mueble como objetivo de clic real), panel derecho con info de la sala (tipo/categoría/piso/tamaño/riqueza/nº muebles/estadísticas, sección 8) y del mueble seleccionado (mover con flechas, rotar con R/botón, eliminar, duplicar, sustituir por otro id del catálogo) más una paleta para añadir cualquier pieza del catálogo haciendo clic en una casilla libre. Botones de generación parcial (mobiliario de la sala / planta / edificio, sección 6) con una casilla `forzar` explícita — la generación nunca es un resultado final bloqueado (sección 4). "Guardar" escribe el edificio actual (generado + modificado mezclado) a `interiores/output/<id>.json`.

## 9quater. Catálogo de Contenido: fuente de verdad unificada

`interiores/src/catalogoContenido.js` es la capa de consulta unificada sobre TODO lo que puede aparecer en una sala — mobiliario, decoración, iluminación, objetos sueltos, puertas y ventanas. No es un JSON nuevo ni duplica los que ya existen: normaliza en memoria `catalogo/elementos.json` (+ `puertas.json` + las combinaciones de `ventanas.json`) a un modelo más rico, derivando categoría/subcategoría/tags/reglas de los campos que ya estaban (`capa`, `colocacion`, `huella`, `materialesCompatibles`, `tiposSalaValidos`...). Un catálogo nuevo se sigue escribiendo con el formato de siempre — la normalización rellena huecos con valores por defecto, nunca exige campos nuevos.

**Catálogo vs. instancia** (sección 6 del pedido): un ITEM del catálogo (`{id, nombre, categoria, subcategoria, dimensiones, huella, rotacionesPermitidas, reglas, salasPermitidas, tags, pesoGeneracion, variantes, ...}`) describe qué es una pieza y qué reglas sigue; una INSTANCIA (lo que vive en `resultado.colocados`/`colgados`/`techo` de una sala) es esa pieza colocada de verdad, con `instanceId` único, `x`/`y`, `rotacion`, `origen` (`"generado"`/`"modificado"` — el mismo campo que ya usaba `edicion.js`, no uno nuevo paralelo), `estado` (`{desgastado, roto, sucio}`, preparado desde ya aunque todavía no haya mecánica que lo altere sola) y, si el catálogo declara variantes con nombre, `variante`. El catálogo nunca se muta al colocar algo — solo se lee.

**Categorías** (derivadas, no una lista aparte que haya que mantener sincronizada): `mobiliario`, `decoracion`, `iluminacion`, `suciedad`, `objetos` (piezas `sobreSuperficie`, el catálogo estilo Project Zomboid de la sección 3bis), `puertas`, `ventanas`. **Subcategoría** agrupa más fino dentro de una categoría (`camas`, `asientos`, `mesas_superficies`, `almacenamiento`, `oficio`, `luces`...) por patrón de id, igual de curado que el resto del catálogo.

**Tags**: cada item lleva un array de tags de función (`asiento`, `superficie`, `almacenamiento`, `cocina`, `dormitorio`, `oficina`, `religioso`, `iluminacion`, `decorativo`), material (`madera`, `piedra`, `metal`...) y rareza (`comun`/`modesta`/`raro` según `riquezaMinima`) — derivados por patrón de id y por los mismos `materialesCompatibles`/`tiposSalaValidos` que ya tenía cada pieza. `buscarPorTag(tag)` acepta un tag o un array (AND: deben cumplirse todos).

**Reglas de colocación** (sección 8 del pedido — el catálogo solo describe, `edicion.js` decide): `reglasParaElemento(el)` calcula `puedeSolapar`, `puedeBloquearPuerta`, `pegadoPared`, `requierePared` y `rotacionesPermitidas`. Los valores por defecto reproducen EXACTAMENTE el comportamiento que el motor ya tenía antes de este catálogo (solo la suciedad podía solaparse; nada bloqueaba puerta "de más"; las 4 orientaciones siempre abiertas) — así que ningún elemento existente cambia de comportamiento salvo que declare un override explícito. `edicion.js`'s `validarHueco` sigue siendo quien decide bloqueo duro (fuera de límites) vs. aviso forzable (solapa/bloquea puerta/rotación no habitual) — el catálogo nunca convierte un aviso en bloqueo por su cuenta.

**Variantes con peso, selección determinista** (sección 4 del pedido): un item puede declarar `variantesNombradas: [{id, peso}, ...]` (opcional — el `variantes: N` simple de siempre se conserva sin tocar para el resto del catálogo). `elegirVariante(id, semilla)` usa el mismo PRNG mulberry32 que el resto del bakeador (`azar.js`, extraído de `colocarElementos.js`/`edificio.js` a un módulo hoja para evitar un require circular con este catálogo) — misma semilla, mismo resultado, siempre. `colocarSala` resuelve la variante de cada pieza colocada con la semilla de esa instancia (`item.instanceId`) en el mismo paso donde asigna `instanceId`/`estado`, así que **catálogo → selección → generación → instancia** ya es un camino real y determinista de punta a punta, no solo un mecanismo preparado sin usar — demostrado en `mesa_comedor`/`silla`/`armario` con 3 variantes nombradas cada uno (madera/madera/desgastada-o-rota), y verificado por test que la distribución real sobre miles de semillas se acerca a los pesos declarados.

**`resolverNecesidades(necesidades, opts)`**: capa de abstracción ADICIONAL para pedir contenido por función en vez de por id exacto — `{ asiento: {min:4, max:8} }` devuelve una lista de ids de catálogo con esos tags, determinista por semilla. No sustituye la generación real de `colocarElementos.js` (que sigue resolviendo capa/colocacion/riqueza exactamente igual que siempre — "no sustituir de golpe la lógica existente si implica riesgo"); es una pieza nueva, probada y documentada, lista para que un futuro paso de generación o el propio editor la usen cuando haga falta pedir "necesidades" en vez de piezas concretas.

**`validar()`**: falla claro ante referencias rotas — id duplicado, `salasPermitidas` apuntando a un tipoSala inexistente, `materialesCompatibles` a un material inexistente, variante sin id o duplicada. Se ejecuta como parte de `interiores/test/catalogo.test.js`.

**Editor web** (`interiores/gui/`, sección 10 del pedido): `GET /api/catalogo` (con `?categoria=`, `?subcategoria=`, `?tag=`, `?q=`, `?sala=`) sirve el catálogo normalizado; la paleta de "añadir mueble" dejó de ser una lista plana por capa y ahora navega por categoría, busca por texto y filtra por tag, mostrando nombre/dimensiones/tags/reglas básicas de cada pieza. Puertas/ventanas aparecen como categorías navegables/buscables (son parte del catálogo, sección 1 del pedido) pero se marcan `estructural: true` y quedan deshabilitadas para "colocar como instancia" — son parte del hueco de la pared (sección 7 del GDD), no del array de mobiliario que edita `edicion.js`; forzar esa integración ahora habría significado tocar el modelo de paredes/huecos sin necesidad real, así que queda documentado como límite explícito, no como promesa incumplida.

## 9quinquies. RoomTags, FurnitureConfig y pipeline de colocación por fases

Capa de arquitectura de datos ADITIVA sobre el motor de siempre — `colocacion`/`tiposSalaValidos`/`huella` de cada elemento y `categoria` de cada tipo de sala siguen existiendo sin cambios, nada de esto los sustituye. El objetivo es que una pieza nueva se pueda describir por FUNCIÓN ("vale para cualquier COMUN_VIVIENDA, es Dominante, se pega a la pared") en vez de listar cada tipo de sala concreto a mano.

**RoomTags** (`interiores/src/roomTags.js`): diccionario de 19 tags — 9 categorías comunes (`COMUN_VIVIENDA`, `COMUN_COMEDOR`, `COMUN_COCINA`, `COMUN_ALMACEN`, `COMUN_BAÑO`, `COMUN_TALLER`, `COMUN_TIENDA`, `COMUN_MILITAR`, `COMUN_EXTERIOR_CUBIERTO`) y 10 no comunes/especializadas (`NOCOMUN_BIBLIOTECA`, `NOCOMUN_IGLESIA_RELIGION`, `NOCOMUN_SERVICIO_PUBLICO`, `NOCOMUN_OFICIAL_GOBIERNO`, `NOCOMUN_GRANJA_ANIMALES`, `NOCOMUN_ALQUIMIA_MAGIA`, `NOCOMUN_MAZMORRA_TORTURA`, `NOCOMUN_CRIPTA_CEMENTERIO`, `NOCOMUN_MINERIA`, `NOCOMUN_PORTUARIO`), más 3 modificadores especiales (`ESPECIAL_NOBLEZA`, `ESPECIAL_ENEMIGO_SALVAJE`, `ESPECIAL_TESORO`). Cada tipo de sala en `tipos_sala.json` declara qué `tags` lleva (algunas salas sin encaje claro en la taxonomía dada — `pasillo`, por no tener tag de "circulación" — se dejan con array vacío en vez de forzar un tag que no pega).

**FurnitureConfig**: cada una de las 118 entradas de `elementos.json` lleva, ADEMÁS de sus campos de siempre:
- `allowedRoomTags` — derivado automáticamente como la unión de los `tags` de cada sala en `tiposSalaValidos` (no hay que mantenerlo sincronizado a mano, se recalculó con un script de migración una única vez a partir del catálogo existente).
- `specialModifier?` — `ESPECIAL_TESORO` para lo explícitamente valioso (`baul_tesoro`, `reliquia`, `moneda_suelta`...), `ESPECIAL_NOBLEZA` para el resto de piezas `riquezaMinima:"noble"`.
- `isMandatory?` — curado a mano: la pieza que DEFINE la sala (`cama_doble` en un dormitorio, `mesa_comedor` en un comedor, `altar` en una capilla, `yunque`/`fragua` en un taller...) — no es derivable de un único campo existente, hacía falta criterio.
- `priority` (1/2/3) — derivado 1:1 de `capa` (`decorFija`→1 Dominante, `decorMovible`→2 Secundario, `iluminacion`/`suciedad`→3 Decoración).
- `anchorType` (`WALL_BACK`/`CORNER`/`WALL_HIGH_FLOATING`/`FREE_CENTER`/`CHILD_SLOT`) — derivado del primer valor de `colocacion` que ya tenía la pieza, en el MISMO orden de preferencia que `colocarUno()` ya comprobaba antes de este cambio (así una pieza con varios valores a la vez, ej. `banco:["juntoAMesa","pegadaAPared"]`, deriva al mismo comportamiento que ya tenía, no a uno nuevo).
- `dimensions` — copia de `huella` en formato `{x,y}`.
- `clearanceFrontal` — 1 para piezas de uso/interacción (`esSuperficie:true` o con `tileInteraccion`), 0 para el resto.
- `allowsLowFurnitureBelow?` — `true` para lo `colgadoEnPared`; es descriptivo, no necesita lógica nueva porque lo colgado en pared YA no competía por la casilla de suelo con nada (nunca llamaba a `ocupar()` sobre `libreSuelo`).
- `childSlots?` — solo en las 8 anclas de asiento (`mesa_comedor`, `mesa_cocina`, `mesa_consejo`, `mesa_alquimia`, `escritorio`, `mesa_juegos`, `mostrador`, `mesa_trabajo`): 4 slots (norte/sur/este/oeste) con `allowedItemTypes` (`silla`/`banco`) y `autoRotateToParentCenter:true`. Anclas de superficie que NO son de asiento (`encimera`, `balda`, `atril`, `altar`) no llevan `childSlots` a propósito — antes cualquier `esSuperficie` aceptaba cualquier satélite `juntoAMesa` sin distinción (una silla podía "sentarse" junto a un altar); ahora `anclaAdmite()` en `colocarElementos.js` exige que el ancla tenga un `childSlot` cuyo `allowedItemTypes` incluya la pieza — sin coincidencia, cae al hueco genérico (`intentarColocarEnSuelo`) en vez de enganchar donde no toca.

**Pipeline de colocación por fases** (`colocarElementos.js`): el bucle principal ahora itera `[1, 2, 3]` (Dominante → Secundario → Decoración) en vez de la lista de capas de amueblado directamente — cada fase sigue agrupando la(s) misma(s) capa(s) de siempre (`CAPAS_POR_FASE`) con su mismo límite (`LIMITE_POR_CAPA`), así que el reordenamiento no cambia CUÁNTO sale de cada capa, solo lo hace explícito como 3 fases con nombre. Dentro de cada capa, `isMandatory` ordena primero (antes solo ordenaban las anclas `esSuperficie` antes que sus satélites) — la pieza que define la sala se intenta colocar antes que el resto de esa capa. `colocarUno()` pasó de comprobar `colocacion.includes(...)` en cascada a un `switch` sobre `anchorType`, con el mismo desambiguado por `colocacion` dentro de cada caso donde un `anchorType` cubre más de un valor de siempre (`CHILD_SLOT` cubre `sobreSuperficie` y `juntoAMesa`; `WALL_HIGH_FLOATING` cubre `techo`, `colgadoEnPared` y el nuevo caso `pared` — texturas de suciedad de pared como `grietas_pared`/`moho`, que antes no tenían ningún tratamiento especial y caían al hueco genérico de suelo; ahora sí van a la pared, que es donde tiene sentido una grieta o un moho).

**`clearanceFrontal` real**: `frenteLibre()` (nueva, en `colocarElementos.js`) exige que las casillas justo delante de la pieza (según su orientación final — mismo criterio de "0° mira al sur" que ya usaba el resto del motor) estén libres en el momento de la colocación; no las reserva contra colocaciones futuras, coherente con que el resto del motor tampoco reserva nada por adelantado. Se comprueba en las tres vías de colocación en suelo (`intentarColocarEnSuelo`/`intentarPegadoAPared`/`intentarCentroSala`) antes de confirmar la casilla.

Probado: los 31 tests de regresión y los 11 del editor e2e con Playwright siguen en verde sin tocarlos — la migración del catálogo y el nuevo dispatch por `anchorType` reproducen EXACTAMENTE el comportamiento anterior salvo las dos mejoras deliberadas (`childSlots` restringe de verdad quién se sienta junto a qué, `pared` ya tiene tratamiento propio). Verificado además a mano que un altar ya no atrae sillas/bancos en anillo a su alrededor (capilla de prueba, semilla real) y con el render isométrico de siempre.

**Ampliación medieval + variantes de material reales (segunda pasada sobre esta misma arquitectura):**
- **4 RoomTags nuevos** (`COMUN_CIRCULACION`, `COMUN_OCIO`, `COMUN_AGRICULTURA`, `NOCOMUN_EDUCACION`) para huecos reales detectados al taguear las 39 salas la primera vez — `pasillo`/`vestíbulo` no tenían tag de circulación propio, `taberna`/`salón de baile`/`sala de juegos` quedaban forzados dentro de `COMUN_VIVIENDA`, `invernadero` solo llevaba `ESPECIAL_NOBLEZA` sin ningún tag funcional, y no había tag para una futura sala de escuela (`biblioteca`/`estudio` ya la llevan ahora).
- **Variantes de material reales, no solo un conteo**: el campo `variantes: N` de siempre nunca se llegó a usar de verdad para resolver una instancia concreta (`elegirVariante` solo mira `variantesNombradas` — antes solo 3 piezas del catálogo entero lo tenían: `mesa_comedor`/`silla`/`armario`). Se generaron `variantesNombradas` para las 78 piezas restantes con `materialesCompatibles` declarado, **tirando de las especies reales** de `baker/catalogo/vegetacion.json` (28 especies de madera) y `baker/catalogo/rocas.json` (15 de piedra) — exactamente lo que la nota de `especiesFuente` en `materiales.json` llevaba documentado desde el principio sin implementar. Metal/cuero/tela_tapiz/cristal/marmol/ladrillo/estuco/papel_pintado/mimbre/paja/adobe usan un acabado propio curado (`hierro`/`bronce`/`acero`/`cobre` para metal, etc., sin catálogo externo del que tirar). **Coherencia por material**: cada pieza solo recibe variantes de los materiales que YA tenía en `materialesCompatibles` — una silla (solo madera) nunca recibe una variante de metal, no hay combinaciones inventadas.
- **5 muebles nuevos de invernadero** (`mesa_semillero`, `semillero_bandeja`, `regadera`, `tijeras_podar`, `enrejado_trepador`) — `COMUN_AGRICULTURA` solo tenía 1 pieza (`bancada_cultivo`) antes de esto. `maceta`/`barril` (ya existentes) se extendieron para valer también en invernadero en vez de duplicarlos.
- **Limpieza**: `src/prueba_render.js` (versión 2D anterior, sin ningún uso propio, superada del todo por `prueba_render_iso.js`) eliminado. `src/roomTags.js` estaba definido pero nunca importado por el motor — ahora `colocarElementos.js` usa `AnchorType`/`Priority` de verdad en vez de literales de cadena/número sueltos. Export redundante `elegirPonderado` quitado de `edificio.js` (solo reenviaba el propio export de `azar.js`, sin nada propio).

Probado de nuevo tras esta pasada: 31/31 + 11/11 en verde, y un `invernadero` generado a mano pasó de tener prácticamente nada (`bancada_cultivo` suelta) a un conjunto coherente (`mesa_semillero`, `enrejado_trepador`, `bancada_cultivo`, `barril`, `maceta`) con variantes de especie real resueltas (`mesa_semillero_arce`, `barril_avellano`...).

## 9sexies. Vista de edificio completo (habitaciones conectadas + pisos)

**Actualización (pasada de revisión):** la vista de edificio completo pasó de plano 2D por plantas a **3D real girable** (`interiores/gui/vista3d.js`, Three.js servido en local desde el `node_modules` del propio repo — sin CDN ni dependencia nueva, es el mismo paquete que ya usa el cliente del juego): el edificio entero con las plantas apiladas por nivel (checkbox "separar plantas" para abrirlas en vertical y ver dentro), paredes de media altura casilla a casilla con sus huecos de puerta REALES (mismas coordenadas de `salaPlanta.puertas` que describía esta sección), cada mueble como caja con su `colorDebug` (incluidos objetos de superficie y colgados de pared en su lado), y el conector vertical marcado con un cono. Órbita/zoom con el ratón (controles propios mínimos) y clic en el suelo de una sala → salta a su vista de habitación. Los datos siguen siendo exactamente los mismos que ya viajaban en `/api/generar`/`/api/edificio` — cambió la presentación, no el modelo.

**Cargar edificios guardados** (el hueco que faltaba): `POST /api/guardar` existía pero no había forma de reabrir un guardado — cada guardado era un callejón sin salida. Ahora `GET /api/guardados` lista `output/*.json` y `POST /api/cargar` reconstruye el edificio en memoria (los `Set` de tiles/puertas/ventanas se re-crean desde los arrays del JSON — `deserializarEdificio`, el inverso exacto de la serialización de transporte), dejándolo indistinguible de uno recién generado: la edición y la regeneración (que respetan `origen: "modificado"`) funcionan igual sobre él. En el editor: desplegable de guardados + botón "📂 Cargar" en la cabecera. Verificado de punta a punta: generar castillo → guardar → generar OTRO edificio encima → cargar el castillo → regenerar mobiliario de una sala del edificio cargado, todo correcto.


Además de la vista sala por sala (isométrica, sección 9ter), el editor (`interiores/gui/`) tiene un segundo modo — botón "🏢 Vista edificio" en la cabecera — que muestra TODAS las plantas del edificio a la vez, cada una como un plano 2D en planta (top-down), apiladas verticalmente de arriba a abajo por `nivel` (planta más alta primero, bodega al final) para leer de un vistazo cómo queda el edificio completo, no solo habitación por habitación.

No hace falta ningún dato nuevo del servidor: `edificio.js`/`generarPlanta` ya calculaba todo lo necesario y ya viaja serializado en `/api/generar` y `/api/edificio` (sección 9ter) — esto es una vista nueva puramente de cliente sobre datos que ya existían:
- **Posición real de cada sala**: cada sala trae `offsetX`/`offsetY` dentro de la rejilla de su planta (`planta.ancho`/`planta.alto`) — el plano dibuja cada una como un rectángulo `resultado.ancho x resultado.largo` en ese offset exacto, coloreado por `tipoSalaId` (hash determinista → mismo color siempre para el mismo tipo de sala) y con su id como etiqueta.
- **Conexiones entre salas, sin grafo aparte**: `s.salaPlanta.puertas` (mismo campo que ya usa `detectarSalas`, sección 9ter) es el conjunto de coordenadas de puerta que tocan esa sala. Agrupando esas coordenadas por TODAS las salas de la planta, una coordenada que aparece en el `puertas` de dos salas distintas **es**, por definición, la conexión entre ambas (se pinta como marca sólida); una coordenada que solo aparece en una sala es una puerta hacia fuera de esa fila/planta (se pinta atenuada). No es una aproximación visual — es literalmente la misma estructura de datos que ya decide la conectividad real en el motor, releída desde el cliente.
- **Conector vertical de cada planta** (escalera/trampilla, `planta.salaConector`): se marca con un icono (`⬍`) sobre la sala que lo aloja. Como `salaConector` es una referencia directa a uno de los objetos de `planta.salas` en memoria del servidor, sus `Set` internos (`tiles`/`puertas`/`ventanas`) no sobreviven la serialización a JSON tal cual — el cliente lo relocaliza dentro de `planta.salas` (ya serializada correctamente) comparando `offsetX`/`offsetY`/`tipoSalaId`, que sí son valores planos.
- **Clic en una sala del plano** selecciona esa sala y vuelve automáticamente a la vista de habitación (isométrica) — la vista de edificio es un mapa de navegación, no sustituye la vista detallada; sigue existiendo "ver habitación por habitación" tal cual pedía el encargo, ahora con una forma rápida de saltar de una a otra guiándose por el plano completo.

Probado con Playwright sobre un `castillo` real (bodega + planta baja + hasta 3 plantas altas, el tipo de edificio con más plantas del catálogo): las 5 plantas se dibujan apiladas correctamente, las puertas compartidas entre salas contiguas aparecen marcadas en el sitio correcto (verificado además comparando contra un volcado directo de `salaPlanta.puertas` por planta, no solo a ojo), el conector vertical se marca en la sala que le toca, y el clic en una sala cambia de vista y selecciona esa sala en el árbol/panel igual que si se hubiera hecho desde el árbol lateral. Los 31 tests de regresión y los 11 del editor e2e siguen en verde — es una vista añadida, no toca `edificio.js`/`colocarElementos.js`/las rutas del servidor.

## 9septies. Generación procedural v2: coherencia de materiales/estado, layout 2D (BSP) con puertas múltiples reales, ventanas y sesgo de circulación

Cuatro mejoras del algoritmo de colocación pedidas explícitamente por el usuario tras comparar una propuesta externa (basada en algoritmos conocidos de generación de interiores: Merrell/Yu para layout de muebles por función de coste, BSP/WFC para forma de planta, dungeon generation para puertas) contra el motor real — investigación completa con fuentes en el hilo de la sesión. Ninguna sustituye el pipeline existente (fases Dominante→Secundario→Decoración, `anchorType`, `childSlots`, `clearanceFrontal`, flood-fill de circulación): todas son extensiones aditivas sobre catálogo/datos ya existentes, coherentes con "las listas crecen, el código no" y con el resto del motor (greedy determinista + backtracking local de 1 nivel, nunca optimización global tipo simulated annealing — se consideró y se descartó explícitamente por cambiar de paradigma sin necesidad demostrada).

**1. Coherencia de paleta de materiales (antes dato muerto)**: `materialesPreferidos` de `tipos_edificio.json` existía desde el principio pero `edificio.js` solo lo copiaba al resultado sin usarlo. Ahora es un **sesgo de probabilidad** (60%, nunca un filtro duro) sobre `materialSuelo`/`materialPared` de cada sala: un material del edificio solo entra como candidato si YA se usa para ese plano en alguna sala de la MISMA `categoria` (derivado de `tipos_sala.json`, sin campo nuevo de compatibilidad) y si su `riquezaTipica` (`materiales.json`) admite la riqueza de esa sala en concreto — así una `papel_pintado` (riquezaTipica:noble) nunca se cuela en una choza humilde aunque el edificio "prefiera" ese material. Se combina (no sustituye) con una tendencia por **nivel de planta** (sección 8, hasta ahora sin implementar): piedra en sótano/planta baja, madera en plantas altas. `colocarSala` acepta `materialesPreferidos`/`nivel` como parámetros nuevos y opcionales — sin ellos, comportamiento IDÉNTICO a antes (verificado: 0 llamadas directas de test cambian de resultado).

**2. Cierra el ciclo de `estado` (desgastado/roto/sucio)**: desde `catalogoContenido.js` (sección 9quater) cada instancia lleva `estado`, pero salía siempre `{false,false,false}` — "preparado desde ya sin mecánica que lo altere". Ahora `calcularEstadoDesgaste(instanceId, riqueza)` lo resuelve con un PRNG **propio** por instancia (no el `rnd` compartido de la sala — no perturba ningún orden de colocación, mismo patrón que `elegirVariante`), con probabilidad de desgastado/sucio decreciente según riqueza (humilde bastante más que noble) y `roto` solo posible si `desgastado` ya es cierto (nunca al revés). Determinista: misma semilla, mismo `estado` siempre.

**3. Layout 2D (BSP) + puertas múltiples reales — cierra un bug latente real**: `edificio.js` solo componía la planta como una fila 1D, y cuando no había pasillo cada sala solo podía tener 2 vecinas como mucho, siempre con la misma silueta alargada. Peor: cada sala intermedia ya recibía DOS aberturas en la rejilla (su propia puerta sur de `colocarSala`, apuntando al margen vacío sin uso real, más la punzada a mano con la vecina) sin que la fase de amueblado supiera nunca de la segunda. Nuevo módulo `interiores/src/bsp.js` (`empaquetarBSP`/`paresAdyacentes`): empaqueta la lista de salas YA dimensionadas (los tamaños los sigue decidiendo `colocarSala`, esto solo decide DÓNDE cae cada una) agrupando por mitades recursivamente, alternando lado-a-lado/apilado a cada nivel, con alineación de esquina fija (nunca centrada) — la alineación de esquina es lo que GARANTIZA matemáticamente que cada par de salas hermanas del árbol queda con solape real (verificado con casos adversariales de tamaños muy dispares). Cada par que queda realmente pegado recibe una puerta real de "abertura ancha" (misma técnica que `generarHabitacionCompuestaL`, sección 9ter). Salvaguarda de última instancia (rara, pero posible): si algún par queda sin solape real tras el empaquetado, un pasillo recto de 1 casilla entre centros de sala lo conecta — nunca puede partir una sala en dos porque toda sala mide como mínimo 4x4 (mismo recurso que ya usa en vivo `server/src/mundo/interiorColision.ts:garantizarConectividad`, aquí a nivel de bake). **Con pasillo, la columna vertebral de siempre se queda intacta, sin tocar** — BSP solo sustituye el caso "sin pasillo". Verificado con 480 edificios (60 tipos × 8 semillas): 0 errores, 0 salas sin detectar, 1416/1419 salas sin pasillo con 2+ puertas reales.

**4. Ventanas — cierra `sala.ventanas` (siempre vacío hasta ahora)**: post-pasada en `generarPlanta`, DESPUÉS de componer toda la planta (una sala individual no puede saber qué pared da al exterior hasta entonces) y ANTES del `detectarSalas` final. Nunca en sótano/bodega ni en `cripta` (ya documentado en `tipos_sala.json` como "bajo tierra"), nunca sobre la puerta propia de la sala ni sobre un `colgadoEnPared` ya puesto (`bordesOcupados`, expuesto ahora por `colocarSala` — antes se descartaba al salir de la función). Densidad moderada (~1 de cada 3 casillas elegibles por muro, no una pared entera de cristal), PRNG propio determinista. **Solo la parte estructural** (marca `TIPO_TILE.VENTANA` en la rejilla — la ventana combinatoria completa de forma×tamaño×marco×cristal con `aporteLuz`, sección 7bis, sigue pendiente: hoy no tiene ningún consumidor en cliente/servidor, así que no se ha inventado esa capa sin necesidad real).

**5. Sesgo anti-estrechamiento de circulación en muebles de relleno**: `distanciaALineaCirculacion()` (nueva, `colocarElementos.js`) — aproximación barata (no un grafo de visibilidad real, sería sobre-ingeniería para salas de 10-20 piezas) a la distancia entre una huella candidata y la línea recta puerta→centro de la sala. Se usa como sesgo de ORDEN (nunca bloqueo duro — `circulacionIntacta` sigue siendo la única autoridad real) en la rama genérica de `intentarPegadoAPared` (armarios/estanterías de relleno, no las anclas ni los satélites): entre huecos igualmente válidos, se prueban primero los que NO estrechan pieza a pieza el paso natural puerta→centro.

**Explícitamente descartado en este pase** (decisiones de alcance, no lagunas): optimización global tipo simulated annealing/MCMC para muebles (Merrell/Yu) — el motor ya es la familia "greedy + reglas duras + backtracking local" recomendada para este caso de uso, cambiar de paradigma sin un problema demostrado sería sobre-ingeniería; WFC para la composición de PLANTA — mismo argumento de coste/consistencia que ya llevó a construir BSP en su lugar (la FORMA de cada sala individual, que en este pase seguía siendo un rectángulo, se resolvió después con un catálogo de plantillas — NO WFC tampoco ahí, ver sección 2ter, decisión 2026-09-04); convergencia de "especie preferida" entre variantes de la misma sala (ej. dos armarios de madera tendiendo al mismo `_roble`) — se evaluó y se descartó por ser ambiguo de derivar sin datos nuevos (los sufijos de `variantesNombradas` mezclan especie real y estado de desgaste, ej. `_desgastada`/`_oxidado`, sin una lista curada que los distinga no hay forma fiable de aislar solo la especie); coordinación puertas↔mobiliario en dos pasadas (que `colocarSala` supiera de antemano dónde caerá una puerta externa para no amueblar encima) — el sistema en vivo YA tiene un colchón para esto (`interiorColision.ts` despeja el hueco+umbral de cada `puertasConexion` sea cual sea el mobiliario debajo, mismo comportamiento que ya existía para el caso "sin pasillo" de antes), así que es una mejora de calidad visual futura, no una corrección de bug.

Probado: 41/41 tests de `interiores/test/catalogo.test.js` (7 nuevos: sesgo de materiales observable y nunca ignora `riquezaTipica`, estado no siempre false y determinista, `roto` implica `desgastado`, BSP produce layout 2D real, puertas múltiples reales, ventanas nunca en bodega/cripta y sí en planta con fachada), 11/11 `editor.e2e.js` sin tocar, y de punta a punta contra el servidor real: `server/test/interiorColision.test.ts` (10/10, genera edificios reales con este motor y los carga con el loader de colisión en vivo) y `server/test/mazmorra.test.ts` (3/3) en verde sin cambios en `server/`.

## 10. Explícitamente descartado (para no reabrirlo sin motivo)

- **Muros en diagonal de verdad** — ver sección 2.
- **Orientación solar del edificio respecto al mapa** — nivel de simulación que no se nota jugando y complica la generación sin beneficio real.
- **Ventanas que renderizan vista al exterior** — el interior es instancia separada, no tiene sentido.
- **Balcones que dan al mapa exterior** — mismo motivo.

## 11. Fuera del alcance de este bakeador

- **Mazmorras/cuevas/dungeons** — geometría mucho más orgánica (BSP/autómata celular en vez de WFC con reglas de arquitectura civil), es un bakeador aparte. No reutilizar este diseño para eso sin pensarlo de nuevo.
- **Cálculo de sombras/iluminación en vivo, incluida la curva de luz ambiente por hora del día** — el bakeador solo deja los datos (posición de fuentes de luz, `aporteLuz` de cada ventana — sección 7bis); el cálculo del resultado final (cuánta luz hay en la sala a las 14:00 vs. a medianoche) es del servidor en vivo.
- **Cono de visión del jugador entre salas** — apuntado en `Backlog_Mecanicas_Futuras.md`, es cálculo en vivo del cliente/servidor según posición y orientación del jugador, no responsabilidad del bakeador.
- **El menú de construcción en sí** (arrastrar/soltar mobiliario, validar una colocación, guardarla) — apuntado en `Backlog_Mecanicas_Futuras.md`. El bakeador (sección 7ter) deja la tabla de reglas de colocación y el nivel `amueblado` que decide qué huecos quedan libres; construir la interfaz y la lógica de validación en vivo es mecánica de cliente/servidor, no del bakeador.

## 12. Estado actual

Diseño cerrado en esta fase de conversación. Catálogo (`interiores/catalogo/*.json`) ampliado a un tamaño comparable al de exteriores en su primera pasada — ver `interiores/README.md` para el recuento exacto y el detalle de qué hay ya escrito. `interiores/config/*.json` documenta con ejemplos el schema de config que consumirá el motor (sección 1), aunque el motor todavía no existe para leerlo de verdad.

Primera pieza real del motor construida y probada (`interiores/src/`): dada una sala con forma rectangular fija (todavía no WFC), `colocarElementos.js` coloca estructura + elementos respetando riqueza, `amueblado` y las reglas de colocación de la sección 7ter, con coherencia de mobiliario real (anclas/satélites, sección 3ter) en vez de piezas sueltas al azar. Probado con `prueba_render_iso.js`, que dibuja el resultado en la misma proyección isométrica 2.5D validada al principio de esta conversación (no en planta 2D).

Sistema de salas/mobiliario ampliado y ya integrado en el mismo motor (`colocarSala`), no solo diseñado en el papel:

- **Detección de sala** (sección 2bis, `salas.js`): el rectángulo del prototipo se convierte en una rejilla real de tiles (`suelo`/`pared`/`puerta`) y `detectarSalas` produce el objeto `Sala` (`sala` en el resultado) por flood-fill — método-agnóstico por diseño, listo para cuando la forma deje de ser un rectángulo.
- **Circulación intacta** (`circulacionIntacta`): cada intento de colocación en suelo (`intentarColocarEnSuelo`, `intentarJuntoAMesa`, `colocarSimetrico`) se deshace si bloquearía el paso desde la puerta — comprobado con flood-fill real, no heurística.
- **Orientación** (sección 7quater, `rotacion.js`): las piezas de suelo prueban las 4 orientaciones; huella y `tileInteraccion` rotan juntas y quedan en el resultado (`rotacion`, `tileInteraccion` en coordenadas absolutas).
- **`categoria`/`nombre`** añadido a los 39 tipos de `tipos_sala.json` (sección 5) y **`aportes`/`tileInteraccion`** añadido a ~39/17 elementos representativos de `elementos.json` (camas, superficies de trabajo, contenedores, piezas de oficio, chimenea, decoración de pared, asientos).
- **Estadísticas de sala** (sección 9bis, `estadisticas.js`): suma aditiva de `aportes` de lo colocado, adjunta como `estadisticas` en el resultado — verificado con salas reales (`comedor_grande` noble → `{funcionalidad:30, confort:37, decoracion:25}`, `taller` humilde → `{funcionalidad:200, confort:5}`, etc.).

Todo verificado con `node -e` sobre `colocarSala` (sin mismatches de `sala.tiles`, sin referencias huérfanas en el catálogo tras las nuevas entradas) y con un render isométrico real (`prueba_render_iso.js` + captura Playwright) que sigue mostrando la coherencia mesa+sillas sin regresión visual tras integrar rotación/circulación.

**Edificios multi-planta + editor manual** (sección 9ter) — segunda pieza real del motor, ya construida y probada de punta a punta, no solo diseñada:

- `edificio.js` compone `Edificio → Planta → Sala` a partir de `tipos_edificio.json` con puertas de conexión reales entre salas contiguas (fila-sobre-pasillo o solape de columna) y habitaciones en L (`generarHabitacionCompuestaL`). Probado con 132 combinaciones (las 44 `tipoEdificio` × 3 semillas cada una) sin errores y sin ninguna sala sin detectar en su planta.
- `edicion.js` da mover/rotar/eliminar/añadir/duplicar/sustituir sobre cualquier pieza ya colocada, y `regenerarMobiliario`/`Habitacion`/`Piso`/`Edificio` regeneran respetando lo ya marcado `origen: "modificado"` — verificado que una sala editada a mano sobrevive intacta a una regeneración del edificio COMPLETO.
- `interiores/gui/servidor.js` + `index.html`: editor web real (no una maqueta) con árbol de navegación, vista isométrica interactiva, edición de mobiliario y regeneración parcial. Probado de extremo a extremo con Playwright: generar edificio → seleccionar sala → añadir mueble desde la paleta → seleccionar/rotar/eliminar → cambiar tipo de sala → regenerar mobiliario/planta/edificio (confirmando que la sala editada a mano no se pierde) → guardar a disco.

**Catálogo de Contenido** (sección 9quater) — tercera pieza real, construida sobre las dos anteriores sin reescribirlas:

- `catalogoContenido.js` normaliza 128 items (118 `elementos.json` + 6 `puertas.json` + 4 combinaciones representativas de `ventanas.json`) con categoría/subcategoría/tags/reglas/dimensiones derivados, sin duplicar el JSON original. `azar.js` (nuevo) extrae `crearPRNG`/`barajar`/`elegirPonderado` de `colocarElementos.js`/`edificio.js` a un módulo hoja — necesario para que el catálogo pueda resolver variantes sin crear un require circular.
- `colocarSala` resuelve `estado`/`variante` de cada instancia en el mismo paso donde asigna `instanceId` — demostrado con `variantesNombradas` reales en `mesa_comedor`/`silla`/`armario` y verificado determinista byte a byte (misma semilla → mismo edificio, incluida la variante de cada pieza).
- `edicion.js` reemplazó su chequeo de solape hardcodeado (`capa === "suciedad"`) por `reglas` calculadas vía el catálogo, con los mismos valores por defecto — cero cambio de comportamiento para las 118 piezas existentes, comportamiento nuevo solo si una pieza futura declara un override. `rotarElemento`/`anadirElemento` respetan `rotacionesPermitidas` como aviso forzable. Nueva función `cambiarEstado`.
- `interiores/gui/`: `/api/catalogo` (categoría/subcategoría/tag/texto/sala) sirve la paleta, que dejó de ser una lista plana — navega, busca y filtra de verdad, mostrando nombre/dimensiones/tags/reglas.
- `interiores/test/catalogo.test.js` (31 tests) y `interiores/test/editor.e2e.js` (11 tests con Playwright, sirve+conduce+apaga el editor real) — ambos en verde. Regresión completa repetida tras el catálogo: 117 `colocarSala` + 132 `generarEdificio` (44 tipologías × 3 semillas) sin errores.

**Panadería sin horno (bug real, 2026-08-28)**: `salasPorPlanta` de `panaderia` pesaba `sala_comercio`/`cocina`/`despensa` casi igual (6/6/3) compitiendo por solo 2-4 huecos (`elegirNumeroSalas`, riqueza "modesta") — una semilla mala podía dejar la panadería sin sala "cocina" (30% de 30 semillas de prueba), y con ella sin `horno_pan` (que además no era `isMandatory`, así que a veces faltaba incluso con cocina real). Arreglado con dos cambios de catálogo: pesos reequilibrados a `sala_comercio:7 / cocina:8 / despensa:1` (nunca faltan las dos importantes a la vez, verificado 0/100) y `horno_pan.isMandatory:true` (ya solo puede faltar por falta de sitio físico, no por sorteo — `temasProfesion:["panaderia"]` evita que se cuele en la cocina de cualquier otro edificio). Test de regresión en `interiores/test/catalogo.test.js`. De paso se auditó qué oficios tienen una pieza `isMandatory` de verdad (yunque/fragua/fuelle de herrería, torno/horno de alfarería, altar de templo, mostrador de tienda/taberna...) y se encontraron dos huecos sueltos del mismo tipo: `telar` (sastre) y `banco_carpintero` (carpinteria) podían faltar por sorteo aunque su `taller` sí saliera — marcados `isMandatory:true` también, mismo criterio, sin tocar nada más.

**Caldero de alquimia inalcanzable (bug real, 2026-09-03, auditoría de oficios)**: `torre_mago`/`academia_magia` (`tipos_edificio.json`) llevan salas `sala_alquimia` desde su creación pero nunca declararon `temaTaller` — el filtro de `colocarElementos.js` (comentado ahí mismo: "si la sala no tiene tema asignado, las piezas con `temasProfesion` simplemente no salen") descartaba siempre el `caldero` (única pieza de esa sala con `temasProfesion:["alquimia"]`, gateado a `nivelOficioMinimo.oficio:"curandero"`) — 0/5 semillas de prueba con caldero antes del arreglo. Añadido `"temaTaller": "alquimia"` a ambos tipos de edificio (mismo criterio que el resto del catálogo: el valor ya existía como `temasProfesion` de la pieza, solo faltaba el lado del edificio) — verificado 40/40 y 17/17 salas `sala_alquimia` con caldero tras el cambio, sin tocar `colocarElementos.js`. Test de regresión: `interiores/test/catalogo.test.js` (42/42 verde).

**Lo que falta de verdad**: ~~resolver la forma de una sala individual con Wave Function Collapse (sigue siendo un rectángulo...)~~ **RESUELTO 2026-09-04, NO con WFC** — catálogo de 15 plantillas de forma (`catalogo/formasSala.json` + `src/formasSala.js`), ver sección 2ter para el diseño completo. Sigue pendiente de verdad: conducto de chimenea vertical entre plantas, colocar puertas/ventanas como instancia real desde el editor (hoy son categorías navegables del catálogo pero estructurales, sección 9quater; las ventanas de la sección 9septies son solo el hueco estructural en la rejilla, no la combinatoria completa forma×tamaño×marco×cristal con `aporteLuz` de la sección 7bis, que sigue sin consumidor), que el editor lea/escriba los config de `config/*.json` en vez de solo el estado en memoria del servidor, y un selector MANUAL de forma en el editor (hoy la plantilla de una sala ya generada solo se ve — `formaSalaId`/`mascara` del resultado —, no se puede forzar/cambiar a mano desde la UI; `regenerarMobiliario` sí conserva la forma existente al regenerar, pero cambiarla a otra plantilla requiere tocar el catálogo o la semilla).
