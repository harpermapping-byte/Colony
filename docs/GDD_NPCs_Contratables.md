# GDD — NPCs trabajadores contratables

**ESTADO: IMPLEMENTADA Y VERIFICADA (2026-09-01, ampliada el mismo día)** — reclutador fijo colocado por el jarl, contratación con coste creciente por oficio, NPC real visible en el mundo operando su mesa asignada, crafteo automático por tick (reusa `crafteo.ts` tal cual, sin reglas nuevas de validación), y salario mensual resuelto perezosamente por día de mundo, con despido automático de los más recientes cuando no llega el dinero — nunca deja Farycoins negativos. Ampliación del mismo día (pedido explícito del streamer): **"transporte" fusionado como un oficio de trabajador más** (§Fusión con transporte) y un **panel de gestión** para reasignar mesa/ruta a un trabajador ya contratado sin despedirlo (§Panel de gestión). Segunda ampliación (2026-09-01): **"tendero"** se suma como tercer oficio EXCLUSIVO de trabajador (mismo criterio que "transporte" — nunca en `OFICIOS_JUGADOR_VALIDOS`), más barato de contratar/mantener EN SOLITARIO que el resto (`OFICIO_TENDERO`/`COSTE_TENDERO_SOLO`/`SALARIO_TENDERO_SOLO`, `server/src/construccion/trabajadores.ts`) — asignado con el MISMO `trabajador:asignarMesa` de siempre a un mueble `puesto_mercado_jugador`, abre la parte pública de un tenderete de jugador; diseño completo en `docs/GDD_Mercado.md` §12.

Pedido del streamer (resumen, ver el mensaje original en el historial de la sesión): un NPC "reclutador" fijo en la ciudad capital, colocado por el jarl donde quiera. Desde él, cualquier jugador contrata NPCs trabajadores con 1+ oficios reales (más oficios = más caro), que aparecen trabajando de verdad una mesa asignada, craftean recetas reales solos sin que el jugador esté presente, y cobran un salario UNA VEZ AL MES in-game a todos los trabajadores de un jugador de golpe — si no hay Farycoins suficientes ese día, los que no cobran se despiden y desaparecen para siempre.

Este documento resume el diseño realmente construido, con el porqué de cada decisión que no venía cerrada del pedido. El "Análisis original" de `docs/Backlog_Mecanicas_Futuras.md` (2026-08-29/30) ya anticipaba la pieza como un TERCER "cerebro" sobre el mismo "cuerpo" de `server/src/mundo/agentes.ts` (rutina horaria / patrulla / **trabajador contratado**) — este documento es esa pieza, cerrada.

## 0. Relación con el "trabajador pagado" de producción pasiva (§5bis de `docs/GDD_Produccion.md`) — sistemas DISTINTOS, a propósito

Antes de esto ya existía un mecanismo separado, `plantilla:asignarTrabajador`, que activa/desactiva un booleano `trabajadorAsignado` en una plantilla de producción PASIVA (aserradero y similares) por un coste FIJO de 50 Farycoins (`COSTE_TRABAJADOR_FARYCOINS`), con un NPC "Trabajador" genérico plantado en el sitio mientras dura. Es un sistema legítimo y se deja intacto — pero no tiene oficios, no craftea recetas reales, no cuesta más por saber más, y no cobra salario periódico (se paga una vez, sigue "activo" indefinidamente).

Los NPCs de este documento son un sistema NUEVO y separado: tienen 1+ de los 10 oficios reales, craftean recetas reales validadas oficio-a-oficio, se contratan desde un reclutador (no desde el menú de cada plantilla), y cobran salario mensual recurrente. Comparten con el sistema viejo el mecanismo de "NPC fijo plantado en el sitio" (`GestorAgentes.agregarNpcFijo`) porque es la pieza correcta a reusar — pero viven en su propia tabla (`npcs_trabajadores`) y su propio protocolo (`reclutador:*`/`trabajador:*`), sin tocar el sistema viejo.

## 1. El reclutador — colocación reusando el mecanismo de NPC tutorial

En vez de construir un sistema de colocación nuevo, el reclutador es una entrada más del catálogo de NPCs fijos que YA tiene colocación en vivo, persistencia y gating de jarl resueltos: `poblacion/catalogo/npcsTutoriales.json`, categoría nueva `"reclutador"` junto a `"tutorial"`/`"lore"` (`server/src/mundo/npcsFijos.ts::NpcTutorial.categoria`). El jarl lo coloca/mueve (quitar+volver a colocar)/quita con los mismos 3 mensajes de siempre: `admin:npcTutorial:colocar {tipoTutorial:"reclutador_trabajadores"}` (en SU posición actual), `admin:npcTutorial:catalogo`, `admin:npcTutorial:quitar {id}` — jarl/superadmin-only (`puedeActuarComoJarl`), persistido en `npcs_tutoriales` (sobrevive un reinicio del servidor).

**Por qué reusar esto en vez de una tabla/mecanismo propio**: el pedido pide EXACTAMENTE lo que ese sistema ya hace ("colocable/movible/eliminable por el jarl, donde quiera, persistente") — construir un segundo mecanismo idéntico solo para poder llamarlo "reclutador" habría sido puro código duplicado. La única diferencia real es que su interacción NO pasa por el diálogo de texto de `npc:hablar` (ver §2).

**Por qué NO vía `npc:hablar`**: `npc:hablar` (con su placeholder de texto o IA) solo está cableado en `HubRoom.ts` — la ciudad capital normalmente vive en una `RegionRoom` (`docs/GDD_Ciudad_Capital.md`), que no tiene ese mensaje. Los mensajes `reclutador:*`/`trabajador:*` (§2) están registrados en `RoomExteriorBase.ts` (la base común de Hub Y Region), así que el reclutador funciona en cualquier mapa donde el jarl lo coloque, no solo en el Hub.

## 2. Protocolo — mensajes nuevos (`server/src/rooms/base/RoomExteriorBase.ts`)

| Mensaje | Quién | Qué hace |
|---|---|---|
| `reclutador:catalogo` | cualquiera | Devuelve los 10 oficios y el coste de contratar con 1..10 de ellos (`costePorCantidad`). |
| `reclutador:contratar {oficios: string[]}` | cerca de un reclutador | Cobra el coste (§3), crea la fila en `npcs_trabajadores`, el NPC aparece YA en el mundo en la posición del reclutador. |
| `trabajador:listar` | dueño | Sus propios trabajadores (para el panel de gestión). |
| `trabajador:asignarMesa {trabajadorId, construccionId}` | dueño/jarl, cerca de la mesa | Teleport instantáneo del trabajador a esa mesa (§4); limpia la receta anterior (puede que ya no aplique). |
| `trabajador:asignarReceta {trabajadorId, recetaId\|null}` | dueño/jarl | Fija qué craftea solo — valida mesa+oficio (§5). |
| `trabajador:despedir {trabajadorId}` | dueño/jarl | Borra la fila y lo quita del mundo — nunca vuelve. |
| `trabajador:asignarRuta {trabajadorId, origenConstruccionId, destinoTenderoteId\|destinoConstruccionId}` | dueño/jarl | **(2026-09-01, fusión con transporte, §Fusión con transporte)** Equivalente a `asignarMesa` pero para un trabajador de oficio `"transporte"`: firma (o reasigna) la ruta que camina en bucle. |
| `trabajador:misConstrucciones` | dueño | **(2026-09-01, §Panel de gestión)** Construcciones/propiedades REALES del jugador — para poblar los selectores de mesa/ruta del panel. |

Errores de las 6 últimas: un único canal `trabajador:error {motivo}` (incluidos los de `reclutador:contratar` — no hizo falta un canal `reclutador:error` aparte, un jugador solo necesita un sitio donde mirar). `transporte:contratar` (el mensaje LIBRE original, sin trabajador detrás) fue **RETIRADO** el mismo día — ver §Fusión con transporte; `transporte:cancelar`/`transporte:estado` se quedan (siguen operando sobre `contratos_transporte`, ahora siempre enlazados a un trabajador).

## 3. Coste de contratación — creciente por oficio adicional

`server/src/construccion/trabajadores.ts::costeContratacionTrabajador(numOficios)`: suma de `coste_base * (1 + incremento*(i-1))` para `i = 1..numOficios`, con `coste_base = 100` e `incremento = 0.5`. Con los valores por defecto: 1 oficio = 100₣, 2 = 250₣, 3 = 450₣, 4 = 700₣... — el oficio i-ésimo cuesta MÁS que el (i-1)-ésimo, así que no es solo "más caro en total" sino que cada oficio adicional pesa más que el anterior (una progresión más dura que lineal, sin llegar a exponencial). Se eligió esta fórmula simple (frente a una tabla fija o una exponencial) porque es fácil de razonar para el jugador ("cada oficio más cuesta más que el anterior") y fácil de rebalancear moviendo dos constantes.

## 4. El trabajador en el mundo — mismo mecanismo que un NPC fijo, sin caminar solo

Un trabajador contratado es, para `GestorAgentes`, exactamente un NPC fijo más (`npcTrabajadorAAgente`, `server/src/mundo/npcsFijos.ts` — mismo patrón que `npcTutorialAAgente`): un único tramo de 24h sin `camino`. Nace en la posición del reclutador; al asignarle una mesa, **teleporta instantáneamente** a la casilla de la mesa (se quita el agente y se vuelve a plantar en el punto nuevo) en vez de caminar hasta allí.

**Por qué teleport y no un paseo real** (el pedido dejaba elegir): `agentes.ts` tiene una regla dura de cabecera — "nada de A* en vivo, si el camino bakeado falta, TELEPORT". Una mesa construida por un jugador en vivo no tiene ninguna ruta pre-calculada por el bakeador offline (a diferencia de las paradas de un NPC de `poblacion/`), así que calcular una ruta ahora sería exactamente la excepción que esa regla prohíbe. El propio "trabajador pagado" de producción pasiva (§0) ya sienta el mismo precedente. Un paseo real queda anotado como posible mejora futura, no como carencia oculta.

`accion` del `NpcBakeado` distingue visualmente dos estados: `"trabajar"` (contratado, sin mesa+receta operativas — de pie, idle normal) y `"craftear"` (mesa Y receta asignadas — dispara la pose "trabajando" del rig, ver §6).

## 5. Crafteo automático — mismo `crafteo.ts` que usa un jugador, sin duplicar validación

El tick periódico (`RoomExteriorBase.tickTrabajadores`, cada 10s vía `this.clock.setInterval`, no un `setInterval` crudo — Colyseus lo pausa/reanuda con la room) recorre los trabajadores de la room con mesa+receta asignadas:

1. Si ya hay un crafteo en curso para ese trabajador (`craftesTrabajador`, un `EstadoCrafteo` — el MISMO tipo que usa `crafteo.ts` para un jugador) y `crafteoListo()` dice que terminó, deposita el resultado y libera el slot.
2. Si no hay crafteo en curso: valida que la receta siga siendo válida para esa mesa (`receta.mesas.includes(viva.objeto)`) y que el oficio de la receta esté entre los del trabajador (`puedeOperarOficio` — reusa `OFICIOS_JUGADOR_VALIDOS`, el MISMO catálogo cerrado de 10 oficios que ya valida a un jugador, requisito §6 del pedido: "reutiliza la validación que ya existe"). Comprueba TODOS los insumos antes de consumir ninguno (para no dejar un consumo parcial si falta el segundo insumo de la lista), y si alcanza, arranca `terminaEn = ahora + receta.tiempoBaseSeg * 1000`.

**Dónde vive el "inventario" del trabajador — decisión de diseño, no estaba especificado**: NO tiene un inventario propio, y NO usa el del jugador dueño. Insumos y resultado van al **almacén de la propiedad de la mesa asignada** (`tenderete_items`, vía `bd.consumirStockTenderete`/`sumarStockTenderete` — las MISMAS primitivas que ya usa `refinamiento:depositar` y el trabajador pagado de producción pasiva). Motivo: es el único sitio que ya existe, ya lo ve el jugador desde su cofre/tenderete de esa construcción, y evita inventar un contenedor nuevo por trabajador que habría que gestionar aparte. El jugador reabastece ese almacén con `refinamiento:depositar` como ya hacía para producción pasiva.

**Sin bonos de energía/nivel de oficio/poción** (a diferencia del jugador): el trabajador craftea siempre a ritmo BASE (`receta.tiempoBaseSeg`, sin `factorVelocidadPorEnergia` ni módulos de mesa ni pociones). Simplificación deliberada — el trabajador no tiene XP de oficio ni sesión de jugador de la que colgar esos bonos; se documenta aquí para que no se lea como un olvido.

## 6. Pose "trabajando" del rig (`client/src/render3d/rigHumanoide.ts`)

Mismo patrón que la pose "caído" de los cadáveres (commit reciente) y "tocando instrumento": un pose FIJO sin keyframes, disparado por un booleano nuevo en `RigHumanoide.actualizar(..., trabajando)`. De pie, inclinación hacia delante, brazos con un balanceo suave hacia la mesa. El cliente lo activa cuando `npc.accion === "craftear"` (`client/src/game.ts`, campo `EstadoJugador.trabajando`) — nunca para `"trabajar"` (contratado sin mesa/receta todavía), que se queda con el idle normal.

## 7. Panel cliente — pulido (2026-09-01, mismo nivel que `panelSastreLegendario.ts`)

`client/src/economia/panelReclutador.ts` + tecla **R** (`client/src/game.ts`) cuando hay un reclutador cerca. Mismo patrón DOM flotante que el resto de paneles pulidos de esta pasada (sin preview 3D — aquí no se genera geometría, solo se contrata/asigna):

- **Contratar**: checkboxes de oficio con el coste TOTAL y el coste MARGINAL del siguiente oficio en vivo (`costeMarginalSiguiente()`, misma fórmula que el servidor pero derivada del array `costePorCantidad` que ya manda `reclutador:catalogo` — sin duplicar la fórmula), más el salario mensual estimado — todo visible ANTES de pulsar "Contratar".
- **Mesa**: "Asignar mesa aquí" (la construcción más cercana al jugador, `RenderConstrucciones.masCercanaCualquiera`) — se mantiene sin selector con lista (mismo criterio "apuntar por proximidad" que el resto de interacciones del proyecto, p.ej. F en una puerta); no fue necesario cambiarlo para el pedido.
- **Receta**: pasó de un campo de texto libre a un `<select>` filtrado a las recetas de los OFICIOS del trabajador (`items/catalogo/recetas.json` importado directo en el cliente, agrupado por oficio una sola vez — mismo criterio que `panelSastreLegendario.ts` importa `ropa/catalogo/prendas.json`). La validación real (mesa compatible con la receta) la sigue haciendo el servidor.
- **Tus trabajadores**: cada fila muestra oficio(s), mesa/receta asignada, salario mensual y "próximo pago en N días (día X)" — calculado en el cliente a partir de `ultimoPagoDia`/`fechaContratacionDia` (que ya viajaban en `trabajador:listado`) y `diasPorMesTrabajador`, un campo NUEVO en `reclutador:catalogo` (antes solo vivía como constante del servidor, `DIAS_POR_MES_TRABAJADOR`) — evita duplicar el "30" como número mágico en el cliente. Encima de la lista, un aviso agregado del "próximo pago del GRUPO" (el ancla real que usa `resolverPayroll`, §8) con el coste total que se cobrará de golpe.
- Errores (`trabajador:error`, incluidos los de `reclutador:contratar`) se muestran dentro del propio panel, no solo en consola.
- Un botón "Contratar transporte..." sigue señalando al sistema de transporte ya existente (`transporte:contratar`, sin panel propio) — no se duplicó UI ni protocolo.

Verificado abriéndolo de verdad con Playwright contra el servidor real (§10): catálogo visible, coste antes de confirmar, contratación real, asignación de mesa+receta con el selector filtrado, indicador de próximo pago — capturas en `client/test/capturas/reclutador*.png`.

## 8. Salario mensual — cálculo perezoso por día de mundo, pagado en bloque, despido por antigüedad

`server/src/construccion/trabajadores.ts::resolverPayroll` (PURA, sin BD/Colyseus — ver tests) — mismo espíritu perezoso que `resolverIngresoDiarioNpc`: nadie corre un cron, se resuelve por comparación de días de mundo (`tiempoMundo().dia`, NO tiempo real: 1 día de mundo = 30 minutos reales) dentro del mismo tick de 10s de §5, agrupado por dueño.

- **El ANCLA del ciclo es el `ultimoPagoDia` MÁS ANTIGUO entre los trabajadores activos de un dueño**: aunque se contraten en días distintos, el primer pago sincroniza a TODO el grupo a partir de ahí — así "si tiene 10, paga a los 10 juntos ese día" (pedido literal) sigue siendo cierto para siempre después, en vez de 10 fechas de pago distintas. Un trabajador contratado a mitad de ciclo se pliega al ciclo del grupo (puede cobrar un poco antes de cumplir su primer mes completo) — efecto secundario aceptado a cambio de la simplicidad de un solo ciclo por dueño, documentado aquí en vez de escondido.
- **Si el saldo no alcanza para todos, se despide primero a los MÁS RECIENTES** (`fechaContratacionDia` descendente) hasta que el resto quepa en el saldo disponible. Decisión de diseño — el pedido dejaba elegir entre "más caros" o "más recientes primero": se eligió antigüedad porque protege a los trabajadores más asentados/productivos del jugador, la lectura más intuitiva de "se me acabó el dinero, pierdo lo último que contraté" — un trabajador de un solo día nunca debería desplazar a uno de varios meses solo por ser más barato de mantener.
- El cobro usa `ajustarFarycoins` (compare-and-swap atómico ya existente) — nunca deja Farycoins negativos por construcción, no hace falta ningún chequeo adicional.
- Los despedidos por impago se BORRAN de `npcs_trabajadores` y del mundo — "dejan de existir, no vuelven" (pedido literal), mismo tratamiento que un despido manual.

**Salario base**: `salarioMensualTrabajador(numOficios) = 15 * max(1, numOficios)` Farycoins/mes — un trabajador con más oficios también cuesta más de MANTENER, no solo más de contratar (simetría deliberada con el coste de contratación).

## 8bis. Fusión con transporte (2026-09-01) — "transporte" como un oficio de trabajador más

Antes de esta pasada, transportar bienes entre construcciones era un sistema PARALELO y separado: `transporte:contratar` (firma libre, SIN coste, `docs/GDD_Produccion.md`), reutilizado desde el mismo reclutador pero sin pasar por "oficios". El streamer pidió fusionarlo de verdad: **"transporte" pasa a ser un oficio contratable más**, con el mismo coste creciente (`costeContratacionTrabajador`) y el mismo salario mensual (`salarioMensualTrabajador`) que herrero/cocinero/etc. — un NPC trabajador con oficio `"transporte"` es quien puede operar una ruta.

**Dónde vive `"transporte"` — decisión de diseño, no estaba especificada**: se añadió `OFICIO_TRANSPORTE`/`OFICIOS_TRABAJADOR_VALIDOS` (`server/src/construccion/trabajadores.ts`) como un conjunto PROPIO del trabajador, distinto de `OFICIOS_JUGADOR_VALIDOS` (`server/src/personaje/oficios.ts`, los 10 oficios que un JUGADOR elige en sus 2 slots con XP/nivel/mesas). `oficiosValidos()` (contratación) ahora valida contra `OFICIOS_TRABAJADOR_VALIDOS = OFICIOS_JUGADOR_VALIDOS ∪ {"transporte"}`; `OFICIOS_JUGADOR_VALIDOS` en sí **no cambia**. Motivo: un jugador nunca "transporta" bienes él mismo del mismo modo que forja o talla — no hay mesa, no hay receta, no hay nivel de oficio que subir craftando. Ofrecer "transporte" como 3er oficio de PERSONAJE habría exigido inventarle sentido a XP/nivel/mesas para algo que conceptualmente es "operar una ruta", no "craftear" — el conjunto separado evita forzar esa semántica donde no encaja. Se documenta aquí explícitamente porque era la alternativa obvia (añadirlo sin más a `OFICIOS_JUGADOR_VALIDOS`) y se descartó a propósito.

**Cómo se enlaza con `contratos_transporte` — tablas separadas, unidas por id (mínima fricción con lo ya existente)**: la tabla `contratos_transporte` (ruta calculada UNA VEZ, camino cacheado, resolución económica perezosa por toque — toda esa maquinaria de `docs/GDD_Produccion.md` sigue INTACTA) gana una columna nueva, `trabajador_id INTEGER` (nullable — `NULL` en contratos previos a la fusión, que ya no pueden crearse pero pueden seguir existiendo en una BD vieja). La alternativa considerada y descartada era mover origen/destino/item DENTRO de `npcs_trabajadores` como columnas propias de una fila de oficio "transporte" — se rechazó porque habría significado: (a) duplicar en `npcs_trabajadores` casi todas las columnas que YA tiene `contratos_transporte` (camino de ida/vuelta, duración, carga por viaje, último viaje resuelto), y (b) tocar `resolverUnContrato`/`resolverContratosDeOrigen`/`resolverContratosDeDestino` (la resolución económica perezosa, ya probada y en producción) para leer de una tabla distinta según el oficio. Enlazar por id deja intacta TODA esa maquinaria — un contrato con `trabajador_id` fijado es exactamente el mismo tipo de fila que resolvía el sistema viejo, solo que ahora "pertenece" a un trabajador en vez de ser libre.

**Protocolo — `trabajador:asignarRuta` sustituye a `transporte:contratar`**: mismo cálculo (origen y destino deben pertenecer al jugador Y a esta room, `calcularCaminoRuntime` UNA VEZ, nunca A* en vivo después — la función interna `crearRutaTransporte` es literalmente el cuerpo del antiguo `manejarTransporteContratar`, movido sin reescribir la lógica) pero exige `trabajadorId` de un trabajador PROPIO con oficio `"transporte"` (`trabajadorPerteneceA` + `oficios.includes(OFICIO_TRANSPORTE)`). Reasignar la ruta de un trabajador que YA operaba una retira el contrato viejo (`desactivarContratoTransporte` + `quitarAgente`) antes de firmar el nuevo — un trabajador nunca opera dos rutas a la vez. `transporte:contratar` (el mensaje libre) se **retiró** de `RoomExteriorBase.ts`; `transporte:cancelar`/`transporte:estado` se quedan tal cual (siguen operando sobre `contratos_transporte`, que ahora siempre nace con un `trabajadorId`) — al cancelar una ruta así, el trabajador vuelve a aparecer como NPC fijo idle en su última posición conocida (mismo estado que recién contratado) en vez de desaparecer del mundo.

**Un trabajador no puede estar en dos sitios a la vez**: si un trabajador multi-oficio (p.ej. `["herrero", "transporte"]`) tiene una ruta activa y se le asigna una mesa (`trabajador:asignarMesa`), la ruta se cancela primero (mismo criterio inverso: asignar una ruta cancela la receta/mesa que tuviera). El NPC visual es uno solo — el slot `trabajadorOficio_<id>` (NPC fijo idle o trabajando su mesa) y el slot `contrato:<id>` (agente transportista caminando su ruta) nunca coexisten para el mismo trabajador; el último assignment gana.

**Salario y despido — sin caso aparte, a propósito**: `resolverPayroll` (§8) no mira los NOMBRES de los oficios en ningún punto, solo `oficios.length` para el salario — un trabajador de "transporte" se paga y se despide por impago exactamente en el mismo ciclo de grupo que cualquier otro. Al despedir (a mano o por impago automático), se retira también su ruta activa si tenía una (`desactivarRutaDeTrabajador`) — un trabajador que deja de existir no puede seguir "caminando" un contrato huérfano.

## 8ter. Panel de gestión (2026-09-01) — reasignar mesa o ruta EN CALIENTE, sin despedir/recontratar

Pedido literal: *"panel para gestión de trabajadores así podemos asignarlos a la estación o ruta que queramos entre nuestras posesiones"*. Se amplió `panelReclutador.ts` en el sitio (no se dividió en un panel aparte — un jugador con varios trabajadores necesita ver contratación y gestión juntas, y ya compartían casi todo el estado/estilo) manteniendo el mismo nivel visual que el resto de paneles pulidos de esta pasada.

- **Selectores de construcciones REALES, nunca inventados**: `trabajador:misConstrucciones` (servidor) reusa `bd.listarPropiedadesDeJugador(nombre)` (ya existía, mismo método que alimenta el panel "todo lo que tienes") + `bd.listarConstrucciones()` (ya existía, mismo método que usa `respawn.ts`/`semillaTestZone.ts`) filtrando por las propiedades del jugador — **no se inventó ningún listado nuevo**, tal y como pedía la instrucción. Cada construcción viaja con `esContenedor` (`entradaDe(objeto)?.esContenedor`) para que el cliente decida solo, sin adivinar, si un destino de ruta es un cofre (`destinoConstruccionId`) o un tenderete normal (`destinoTenderoteId`, usando su propia `propiedad`).
- **Mesa**: pasó de "la construcción más cercana a ti" (proximidad, sin selector) a un `<select>` real de construcciones propias — necesario para reasignar sin desplazarse físicamente hasta la construcción nueva, que es justo lo que pedía "asignarlos a la estación... que queramos". El servidor sigue exigiendo estar cerca de la mesa elegida (`RADIO_INTERACCION`) al confirmar — el selector no relaja esa validación, solo evita tener que caminar hasta AL LADO de cada candidata solo para verla en la lista.
- **Ruta**: dos selectores (origen/destino) de las mismas construcciones reales; el botón cambia de "Asignar ruta" a "Reasignar ruta" cuando el trabajador ya tenía una activa (`trabajador:listado` trae `rutas`, cruzado por `trabajadorId` sin una segunda ida y vuelta de red).
- **En caliente, de verdad**: ni mesa ni ruta exigen despedir primero — `trabajador:asignarMesa`/`trabajador:asignarRuta` funcionan sobre un trabajador YA contratado en cualquier momento, cuantas veces se quiera (verificado en el E2E de §10 reasignando dos veces seguidas sin recontratar).
- Un trabajador de oficio `"transporte"` muestra la sección de ruta; cualquier otro oficio (o combinación sin transporte) muestra mesa+receta como antes — la distinción es `t.oficios.includes("transporte")` en el propio panel, sin pedir nada nuevo al servidor para decidirlo.

## 9. Persistencia — tabla `npcs_trabajadores`

Mismo molde dual SQLite/Postgres que `contratos_transporte`/`npcs_tutoriales` (`server/src/datos/bd.ts`): `id, mapa_id, dueno_id, nombre, oficios (JSON), construccion_id (NULL hasta asignar), receta_id (NULL hasta asignar), x, y, fecha_contratacion_dia, ultimo_pago_dia, creado_en`. Se recrean al arrancar la room (`HubRoom.ts`/`RegionRoom.ts::listarNpcsTrabajadoresDeMapa` → `registrarTrabajadorEnMemoria`) — sobreviven un reinicio del servidor, verificado en el E2E (§10) con tres arranques reales sobre la misma BD.

**Fusión con transporte (§8bis)**: `contratos_transporte` gana `trabajador_id INTEGER` (nullable — migración `ALTER TABLE ... ADD COLUMN` en ambos backends, mismo patrón que el resto de columnas añadidas a tablas ya desplegadas). Sin columnas nuevas en `npcs_trabajadores` — un trabajador de oficio `"transporte"` no tiene mesa ni receta (`construccion_id`/`receta_id` se quedan `NULL`), su asignación vive enteramente en la fila de `contratos_transporte` enlazada por `trabajador_id`.

## 10. Verificación

- **Unit/integración** (`server/test/trabajadores.test.ts`, 11 tests): coste de contratación (marginal creciente), validación de lista de oficios (vacío/duplicados/fuera de catálogo se rechazan), `puedeOperarOficio`, `salarioMensualTrabajador` escala con oficios, `resolverPayroll` PURA (no toca pagar antes de ciclo; paga a todos de golpe con saldo de sobra; el ancla es el `ultimoPagoDia` más antiguo del grupo; despide a los más recientes primero cuando no alcanza; despide a todos y no cobra nada si ni siquiera alcanza para el más antiguo), persistencia BD completa (contratar → asignar mesa → asignar receta → listar → marcar pago → despedir, sobre sqlite en memoria), y que `ajustarFarycoins` nunca deja saldo negativo.
- **E2E real contra el servidor** (`server/test/npcs_trabajadores.e2e.mjs`, servidor+BD+protocolo Colyseus REALES, sin mockear nada, tres arranques del proceso sobre la MISMA BD sqlite para poder forzar el "día de mundo" del pago mensual sin esperar las ~15h reales que tardaría un mes de juego): el jarl coloca el reclutador (mismo mecanismo que un NPC tutorial), `reclutador:catalogo` devuelve coste marginal creciente, contratar con fondos insuficientes se rechaza SIN tocar el saldo, contratar con fondos de sobra cobra el coste exacto y el trabajador aparece YA en `state.npcs` con `accion:"trabajar"`, `trabajador:listar` lo devuelve, un jugador que no es el dueño no puede despedirlo, el dueño real sí puede y desaparece de BD y del mundo para siempre, y — el caso importante — un segundo trabajador contratado con el saldo vaciado a 0 y el día de mundo forzado (`DIA_FORZADO`) a +31 desde su contratación es **despedido automáticamente** por el tick de payroll en el arranque siguiente, sin dejar Farycoins negativos. Los 6 pasos, contra un servidor real, en verde.
- **E2E de mesa real + crafteo (2026-09-01, `server/test/npcs_trabajadores_crafteo.e2e.mjs`, sin navegador, servidor real sobre el mapa PRINCIPAL)** — la pieza que quedaba sin cerrar: una construcción `yunque_tocon` real se siembra DIRECTO en BD como atajo de test (mismo patrón ya establecido por `herreria.e2e.mjs`/`alquimia.e2e.mjs`/`cocina.e2e.mjs` para saltarse la UI de colocación estilo PZ, que ya se prueba en `docs/GDD_Construccion.md` y no es el objetivo de este E2E) con `lingote_hierro` en el almacén de su propiedad (`tenderete_items`). Confirma: `trabajador:asignarMesa` TELEPORTA al trabajador a la casilla exacta de la mesa; sin receta, `accion` sigue `"trabajar"`; una receta de un oficio que el trabajador NO tiene (`masa_pan`, molinero, sobre un herrero) se rechaza con el motivo correcto; `trabajador:asignarReceta` con `clavos_hierro` (herrero, mesa compatible) deja `accion:"craftear"` **de inmediato**, antes de que corra ningún tick — es la señal exacta que dispara la pose "trabajando" del rig en el cliente (§6); el tick de 10s arranca el crafteo consumiendo el insumo del almacén de la MESA (nunca del inventario del jugador); el tick siguiente (pasado `tiempoBaseSeg`) deposita el resultado ahí mismo, sin que ningún jugador intervenga — verificado leyendo la fila de `tenderete_items` directamente. Los 5 pasos, contra un servidor real, en verde.
- **E2E visual del panel + la pose renderizada (2026-09-01, `client/test/panelReclutador.e2e.mjs`, Playwright con Chromium real, servidor+Vite reales sobre el mapa principal, misma mesa sembrada que el E2E anterior)**: sin reclutador cerca, R no abre el panel; tras colocarlo, R muestra el catálogo con coste marginal ANTES de confirmar; contratar un herrero desde un clic REAL en el botón lo hace aparecer en "Tus trabajadores" con salario y próximo pago; "Asignar mesa aquí" + el selector de receta (dropdown filtrado a sus oficios, ya no texto libre) dejan la fila en "craftando"; en el MUNDO 3D, `window.__npcs().porSlot[...]` confirma `accion:"craftear"` y `trabajando:true` — la pose "trabajando" del rig disparada de verdad, con una captura real del NPC posado sobre la mesa (`client/test/capturas/reclutador5_pose_trabajando_en_el_mundo.png`). Sin errores de JS en toda la corrida. Los 5 pasos, contra el juego real, en verde.
- **`tsc --noEmit`** limpio en `server/` y `client/`. Suite completa de servidor: **1098/1098** (incluye los 17 tests de `trabajadores.test.ts` — 11 originales + 6 nuevos de la fusión con transporte; los E2E corren aparte, no forman parte de `npm test`).
- **Fusión con transporte — unit** (`server/test/trabajadores.test.ts`, 6 tests nuevos): `OFICIO_TRANSPORTE` NO está en `OFICIOS_JUGADOR_VALIDOS` pero SÍ en `OFICIOS_TRABAJADOR_VALIDOS`; `oficiosValidos` acepta `"transporte"` solo o combinado; `costeContratacionTrabajador`/`salarioMensualTrabajador` no tienen ninguna rama especial para "transporte" (mismas fórmulas, solo miran la cantidad); `resolverPayroll` paga/despide un trabajador de "transporte" en el MISMO ciclo que cualquiera, sin protegerlo ni penalizarlo aparte; `contratos_transporte` enlaza y desenlaza con `npcs_trabajadores` por `trabajadorId` (BD real, sqlite en memoria); un contrato histórico con `trabajadorId: null` (previo a la fusión) sigue funcionando.
- **Fusión con transporte — E2E real contra el servidor** (`server/test/npcs_trabajadores_transporte.e2e.mjs`, servidor+BD+protocolo Colyseus REALES sobre el mapa PRINCIPAL, colmena/cofres/yunques sembrados directo en BD como atajo de test — mismo patrón que `npcs_trabajadores_crafteo.e2e.mjs`): el catálogo del reclutador incluye "transporte" con el MISMO coste que cualquier oficio; contratarlo cobra exactamente ese coste; `trabajador:asignarRuta` con una construcción AJENA como origen se rechaza; una ruta REAL (colmena propia → cofre propio) queda activa — `trabajador:listar` la refleja en `rutas`, y en `state.npcs` el trabajador aparece caminando como agente transportista (`contrato:<id>`) SIN quedar duplicado como NPC fijo (`trabajadorOficio_<id>`); reasignar la ruta del MISMO trabajador retira el contrato viejo y crea uno nuevo (nunca dos activos a la vez, ni dos agentes visuales); un trabajador de oficio de MESA (herrero) se reasigna EN CALIENTE entre dos construcciones DISTINTAS (dos `trabajador:asignarMesa` seguidos, sin despedir/recontratar) y termina teleportado a la segunda, sin duplicar su NPC. Los 10 pasos, contra un servidor real, en verde.
- **Panel de gestión — E2E visual** (`client/test/panelReclutador.e2e.mjs`, ampliado el mismo día): el paso de asignar mesa pasó de "Asignar mesa aquí" (proximidad) a un `<select>` real — se verifica que el selector NO está vacío/inventado (trae la `yunque_tocon` sembrada, con una fila de `propiedades` real dando el jugador como dueño) y que elegirla + confirmar asigna la mesa igual que antes; el resto del flujo (contratar, receta filtrada por oficio, pose "trabajando" renderizada, captura real) sigue verificado de punta a punta como ya lo estaba. Todo en verde, sin errores de JS.
- Lo que quedaba pendiente en la primera pasada (mesa real, tick de crafteo, pose renderizada) sigue verificado por los E2E ya existentes — la fusión con transporte no lo tocó.
- Lo que SIGUE sin probarse: la combinación exacta "un jugador dueño real de una parcela COLOCA la mesa/construcción con la UI estilo PZ (tecla B) y LUEGO la usa desde el panel de gestión" — todos los E2E de esta familia siembran construcciones/propiedades directo en BD (atajo de test ya establecido) en vez de pasar por esa UI, que tiene su propia cobertura en `docs/GDD_Construccion.md`. No hay motivo para pensar que falle (`manejarTrabajadorAsignarMesa`/`crearRutaTransporte`/`manejarTrabajadorMisConstrucciones` solo leen `ctx.vivas`/`propiedades`, que se llenan igual venga la fila de la UI o de BD) — pero se documenta en vez de darlo por hecho.
- El panel (`panelReclutador.ts`) sigue sin arte final (mismo criterio "placeholder pulido, no diseño final" que el resto de paneles del proyecto a la espera de que el streamer apruebe un estilo visual definitivo) — pero ya cubre selectores reales de mesa Y ruta, catálogo con coste marginal, selector de receta filtrado, indicador de próximo pago por trabajador y por grupo, errores visibles en el propio panel.
- **Decisión no verificada visualmente por Playwright**: la parte de RUTA del panel (selectores origen/destino + "Asignar/Reasignar ruta") se probó por código (compila, y la lógica de servidor que consume — `trabajador:asignarRuta` — está verificada de punta a punta en el E2E de servidor de arriba) pero NO tiene un E2E de Chromium dedicado con captura, a diferencia de la parte de mesa/receta que sí lo tiene desde la pasada anterior. Se documenta honestamente: la UI de ruta en sí (el DOM, los selectores, el botón) no se ha visto renderizar en un navegador real dentro de esta pasada.
